# 0.1.245 预览恢复与文件选择隔离验收

## 报告与定位

- 用户明确：点击新建任务的“本地文件”后，系统选文件框尚未出现即卡住。
- 原 Windows 文件选择插件在平台线程调用系统 Shell。具体在同事机器上阻塞的 Shell 组件尚未取得现场证据，不能宣称已根治该组件。
- 已确认预览缺陷：加载成功前写入去重缓存、过期规格化结果覆盖新计划、合并队列把最新错误交给旧请求、真实播放器异步错误只写日志、失败代理被永久缓存。
- 进入工作台曾同时打开裸原片与多轨计划，已收敛为多轨计划单一入口。

## 预览验证

- 回归先红后绿覆盖：同步打开失败后同源重试、异步解码失败后重试、旧结果晚到、页面销毁、连续请求最后一版失败、加载时长重置与超时、代理失败恢复。
- 文件选择隔离合入后完整 Flutter 回归 4707 通过、15 跳过；静态分析无问题，正式 Release 构建退出码 0。
- 正式 Release 通过 build_app.ps1 构建、从 D:/Ishkafel/App/current 启动，不使用调试包。
- 使用真实任务 hmc91m4qqd（飞书视频，现存已切分底片、替换镜头及配乐），连续播放到 01:58 片尾，再从底片单元开始播放；没有先切换素材来恢复。
- Windows preview_health.ps1 分析本轮 101 个连续采样点：输出重建 0、硬纠偏 0、规格不一致 0、代理失败 0、音频最大滞后 59ms、最大超前 36ms、主时钟最长一跳 1000ms，退出码 0。
- 文件选择隔离版再次真实播放 48 秒，追加体检 49 个采样点：输出重建/硬纠偏/规格不一致/代理失败均为 0，最大滞后 60ms、超前 20ms、最长一跳 1000ms，退出码 0。
- 这是本机真实路径验证，不等于已在同事电脑复现并消除全部环境性故障。

## 文件选择真机验证

- 从正式桌面应用点击“新建任务 → 替换裂变 → 本地文件”，实际出现独立 Windows 文件框。
- 系统文件框取消后可重新选择；文件框打开期间，主窗口“取消文件选择”可以终止 helper，不会锁住主界面。
- 实际输入并选择 D:/Ishkafel/Media/Imports/飞书20260818-114632.mp4，中文文件名正确回填，开始分析按钮恢复可用。
- 再次打开文件框并关闭新建向导，helper 退出，主窗口保持响应；data/tmp/video-picker 无遗留目录。
- 日志实测覆盖 started、cancel/show、success/result 阶段；不记录选择路径。

## 独立诊断监测

- 真机发现桌面进程继承的 PSModulePath 与终端不同，Get-Process 和 ConvertTo-Json 的模块自动发现阻塞，不能用终端脚本运行成功代替桌面验收。
- 脚本改用 Windows 自带 .NET Framework API，不依赖 PowerShell 7、不修改系统或用户环境变量。
- 新增桌面 PSModulePath 回归先失败后通过；8 项监测回归通过。同桌面父进程环境与工作目录实测首样本耗时 0.219 秒。
- 独立复审未发现 Critical/Important 问题。最终正式 GUI 从桌面于 10:40:21 UTC 启动，10:40:22 已确认采样；watchdog.json 的 PID 22684 与实际 GUI 一致，后续每 5 秒持续采样且 responding=true。
- 本轮最后将窗口置前的自动化接口两次返回 `failed to activate captured window`，停止重复 UI 输入；应用进程仍响应，未将工具置前失败当作应用故障。文件选择与 48 秒播放验收已在同一功能构建上完成，末次重构建仅纳入监测脚本修复。

## 交付边界

- 原 0.1.244 安装目录保留于 D:/Ishkafel/App/previous-0.1.244，用户任务及素材不删除。
- 故障报告继续只收有限日志与系统/硬件信息，不上传原视频或内存转储。
- 分发包位于 D:/Ishkafel/Deliverables/ishkafel-windows-0.1.245-同事分发；ZIP 130458944 字节，MSIX 133958545 字节。test_distribution.ps1 解包/清单/哈希验证通过。
- ZIP 内监测脚本 SHA-256 与源码、正式安装目录一致；包内无 .secrets、.git、UserData 或 PDB。MSIX 未签名，仅作格式验收；同事使用便携 ZIP。
- 最终默认并发完整测试出现两项旧 FrameCheckCache 测试 did not complete，退出码 1；随后该文件独立 7 项通过。停止同时构建打包并降低并发至 4 后，完整复跑 4708 通过、15 跳过，退出码 0；未跳过失败用例或修改断言。该次未完成的确切测试运行器原因尚未确认。
