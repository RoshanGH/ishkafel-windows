# 0.1.243 诊断报告验收

## 结果

- `flutter analyze --no-pub`：无问题。
- 全量 `flutter test --no-pub --reporter expanded`：4665 通过、15 跳过。
- Python 3.12/Pillow 12.3.0 服务测试：6 通过；部署镜像中再次执行同样 6 项通过。
- 正式 `scripts/windows/build_app.ps1 -Mode Release` 成功；独立诊断 exe 也在正式脚本中生成。
- `package_app.ps1 -SkipBuild` 生成 ZIP 和未签名开发 MSIX；对员工只分发 ZIP。
- 独立代码审查三项 Important（JSON 密钥、helper 重传、截断图片）均修复并复查通过。

## 真机/线上

- 通过 Windows 实际界面点击提交，不填写描述、不选截图；最终报告 `08df8c9e58e37dda83b8abf526c51d0a` 在 MySQL 中可读。
- 收到 99529 字符日志，CPU i5-12400F、NVIDIA RTX 3060 及驱动、内存、磁盘字段齐全。
- 初次真机遇到 PowerShell/CIM 冷启动约 17 秒，8 秒上限漏硬件；调整为进程 30 秒、外层 35 秒，UI 显示等待预期后复测通过。
- 独立工具真实提交 `0526d42f91bb1ae12596c39b1eeae25d`；R 重传入口可用。服务测试验证断网失败保留、同编号重传确认后删除。
- 监测脚本实际捕获测试进程退出码 7；真实 GUI 活动状态也产生 5 秒采样记录。
- HTTPS 对 IP 和专用 CA 的校验成功；提交 token 读取返回 403；处理 token 可回写状态；相同编号同内容重发 200，不重复入库。
- 更新后的线上服务拒绝仅含 PNG 魔数的图片（400），实际生成的 1x1 PNG 上传和读回一致。
- 文本对当前已加载 AI/更新/提交密钥的扫描通过；正式 app.so 与独立 helper 中无报告读取/处理 token。

## 预览回归

手动打开真实任务 hmc91m4qqd，使用原始飞书视频与现有替换方案连续播放约 54 秒。
将本次播放日志交给 Windows `preview_health.ps1 -AnalyzeLogPath`：55 个采样点；输出重建 0、音频硬拽 0、未统一规格 0、代理失败 0；最大音频滞后 71ms（阈值100），领先0ms，最长主时钟步长1000ms（阈值1150）；全部通过。

## 说明

中途与构建并行的一次全量运行有既有缓存测试 did not complete；该测试单独复跑 7 项通过，随后最新全量 4665 项全部通过，未改缓存实现。
没有模拟真实显卡驱动崩溃，不将监测退出码等同于原生崩溃堆栈。截图可能含敏感业务内容，只由用户主动选择。
