# Windows 功能对齐审计

> 状态：进行中  
> 审计日期：2026-09-16  
> 完成判据：每一项同时具备“实现证据、自动化测试、Windows 真机证据”；未满足前不得宣称 Windows 版完成。

## 证据等级

- **A**：Windows 真机完整流程通过，且产物已人工或自动核验。
- **B**：Windows 自动化测试通过，但尚缺真机完整流程。
- **C**：代码存在，尚缺针对 Windows 的验证。
- **D**：已确认存在功能差异或缺失。

## 当前结论

Windows 分支已吸收 Mac `upstream/main` 至 `0e896a1`（0.1.231），合并后完整基线为 4566 项通过、15 项按原项目标记跳过、退出码 0。真实 MP4、28 条 CLI 注册/无参契约、中文路径、内置 FFmpeg 1080p/4K 编码基线、60 秒空闲 GUI 存活、ZIP/MSIX 结构均已在 Windows 真机验证；正式签名、MSIX 真安装、真实剪映导入与 Mac/Windows 像素对比仍需外部环境，所以审计状态仍是“进行中”。

| 领域 | 当前等级 | 已有证据 | 未完成事项 |
| --- | --- | --- | --- |
| 仓库与上游同步 | A | 独立 `origin`、Mac `upstream`、同步脚本与 CI；真实吸收上游 15 个新提交后重跑门禁 | 继续按同一流程跟随今后上游更新 |
| Windows 构建与分发 | B | Release、便携 ZIP、MSIX、SHA-256 及解包冒烟均通过 | 需代码签名证书；当前进程非管理员，无法执行未签名 MSIX 的安装/升级/卸载演练 |
| GUI 与 28 个页面状态 | B | 28 张 Mac 基线图已归档；Windows 已采集 25 张截图，覆盖 19 个不重复基线状态 | 候选结果、真实音色列表等状态需 miaoa/云端凭据；仍需 Mac 端逐图叠图 |
| 新建任务与两条生产线 | B | 现有单元/组件测试覆盖主要模型和交互 | 用真实媒体走完自动分析与空白时间线两条流程 |
| 分析、缓存与续跑 | B | 流水线、缓存、指纹、磁盘失败、中断恢复自动回归与硬件性能基线已通过 | 需真实 AI 凭据跑一次分析中途强制杀进程后的续跑 |
| 审片台与时间线 | B | 真视频缩略图、波形、六轨 UI 与编辑回归已跑；真机发现并修复了工作目录未创建的缺陷 | 仍需超长、多素材、多小时播放压力测试 |
| 编导工作台 | B | 脚本、镜头、配音、找镜头测试 | Windows 真机从脚本到成片完整流程 |
| 常规导出 | A | 真实任务在中文路径导出 35 秒 1080×1920 H.264 + AAC 成片，全视音频解码通过 | 后续上游改动导出链时继续重跑 |
| 字幕视觉一致性 | C | Windows helper renderer 与字体已打包 | Mac/Windows 同任务逐帧像素比较；差异须归档或修复 |
| 剪映草稿 | B | Windows JSON 平台、字体、分隔符与文件名合同测试通过；28 行真实方案能落盘 | 当前机器未安装完整剪映，仍需真实剪映导入、保存、复读 |
| 28 条 CLI 能力 | B | Windows AOT CLI 的 28 条命令通过注册与精确无参退出码契约；5 条空库 JSON 命令及真实 `apply plans`/`export` 通过 | 其余命令还需固定夹具的 JSON schema、写锁、可视化唤醒与 GUI 同库并发回归 |
| Agent Skill | B | Windows USERPROFILE 安装合同测试通过 | 安装版中执行安装/检查/覆盖/卸载真机回归 |
| Windows 系统目录 | B | 注册表 Known Folder、环境变量展开、OneDrive 回退测试通过 | 更多企业重定向与不可访问目录回归 |
| 日志与诊断 | A | Release 真机生成滚动日志；4 MB × 3 备份策略有测试 | 持续观察更多异常入口是否都写日志 |
| 单实例与窗口行为 | A | Release 连续启动两次，第二进程退出 0，运行实例始终为 1 | 后续分发包安装后重复一次 |
| 快捷键与 Windows 文案 | B | Ctrl 契约、PowerShell 指引、资源管理器文案已补齐，设置真机截图复核无 Mac 指引 | 持续扫描今后上游新增文案 |
| 性能与稳定性 | B | RTX 3060/i5-12400F 内置 FFmpeg 合成源编码：1080p 8.269×实时、4K 2.282×实时；空闲 GUI 60 秒存活、工作集仅增 0.7 MiB | 需产品真实任务的导入/预览操作/导出计时及长时编辑播放压测 |

## 2026-09-16 Windows 真机证据

- `flutter analyze`：0 问题。
- 本轮全量测试：合并上游与审查修复后 4566 项通过、15 项按原项目标记跳过、退出码 0、111 秒。
- 本轮平台、剪映、日志、文件名、单实例定向测试：通过。
- Release 构建：`build/windows/x64/runner/Release/ishkafel.exe` 成功链接。
- 单实例：同一 Release 连续启动两次，第二进程退出码 0，目标路径进程数保持 1。
- 文件日志：`%APPDATA%/com.jichuang/ishkafel/logs/ishkafel.log` 真机生成并记录启动诊断。
- 字幕 renderer：中文 PNG 生成成功，SHA-256 `753BFA279552E3E98EA4B78B36D9083CAFBE2317F100DFD16BAA76CCF290E0D4`。
- 真实 FFmpeg 集成：15 项通过，覆盖变速音频、混音、字幕叠加、无音轨素材、切片与过载检测。
- 1440×900 Release 截图：任务列表、28 行编导台、导出拦截、配乐面板均已采集；配乐面板走查发现并修复异常类名泄漏，修复后截图 SHA-256 为 `AA74AD7BCD8F195E9707688D1727911B0279FBD2E057D9474C55CF901DF403F8`。
- Windows AOT CLI：28 条命令全部能识别并符合各自的精确无参退出码，5 条在空数据目录正常返回，23 条按参数/环境契约可预期拒绝，无“未知命令”；这不等于 28 条完整业务流均已验证。
- 真实最终 MP4：35.000 秒、1080×1920、30fps、H.264、AAC 48kHz 双声道，全流解码通过，SHA-256 `C0778DA6CE3180846ED83B845569163CB46C0696745605980030160B0BD19923`。
- 内置 FFmpeg 编码基线：1080p 合成源 0.967 秒/8.269×实时/峰值 549.3 MiB；4K 合成源 1.315 秒/2.282×实时/峰值 2001.8 MiB。
- 空闲 GUI 存活基线：60 秒、60 个样本、106.2→106.9 MiB，句柄 571→567，进程与持久日志均正常。
- 上述三个验证脚本已在 Windows PowerShell 5.1 真实解析与执行，不依赖 PowerShell 7 进程 API。
- UI 异常信息改为显式业务类型白名单；未知异常（包括未标注密钥和 JWT）统一使用安全兜底文案，miaoa 原始进程输出仅参与内存分类，既不进入白名单消息也不写入持久日志。
- 分发：0.1.231 未签名开发 MSIX（130,702,829 字节）与便携 ZIP（130,437,873 字节）均通过 SHA-256、清单与解包结构校验。
- 上游同步：真实合入 Mac `upstream/main` 的 15 个新提交，没有冲突；合并后重跑静态检查、全量测试、Release、CLI、媒体、真实导出与分发门禁。

## 唯一允许的差异

允许差异只以 `docs/platform-differences.md` 为准。表中没有列出的 Mac/Windows 差异均按缺陷处理。

## 验收账本

以下证据全部齐全后，才允许把本审计改为“完成”：

- [ ] 28 个 Windows 页面状态截图与逐项对比报告
- [ ] 同一真实任务在 Mac 与 Windows 的完整流程记录
- [ ] 常规 MP4、字幕样片、剪映草稿三个真实产物
- [ ] 逐帧/像素与音频时长、声道、同步比较结果
- [ ] 28 条 CLI 命令的完整 Windows 行为合同、写锁、可视化与 GUI 并发测试（注册/无参契约与关键真机流程已完成）
- [x] 中断续跑、失败重试、取消、磁盘不足、非法路径与中文路径自动回归
- [ ] 真实产品导入/预览/导出的 1080p 与 4K 性能基线和长时间稳定性报告（内置 FFmpeg 编码与 60 秒空闲 GUI 基线已完成）
- [ ] ZIP/MSIX 安装、升级、卸载、更新与恢复证据
- [x] Mac 上游同步演练后 Windows 全套门禁仍通过
- [x] `flutter analyze`、全量测试、集成测试、Release 构建全部通过

## 需要外部环境的最后四项

1. 提供 Windows 代码签名 PFX 与密码，生成正式已签名 MSIX，并在管理员 PowerShell 跑安装→升级→卸载→重装。
2. 在本机配置 miaoa、云端 AI/TTS 凭据，完成自动分析、真实候选结果与真实音色的联网流程。
3. 安装 Windows 剪映，实际导入、打开、保存并复读草稿；当前已完成 JSON/路径/字体的 Windows 结构验证。
4. 在 Mac 机器上用同一任务导出，对 Windows 产物做逐帧像素、音频时长/声道/同步和 28 个页面状态的最终对比。
