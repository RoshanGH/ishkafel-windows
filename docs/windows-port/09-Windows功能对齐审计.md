# Windows 功能对齐审计

> 状态：进行中  
> 审计日期：2026-09-16  
> 完成判据：每一项同时具备“实现证据、自动化测试、Windows 真机证据”；未满足前不得宣称 Windows 版完成。

## 证据等级

- **A**：Windows 真机完整流程通过，且产物已人工或自动核验。
- **B**：Windows 自动化测试通过，但尚缺真机完整流程。
- **C**：代码存在，尚缺针对 Windows 的验证。
- **D**：已确认存在功能差异或缺失。

## 当前结论

Windows 分支已吸收 Mac `upstream/main` 至 `0e896a1`（0.1.231）。本轮使用 93.25 秒真实竖屏 HEVC/AAC 视频完成联网 AI 分析、人工审片、保存恢复、人声分离、MiaoA 配乐、两次成片导出和剪映 11.4.2 导入/播放；完整基线为 4567 项通过、15 项按原项目标记跳过、退出码 0。Release、便携 ZIP、MSIX 安装/启动/升级/卸载/重装、真实媒体和性能稳定性均已在 Windows 真机验证。正式代码签名及 Mac/Windows 同任务像素对比仍待完成，所以审计状态仍是“进行中”。

| 领域 | 当前等级 | 已有证据 | 未完成事项 |
| --- | --- | --- | --- |
| 仓库与上游同步 | A | 独立 `origin`、Mac `upstream`、同步脚本与 CI；真实吸收上游 15 个新提交后重跑门禁 | 继续按同一流程跟随今后上游更新 |
| Windows 构建与分发 | B | Release、便携 ZIP、MSIX、SHA-256、解包冒烟，以及管理员模式下安装/CLI/GUI 启动/升级/卸载/重装均通过 | 正式公开分发仍需受信代码签名证书 |
| GUI 与 28 个页面状态 | B | 28 张 Mac 基线图已归档；Windows 已采集真实任务流程截图，覆盖分析、审片、配乐、导出及重启恢复 | 仍需 Mac 端逐图叠图 |
| 新建任务与两条生产线 | B | 真实媒体已走完联网自动分析；空白时间线由自动化测试覆盖 | 空白时间线仍需再做一次真人完整流程 |
| 分析、缓存与续跑 | B | 真实 AI 分析得到 7 个单元/13 个镜头；流水线、缓存、指纹、磁盘失败、中断恢复已有自动回归 | 真实联网分析中途强杀后的续跑仍待验证 |
| 审片台与时间线 | A | 真人播放、选中镜头、微调边界、手动恢复、自动保存、关闭重启恢复均已验证 | 后续增加多小时、多素材压力测试 |
| 编导工作台 | B | 脚本、镜头、配音、找镜头测试 | Windows 真机从脚本到成片完整流程 |
| 常规导出 | A | 93.23 秒真实任务两次导出 1080×1920 H.264 + AAC；原声版及“分离人声+MiaoA 配乐”版全流解码通过 | 后续上游改动导出链时继续重跑 |
| 字幕视觉一致性 | C | Windows helper renderer 与字体已打包 | Mac/Windows 同任务逐帧像素比较；差异须归档或修复 |
| 剪映草稿 | A | 剪映 11.4.2 识别真实草稿；关闭重启后仍出现在首页，进入编辑器可见视频/音频/字幕轨，并已实际播放和跳转 | 后续上游改动草稿格式时继续重跑 |
| 28 条 CLI 能力 | B | Windows AOT CLI 的 28 条命令通过注册与精确无参退出码契约；5 条空库 JSON 命令及真实 `apply plans`/`export` 通过；MSIX 安装版 `ishkafel.exe` 执行别名实机启动 | 其余命令还需固定夹具的 JSON schema、写锁、可视化唤醒与 GUI 同库并发回归 |
| Agent Skill | B | Windows USERPROFILE 安装合同测试通过 | 安装版中执行安装/检查/覆盖/卸载真机回归 |
| Windows 系统目录 | B | 注册表 Known Folder、环境变量展开、OneDrive 回退测试通过 | 更多企业重定向与不可访问目录回归 |
| 日志与诊断 | A | Release 真机生成滚动日志；4 MB × 3 备份策略有测试 | 持续观察更多异常入口是否都写日志 |
| 单实例与窗口行为 | A | Release 连续启动两次，第二进程退出 0，运行实例始终为 1 | 后续分发包安装后重复一次 |
| 快捷键与 Windows 文案 | B | Ctrl 契约、PowerShell 指引、资源管理器文案已补齐，设置真机截图复核无 Mac 指引 | 持续扫描今后上游新增文案 |
| 性能与稳定性 | B | RTX 3060/i5-12400F 内置 FFmpeg 合成源编码：1080p 8.928×实时、4K 2.227×实时；真实 93 秒任务完成 AI、人声分离、预览和两次导出；GUI 60 秒存活 | 仍需多小时编辑播放压测及真实导出的稳定计时基线 |

## 2026-09-16 Windows 真机证据

- `flutter analyze`：0 问题。
- 本轮全量测试：4567 项通过、15 项按原项目标记跳过、退出码 0；新增 Windows 人声分离安装回归测试。
- 本轮平台、剪映、日志、文件名、单实例定向测试：通过。
- Release 构建：`build/windows/x64/runner/Release/ishkafel.exe` 成功链接。
- 单实例：同一 Release 连续启动两次，第二进程退出码 0，目标路径进程数保持 1。
- 文件日志：`%APPDATA%/com.jichuang/ishkafel/logs/ishkafel.log` 真机生成并记录启动诊断。
- 字幕 renderer：中文 PNG 生成成功，SHA-256 `753BFA279552E3E98EA4B78B36D9083CAFBE2317F100DFD16BAA76CCF290E0D4`。
- 真实 FFmpeg 集成：15 项通过，覆盖变速音频、混音、字幕叠加、无音轨素材、切片与过载检测。
- 1440×900 Release 截图：任务列表、28 行编导台、导出拦截、配乐面板均已采集；配乐面板走查发现并修复异常类名泄漏，修复后截图 SHA-256 为 `AA74AD7BCD8F195E9707688D1727911B0279FBD2E057D9474C55CF901DF403F8`。
- Windows AOT CLI：28 条命令全部能识别并符合各自的精确无参退出码，5 条在空数据目录正常返回，23 条按参数/环境契约可预期拒绝，无“未知命令”；这不等于 28 条完整业务流均已验证。
- 真实输入：`飞书20260818-114632.mp4`，93.251 秒、720×1280、30fps、HEVC + AAC 44.1kHz 双声道，全流解码通过，SHA-256 `86AF5AEE0510B2638A4509AEB328182E5F55D6E6928FADF1C56C78C500E8E8D7`。
- 官方 MiaoA CLI 0.4.0 已安装并登录生产租户/项目；真实 AI 分析生成 7 个单元、13 个镜头，13/13 镜头有标签和描述，9 个灰场候选经人工审阅移除。日志与文档不保存 token、签名 URL 或交接凭据。
- 真人 GUI 路径：播放真实时间线和波形，选中 U2/S4，将起点 46.26 秒微调到 46.27 秒并恢复至 46.26 秒；自动保存后关闭应用再启动，任务、7/13 结构、时间线和修剪值均恢复。
- 人声分离：真实 93.3 秒音频使用 UVR-MDX-NET-Inst_HQ_3 模型完成 11 段 CPU 推理，得到人声和背景两个 WAV。真机由此发现 PATH 上 Python 3.7 会让 `uv` 解析失败，以及 `audio-separator` 0.47.0 漏声明 `audioread`；Windows 安装配方已固定隔离 Python 3.12 并显式补依赖，回归测试覆盖实际参数。
- 真实原声成片：93.232 秒、1080×1920、30fps、H.264、AAC 48kHz 双声道，全流解码通过，SHA-256 `865E5657FCCA7AA375D83DD9AACE6AD5DFAD5D60CF80AD9552FF8C10707FC84D`。
- 真实分离人声 + MiaoA 配乐成片：93.232 秒、1080×1920、30fps、H.264、AAC 48kHz 双声道，全流解码通过，SHA-256 `FAC600857F552553864F5DC35C82DB8125162BE7628E47D926880310FC5A48BB`；与原声版 U1 解码 PCM 哈希不同，音频差值 RMS 为 -8.986 dB。
- 剪映 11.4.2 真人互通：关闭重启剪映后草稿仍显示为 93.3 秒/2 轨/1 素材；打开编辑器可见视频、音频、字幕轨，实际播放至 5.06 秒并跳转至 21.06 秒。
- 内置 FFmpeg 编码基线：1080p 合成源 0.896 秒/8.928×实时/峰值 548.9 MiB；4K 合成源 1.347 秒/2.227×实时/峰值 2002.1 MiB。
- GUI 存活基线：60 秒、60 个样本、110.9→112.6 MiB，峰值 112.6 MiB，句柄 595→585，进程与持久日志均正常。
- 上述三个验证脚本已在 Windows PowerShell 5.1 真实解析与执行，不依赖 PowerShell 7 进程 API。
- UI 异常信息改为显式业务类型白名单；未知异常（包括未标注密钥和 JWT）统一使用安全兜底文案，miaoa 原始进程输出仅参与内存分类，既不进入白名单消息也不写入持久日志。
- 分发：0.1.231 未签名开发 MSIX（130,706,072 字节）与便携 ZIP（127,261,805 字节）均通过 SHA-256、清单与解包结构校验。
- 第一次管理员生命周期实测发现包内 CLI 未注册执行别名，直接从 WindowsApps 目录启动被拒绝。清单现按 Windows 规范注册 `ishkafel.exe` 控制台别名并声明多实例，分发冒烟会检查最终 MSIX 而非只检查模板。
- MSIX 生命周期：开发包初装 `0.1.231.231` 后，安装版 CLI 与 GUI 均成功启动；随后升级到 `0.1.231.232`、卸载、重装初始版本并最终卸载，报告中的六项预期真值均为 `true` 且 `finalPackagePresent=false`。
- 上游同步：真实合入 Mac `upstream/main` 的 15 个新提交，没有冲突；合并后重跑静态检查、全量测试、Release、CLI、媒体、真实导出与分发门禁。

## 唯一允许的差异

允许差异只以 `docs/platform-differences.md` 为准。表中没有列出的 Mac/Windows 差异均按缺陷处理。

## 验收账本

以下证据全部齐全后，才允许把本审计改为“完成”：

- [ ] 28 个 Windows 页面状态截图与逐项对比报告
- [ ] 同一真实任务在 Mac 与 Windows 的完整流程记录（Windows 端已完成）
- [x] 常规 MP4、字幕轨、剪映草稿三个真实产物
- [ ] 逐帧/像素与音频时长、声道、同步比较结果
- [ ] 28 条 CLI 命令的完整 Windows 行为合同、写锁、可视化与 GUI 并发测试（注册/无参契约与关键真机流程已完成）
- [x] 中断续跑、失败重试、取消、磁盘不足、非法路径与中文路径自动回归
- [ ] 真实产品导入/预览/导出的 1080p 与 4K 性能基线和长时间稳定性报告（真实 1080p 流程、合成源 1080p/4K 编码与 60 秒 GUI 基线已完成）
- [x] ZIP/MSIX 安装、升级、卸载、更新与恢复证据
- [x] Mac 上游同步演练后 Windows 全套门禁仍通过
- [x] `flutter analyze`、全量测试、集成测试、Release 构建全部通过

## 需要外部环境或人工授权的最后事项

1. 正式公开发布前提供受信任的 Windows 代码签名证书，并复跑相同生命周期。
2. 在 Mac 机器上用同一任务导出，对 Windows 产物做逐帧像素、音频时长/声道/同步和 28 个页面状态的最终对比。
