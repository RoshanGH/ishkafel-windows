# 0.1.244 截图粘贴与报告提交验收

## 交互

- 报告框 Ctrl+V / Shift+Insert 读取截图，预览、移除，无选择文件或粘贴按钮。
- Windows 原生 PNG 与系统位图通道；图片限 4 MB、900 万像素、单边 8192。
- 只主动粘贴时读取剪贴板；提交前图片保留内存；普通文字粘贴保留。
- 用户在正式 Windows app 中确认截图粘贴可用。
- 粘贴中的提交/重传暂禁，防止附件尚未返回时发送旧内容。

## 真实提交

- 原报告编号：`39b0aca261feea134b1eae0ef85703b8`。
- 首次 GUI 提交失败，本地原报告完整保留，大小 160069 字节。
- 独立 Dart HTTPS 探针首次复现 `HandshakeException: Connection terminated during handshake`；同一环境随后重试成功，未改服务器配置、证书校验或原报告。
- 从服务端读回：PNG 24907 字节，图片 Base64 与本地逐字一致，说明文本一致，日志 102354 字符，系统和硬件字段存在。
- 在 GUI 点击“重试发送”后显示同一报告编号成功；本地待发文件清除；服务端仍只有一条，验证重复发送幂等。
- 握手中断的更底层网络原因未确定，不能声称已根治；现有可见失败、保留和手动重传机制已验证有效。

## 构建与审查

- `flutter analyze --no-pub` 无问题；定向诊断测试 7 项通过。
- 完整回归 4667 通过、15 跳过。
- 正式 Release 构建、便携 ZIP 打包完成；未签名 MSIX 不用于同事分发。
- 独立审查发现并修复 PNG 尺寸绕过与粘贴/提交竞态，复核通过。
- 本次不修改预览、音视频或任务数据。上一版本保留在 D:/Ishkafel/App/previous-0.1.243。
