# ishkafel 项目配置

## 项目简介

素材生产平台（替换裂变 / 脚本成片）：Flutter 跨平台桌面 app + libmpv 预览 + 本地 ffmpeg 合成 + 云端 AI API + miaoa 素材检索。
核心概念与流程见 `docs/术语表.md` 与 `docs/2026-07-29-项目方向与架构设计.md`。

## Windows 独立仓库规则（强制）

- 本仓库身份是 `RoshanGH/ishkafel-windows`：`origin` 指向 Windows 独立项目，
  `upstream` 指向 Mac 主项目 `RoshanGH/ishkafel`。上游更新只通过同步 PR 吸收，绝不自动合并。
- `docs/platform-differences.md` 是允许差异的唯一清单；**未登记的平台差异 = bug**。
- Windows 原生交互遵循 Windows 11 桌面习惯；原文中的 Apple HIG 要求继续约束信息架构、
  密度与品质，但不得覆盖标题栏、Ctrl 快捷键、资源管理器、文件对话框、滚轮和 DPI 等 Windows 契约。
- 回复、注释与提交信息仍用中文；PowerShell 脚本必须兼容 Windows PowerShell 5.1 的文本编码。
- 正式 Windows GUI 构建只能使用 `scripts/windows/build_app.ps1`，CLI 构建使用
  `scripts/windows/build_cli.ps1`，不得在交付时裸跑 `flutter build windows` 冒充正式包。
- 每个代码波结束都要运行 Windows 定向测试、`flutter analyze`、完整 `flutter test`、
  Release 构建并真实启动应用验收。若已知 P0 尚未消红，要如实列出退出码，不得声称完成。
- `.secrets` 的内容不得进入终端日志、Git、CI artifact、issue 或 PR；缺失时只报告文件名。

## 这个软件是干什么的（所有取舍的最终依据）

**把一个熟练编导挑镜头的判断力，做成任何人都能调用的东西。**

下面四段是产品负责人的原话与定调。**遇到拿不准的取舍——某个功能该不该做、
某处该拦还是该放、某个东西该给谁——回来对照这里，而不是凭手感。**

**一、给谁用、替他干掉哪一段**

给所有要做视频的人——剪辑、编导，不限于会用专业工具的那批。他在这里选素材、
写脚本、把画面配上，然后带一份工程跳去剪映精修。**选素材、写脚本、配画面**
这一段最耗经验、最累人，也最难自动化，正是这个软件要接管的部分。

**二、为什么必须是 Agent，而不是自动化脚本**

一条片子要挑 25~45 个镜头。每一镜都要看图判断：像不像参考镜、跟前后连不连得上、
素材上烧没烧别人的字、露的是不是竞品、够不够铺满这一句的时长。**这是判断，
不是规则**——写死规则挑出来的东西看一眼就知道不能用（「自动铺一版」就是那个
规则版本，它只配当草片）。

所以要让 Agent 来做，前提是**把编导操作台上的每一个旋钮都交到它手上**，
事实也一样不少地摆给它（参考图、ASR 台词、画面描述、时长、烧字、品牌）。
**软件提供能力和保护，判断权归 Agent。**

推论（这几条都是踩出来的）：

- **人在界面上能拧的每个旋钮，Agent 都要能拧**。少一个，它就只能被动接受
  软件给的默认值——那又退回成规则了
- **软件不替 Agent 做判断，也不背着它自己干活**。Agent 可以调用「自动铺一版」
  这样的整装功能，但它真正该做的是自己一步步调底层能力
- **播报只报 Agent 在做什么**。软件自己跑的活儿不许占那条通道——占了，
  人就分不清是谁在动手

**三、可视化模式站在什么位置**

**终局是静默执行**，但通往静默只有一条路：**人得能从头跟着看它是怎么想的。**

> 人需要去干涉的时候，你只给他最后的成片去干涉，就已经来不及了。他需要从头
> 开始干涉。他如果从头跟了你两遍，发现你整个找视频的逻辑都没有问题，
> 那后面就可以写 skills 总结，让 Agent 直接静默执行了。

所以**可视化验的是它的判断逻辑对不对**，不是「它做完了没有」。这决定了：
每一步都要说人话、慢步骤要有带分母的进度、界面要跟着它走、
正在动哪一行要一眼看得出来。详见 `docs/可视化验收标准.md`。

**四、所以才有下面那些「过分」的要求**

出口是**要交付给客户的片子**。一条两层字幕打架的、一条跨品牌的、一条前后音色
不一样的，都是要出事的。所以宁可停下来点名，也不许静默降级、不许悄悄少一段、
不许拿默认值顶上。

还有一条只能靠人盯出来：**「能跑通」离「能用」还有很远**。流程全绿、测试全过、
片子导得出来，但人一听就知道情绪不对、一看就知道镜头切碎了——这类问题不会出现
在任何错误日志里。所以产品负责人会自己上手跑、自己盯屏幕，**他指出来的每一条
都要当成真问题去查根因**，不要先怀疑他看错了。

## 最高准则：按商业化 To C 软件的标准做（强制，凌驾于其他所有条目）

**这个 app 虽然不对外销售，但必须做到可以直接拿去卖的品质。** 任何实现、评审、
验收，先问一句「这东西如果是花钱买来的商业软件，用户能忍吗」——不能忍就是不合格，
哪怕功能上「能用」。这一条优先级最高；与其他条目冲突时以本条为准。

具体到可执行的判据：

- **不做一次性的脏活**：算过一次的东西要落地复用，改动了才重算。每次进页面都
  重跑一遍几十次 ffmpeg、每次都重下一遍素材，用户看到的是「这软件真慢」。
- **不留孤儿数据**：磁盘上躺着的每一份数据都要有人读、有人删。中间产物用完即弃，
  归属不到现存对象的一律清掉，缓存要有配额。产物清单收敛成唯一一份
  （见 `TaskArtifacts`），新增一种产物只改那一处。
- **不静默降级**：影响最终成片的东西出了错就直接失败并点名，不许悄悄少一段、
  少一句、或用一个默认值顶上。可降级的只有预览这类「人还在看着」的环节，
  且必须把降级说出来。
- **状态必须可见且可操作**：后台在干活就说清在干什么、到第几步；失败要给原因和
  重试入口。转圈不说话、点了没反应、失败只写日志，都是不合格。
- **每一次等待都要有交代**：超过一两秒的操作要有进度或预期；能提前做的提前做，
  能省的省掉。
- **数据不能凭空消失或凭空错**：外部依赖（素材库、签名地址、登录态）随时会变，
  凡是进入用户方案的东西都要固定到本地；缓存复用一律按内容指纹判定，
  绝不能按「文件名存在」就当命中——那会静默放出上一个版本的内容。
- **自己先验一遍**：UI 改动要真机跑起来截图核对，不允许只报「测试通过」。

## 设计标准（强制）

**本 app 的 UI/UX 必须符合 Apple 人机界面指南（Apple HIG）的标准，达到高标准 To C 商业化软件的品质。** 具体要求：

- **对标产品**：剪映专业版、Final Cut Pro、CapCut 桌面版——专业视频工具的布局逻辑与操作习惯
- **视觉语言**：遵循 Apple HIG——系统字体栈（SF Pro / PingFang SC）、macOS 暗色模式标准色板（层级背景 #1e1e20 → #252528 → #2c2c2e）、系统语义色（蓝 #0A84FF、红 #FF453A、绿 #30D158、橙 #FF9F0A）、克制的圆角与留白、清晰的视觉层级
- **质感要求**：精致的间距系统（4pt 网格）、细腻的边框（rgba 白 8%）、必要的材质感（毛玻璃/浮层阴影）、避免过度装饰
- **布局原则**：竖屏（9:16）素材为主要工作对象，播放器窄而居中、不浪费横向空间；时间线是主操作区，占比要充足（参考剪映约 40% 高度）；信息密度与呼吸感平衡
- **交互标准**：所有主操作有明确的视觉反馈；破坏性操作有确认；键盘快捷键完备（空格播放、JKL、方向键逐帧）；拖拽有吸附与磁贴
- **帧对齐（强制）**：时间线上所有切分边界的调整（语义单元与视觉镜头两层）一律以帧为最小单位——拖拽吸附帧边界、步进按钮 ±1 帧、时间显示精确到帧
- **文案标准**：中文界面，用语精准克制，不用技术黑话面向用户

任何 UI 相关的实现、评审、验收都以本节为准绳。

## 工作流要求（强制）

- **每次代码改动落地后（任务波/里程碑收尾时），必须重新构建并启动 app**，让用户能立即看到效果；不允许只报告"测试通过"就结束
- **留给用户的必须是正式构建**：调试构建（`flutter build macos --debug` / `flutter run`）不带云端 AI 凭据和内置 CLI，用户接手会看到"未配置/未包含"的降级提示并以为软件坏了（真机发生过两次）。验证结束后必须退出调试版并启动 Release 产物（通常直接 `./scripts/pack.sh` 打新版号正式包），绝不把调试版留在前台
- **构建一律走脚本，绝不裸跑 `flutter build`**：云端 AI 凭据只在编译期由 `scripts/build_macos.sh` 从 `.secrets/` 注入，裸 `flutter build macos --release` 出来的产物照样没有 key、照样弹「尚未配置 AI 服务」（真机发生过第三次）。给用户启动的 app 只能来自 `./scripts/build_macos.sh --release`（不动版本号）或 `./scripts/pack.sh`（发新版号）；裸 flutter 命令只许用于 analyze / test
- macOS 调试运行若出现 `Failed to foreground app`，需主动用 `open build/macos/Build/Products/Debug/ishkafel.app` 或 AppleScript activate 把窗口带到前台并核实可见

## 语言

回复、注释、提交信息一律中文（继承全局配置）。
