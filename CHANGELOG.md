## 0.1.233

**修：挑完素材预览不换，要去点一下别的台词语义单元才看得到画面。**

你说的：「选了底片，但是要切换下其他的台词语义单元才能显示出来正常的预览。」

改字幕、改音轨档位、拖边界都记得把预览推一次，偏偏**挑素材这个最主要的动作
漏了**——选完那一段是黑的，直到你点了别的单元（那条路正好会经过另一个入口）
才刷出来。顺着把所有改替换方案的地方清了一遍，Agent 把方案投影到时间线那处
也漏了：播报说「已经落在时间线上」而画面还是原片，你没法判断这一步到底成没成。

**修：「替换素材」上的角标数的是全片，不是当前这个单元。**

你说的：「选了一个，为什么显示 3 个？」

角标数的是全片已挑的素材条数，而这个 tab 里摆的是**当前单元**的挑选面板——
角标贴在它的标题上，读出来只能是「这个单元选了几条」。你那 3 条里有 2 条是
隔壁 U2 的某一镜挑的。现在角标跟着它旁边的内容走；全片的总数在底部摘要和
整片属性页上看。


**修：新加的台词语义单元拖到第一位，还没做任何操作就凭空带上了隔壁那个的标签。**

你问的：「这些信息不是应该跟台词语义单元走吗？位置变换以后是要跟着走的呀。
现在每个台词语义单元没有自己固定的编号吗？」

**有**，而且这条规矩是你定的，写在代码开头：「它这个编号下的所有数据都是
跟着这个编号走。」挑好的素材、换的音色、手改的字幕三份都照做了——**标签这份
漏了**，还在按位置走。

两处写回都按位置配对，而它们都跨过了你的编辑：

- **后台补标签**：跑完会重读一次任务再写回（免得把你这期间的编辑抹掉），
  可重读回来的那份正是你改过的——加过单元、拖过顺序，位置早就换了一套人。
  注释写着「重读再写」，配对却还是按位置
- **分析完的合并**：打标占整条分析的七成时间，那七成里你就坐在时间线前面改。
  这处有一道「边界变了就不认」兜着，但边界恰好相同就照样错

还有更前面一层：**切分出来的单元压根没发编号**，要等落库、下次读档才补上。
而「切分好放你进去 → 后台打标 → 把标签合并回来」整段正好在这段空窗期里，
按编号配对一个都配不上。现在编号在切分那一刻就发。

镜头那层仍然按位置配：传进去的是同一个单元的两份镜头列表，还校验起止毫秒，
镜头没有自己的编号也不需要。

**你手上那条任务里已经写错的标签**：在属性面板「U1 的标签」里点掉就行，
手填的标签不会再被后台覆盖。

## 0.1.231

**你自己加的那一段，现在和参考视频里的一段一模一样了。**

你的原话：「我就是想添加一个台词语义单元，然后调整它的位置，然后把它选一个
底片，然后你就把它当做是我参考视频里面的一段，走完所有参考视频该走的这个
流程全部走完。」

上一版底片能切了，但走完之后**该有的东西还缺一大半**。这一版一条条补齐：

**画面和音频不再是空的。** 那一段的底片是挑来的素材，缩略图和音频波形就该
从那条素材上取。之前两条轨都在「原片里没有就提前 return」那一步被挡掉，
标了一行「原片里没有」就完事——可它明明有画面。

**字幕有了，而且是真转写出来的。** 你在两个方案里选了 B：对底片素材**真跑
一遍 ASR**，拿到词级时间戳。不是按时长摊字数——摊出来的时间点在成片里一看
就飘。转完的台词会落进单元，左栏、时间线、台词框都读它；换了素材的那一镜
保留底片原有的字幕，和正常参考视频里的处理完全一样。

**预览画面上出字了。** 这一段的画面已经不是原片，判「整段替换」的那个口径
把整个单元一起吞掉了——时间线的字幕轨画得好好的，播到那儿画面上一个字都没有。
「整段换成一条素材」和「换了底片但自己还切着镜头」是两回事，现在分开判。

**时间线上的字幕块位置修了。** 取字幕全 app 有三个入口，我只改了预览和导出
两个，时间线那个（画字幕轨、拖拽、双击进编辑弹窗都走它）还在拿原片那份 ASR
——拼片任务里那份是空的，块退回成「整镜一块」的兜底画法，位置和宽度都对不上
镜头轨。这次把守卫从手写清单改成全仓扫描，再多一个入口也跑不掉。

**切完自动打标，命令行那条路也一样。** 界面上切完会接着送去打标，命令行
之前只切+转写就收工——Agent 拿到的是一堆没标签没画面描述的镜头，按标签搜
素材一条都搜不出来，而它会以为素材库里真的没有。补打标那条后台流程原来
一刀切跳过所有手加的单元，固定过底片的现在也排进去。

**导出真导了一条才发现还有三处不通。** 拼片任务被判成「还没挑素材」直接
拦在门口；命令行那条导出路自己抄了一份组段落的逻辑、不认底片；字幕的判据
拿错了字段。端到端导一条片子才逼出来，只跑测试全是绿的。

**「这一段画面从哪儿来」三处判错。** 拼片任务的分子是从别的任务搬过来的一段，
它「有原片来源」为真、同时又固定了底片，而这条任务根本没有原片文件。于是
属性面板写着一条并不存在的原片上的坐标；画面轨把原片同一个时间点的缩略图
铺了上去，和底片自己的缩略图叠在一起；「原片里没有」那个标注要等底片缩略图
解出来才让开，中间那几秒先给你扣一顶帽子。三处都改成先看底片。

**属性面板上的「时长」和同一张卡上的「成片 0~16.09」自相矛盾。** 16.09s 的
底片挂在一个 10s 的原片坑位上，时长那一行照着坑位写 10.00s。改成和上面两行
同源。

**属性面板补上「取自底片」。** 之前只有有原片时才显示「取自原片 xx–xx」，
底片单元那一行凭空缺席，你不知道这段画面从哪儿来。

**还修了一个一直都在的毛病：** 切点的「依据」查不中。切点记的是原始毫秒，
镜头边界吸到了帧上，两边永远对不上——所以点开镜头看「为什么切在这儿」
一直是空的。这次按帧重新对齐，原片和底片两条路都能回看依据了。

## 0.1.228

**修：切开的那一段，好几处还在拿原片当自己。**

你问的：「替换的台词语义单元里的视觉镜头换了之后，相关的操作对齐了吗？
比如字幕，比如音轨。」——查下来**字幕没对齐，音轨对了一半**，顺着清点又
揪出两处。四条都改了：

**字幕会把原片的台词烧到素材画面上。** 那一镜换了素材之后，字幕照着 ASR 的
原片时间戳取词——取出来的话跟画面毫不相干，而它会结结实实烧进成片。这还跟
「整段替换不渲字幕」自相矛盾：整段不渲，切开逐镜反而渲了。现在底片是素材时
**不拿 ASR 硬凑**，只认你手排过的（手改成空同样算数）。预览和导出看同一份。

**声音选「人声/背景声」会去分原片的轨。** 那一段的画面早就是素材了，分原片
毫无意义：原片有分离轨就放行、导出跑到一半抛错；原片没有就在门口拦你，叫你
「去重新分离原片」——两种都不对。现在分**这条素材自己的**，分不出来时报的话
也换成「这一段的底片分离失败」。

**Agent 看不见底片。** 任务视图里报了「有没有原片来源」，却没报「这一段的
底片是哪条素材」——Agent 于是拿原片的时间线去理解这一段，也不知道它已经切过、
每一镜都能单独挑。镜头的「取自哪一段」对底片单元现在报的是**素材内偏移**，
不再是原片坐标。

**审核页的「本来的样子」读错了文件。** 那张卡是拿来跟候选比对的参照物，
底片单元却去原片的同一个时间点抽帧——你是照着一段毫不相干的画面在做取舍。
现在读底片那条素材，缩略图也按素材分开存（不分会串）。


**答一个问题，修三个毛病——都是从同事那张截图追出来的。**

你转的那张图：画面轨和音频轨大片空白。问「分析完之后是不是应该补上」。

**先说答案：那几段不该补，因为原片里本来就没有它们**（插入段是你自己加进来
的，画面只能来自素材）。**但界面一个字都不说，这是我的问题。** 现在那几段会
标出来：浅底 + 一行「插入段 · 原片里没有」，画面轨和音频轨各一条。

**修：真坏了的时候也不说话。**

顺着查下去发现更糟的：抽帧失败只写日志、照常返回一份「全是 null」的列表，
音频提取失败返回「全 0」的列表——而提示的判断是「列表空不空」，全 null、
全 0 都不算空，于是提示永远出不来；再加上状态报的是「已就绪」，那一层提示
直接跳过。**两条轨真坏了，你看到的也只是一片空白。** 现在：

- 全没抽出来 / 波形全 0，一律当「没有」，明写「画面缩略图生成失败」
- **两条轨各判各的**——抽帧全失败而波形好好的，用一个总状态只能二选一地说谎

**修：加一个单元，却说「改到了 7 个」。**

在末尾添加一个空的台词语义单元，弹出来问「改完之后要连带处理吗」，说改到了
U1~U7 等 7 个，**而且两项默认都勾着**：取消它们已挑的素材、送去重新打标。
手快点下去就是白清一遍素材、白花一次打标的钱。

原因是「单元数变了就认为下标全乱了，凡是有东西可丢的都问一遍」。现在改成
**按单元自己的身份比对**：加一个空单元不动任何人，就不问了；真的拆分/合并，
被动到的那个照样问到。老存档没有身份标记的，仍然宁可多问一次。

**修：分析完之后画面轨不补。** `_loadMedia` 读的是进页面那一刻的任务快照，
而且只跑一次。现在读当前任务，并在分析把原片元信息补上来时重跑一次（人声轨、
ASR 也一并接过来）。

## 0.1.227

**改：切分那一下现在一步做完——切完接着打标，不用你再点一次。**

你说的：「点了之后它就会切，切分之后的动作也要进行的……正常的参考视频的
台词语义单元里面的 S1、S2、S3 要干嘛，你这边也是要干嘛的，都要走一遍。」

对着原片那条线逐项比了一遍，底片切出来的镜头少两样，都补上了：

- **打标**：原片的镜头是分析时顺带打好的，底片的却要你再点一次按钮。现在
  切完直接接着打，中间不停。因为这一下要花钱，**确认框每次都弹**，把「做
  两件事、两步都要等、打标要花钱」写在点之前
- **这一刀是怎么定出来的**（画面差异分数、是直接确认还是灰区复核保留）：
  属性面板里那一栏，原片的镜头有、底片的没有。现在一样有

**顺带修了一个一直都在的毛病**：那一栏其实大面积是空的。切点检出来是原始
毫秒（`3435`），而镜头边界一律吸到帧上（`3433`）——拿对齐后的值去查原始
记录，绝大多数查不中。缺了不报错，所以一直没人发现。实测这一段 6 个切点
原来只贴上 2 条，现在 6 条全在。**原片那条线也是同一个毛病，一起修了。**

**修：确认框里的星号。** 「改完之后要连带处理吗？」和「切分这一段」里都
有 `**这样**` 的写法——界面不解析 Markdown，星号是直接显示出来的。顺手加了
条守卫扫全仓，当场又抓出「自动铺一版？」和设置页「切换企业」里的三处，
一并改掉。

**改：「改完之后要连带处理吗？」说清楚影响范围。**

你问的：「这个按钮点击了之后会怎么样，影响范围有哪些，是只有 U1 还是所有。」

答案是**只有点名的那几个**，但原来的文案没说。现在：

- 正文下面一行直接写「下面两项只作用在 U1 上，别的单元一个都不碰」
- 「取消已挑的素材」会报出**具体条数**（「把 U1 挑的 3 条素材全部取消
  （整体替换的、每一镜的都算），回到『保留原片』」）；一条都没挑时直接说
  「勾不勾都一样」
- 「重新打标」写明「点完就送去云端逐个看图打标，要花钱、要等一会儿」，
  不勾是什么后果也写清

## 0.1.226

**新增：插入段和拼片的分子，可以切成分镜再一刀一刀地换了。**

你说的：「我既然是添加的一个空的台词语义单元，这个台词语义单元可不可以支持
替换分镜？……它应该是可以走切割，然后打标的这一套流程，然后去替换里面的分镜。」

先说查下来的实情：**挡路的不是「它被替换过」**。系统从没这么认为——它只是
没有可替换的对象（`canUsePerShot` 就一行：有没有切出镜头）。顺着这条往下想，
缺的是「底片」这个概念。

**底片是什么**：这一段的画面从哪张片子的哪一段来。原片单元的底片是原片那一段，
插入段挑了素材之后，那条素材就是它的底片。**整体替换定的是「底片是谁」，
镜头替换定的是「在这张底片上换掉哪一刀」**——两件事本来就不在同一层，
是过去的数据模型把它们摆成了平级，才必须二选一。

**怎么用**：挑了素材的插入段，属性面板里多了一张「这一段的底片」卡片，
写着画面取自哪条素材，底下一个「切分这一段」。点下去，软件读一遍这条素材、
按它自己的画面切点划成几个镜头——从此每一镜都能单独挑素材，跟有原片的任务
完全一样。

三条规则，点之前会在确认框里写清楚：

- **底片只能有一张**。这一段本来选了三条素材能出三条变体，切下去就只剩被选中
  的那一条；这一段不再产出变体，变体只来自它内部各镜头的选择
- **换底片会作废**已经切出来的镜头和挑在上面的素材。不禁止你换——禁止了你就
  被锁死在第一次的选择上——但会先问一句
- **到此为止**。底片切出来的第 N 镜换进来的那条素材，不再往下切。一层底片 +
  一层镜头，就这两层

切分要读一遍素材、灰区切点还要送云端复核，**要等一会儿也要花钱**，所以它是
你点的，不会在挑完素材时偷偷跑。实测一条 16 秒的素材切成 7 个镜头，用了 4 秒。

**这一段不带台词字幕**（时长跟素材走、和原坑位对不齐，按原时间戳贴上去必然
错位）。要字幕的话用上一版做的「字幕时间可编辑」自己排。

「原片这一镜的声音」在这些段落上改叫「**底片这一镜的声音**」——原片那一段
已经不在成片里了，还写「原片」会让你以为调的是另一条声音。

**同一件事 Agent 也能做**：

```
ishkafel unit base <任务> --unit 1      # 底片是谁、能不能切、切了会掉什么
ishkafel unit segment <任务> --unit 1   # 切
ishkafel unit unpin <任务> --unit 1     # 换一张底片
```

**顺带修：整体替换的素材长度恰好等于原坑位时，台词字幕会贴到已经不是原片的
画面上。** 原来判「替换没替换」只看时长变没变，长度正好相等就判成没换，
于是按原片时间戳算的字幕照贴——对不上，而且哪儿都不报错。

**内部**：「这一段画面从哪儿来」过去在预览、导出、音频三处各答一遍，规则还
不完全一致（一边有声一边黑屏那类问题就是这么来的）。现在收敛成一份
（`baseChoiceOf` / `baseOf`），加了静态守卫盯着不许再各写一份。

**有原片的真实单元也能这样精修**：整体替换成一条素材之后，同样可以切开、
一镜一镜地换。（上一稿说「还不能，要先做坐标解耦」——那是我判断错了，
真跑一遍就清楚：镜头坐标本来就是「单元起点 + 偏移」，成片位置按偏移算，
原片那个坑位根本没参与。真正要补的是外围六处「只认原片、不认底片」的地方。）

**真机走查逼出来的一个洞**：切完分镜再给某一镜挑素材，方案从「整体替换」
落到「镜头替换」，底片就不在方案里了——而「已选素材」的收敛按方案走，
于是把底片当成没人要的清掉，**预览整段变黑，还哪儿都不报错**。现在
「这条素材还有没有人用」只有一份判据（方案里的候选 + 单元身上的底片），
加了静态守卫；守卫当场又抓出命令行那边同一份摊平的拷贝。

顺手修掉的几处，都是同一类「只认原片」：

- 拖单元边界会悄悄放行、拆分直接抛断言——固定过底片的单元现在一律拒绝
- 「切分已锁定」把**镜头**的切点调整也一起锁了——现在分两层：单元边界不能
  动（一动镜头就全对不上素材），每一镜的切点和素材照样能调
- 剪映工程里那几段指回原片的同一个时间点（一段毫不相干的画面）
- 换过音色的段落，配音按原片坑位长度裁，声画从那儿起全错位
- 镜头卡片写「取自原片」、镜头选择器写「原片」——其实取自底片
- 底部统计把固定了底片的分子算成「还没挑素材」

**新切的镜头还没打标**，按画面/标签搜素材会搜不出东西。卡片上现在会说出来，
并给一个「给这些镜头打标」——不自动跑，那是逐镜看图的云端调用，切成七八镜
就是七八次。

## 0.1.225

**改：替换裂变的配乐，一段可以选好几首——这件事现在看得出来了。**

你说的：「选择背景音乐的时候可以多选……一个是在预览里面播放的，导出的时候背景
音乐不参与笛卡尔积的计算，它是平分的。」

先说实话：**这个能力本来就有**，是界面把它藏起来了——行首画的是单选用的空心圆、
按钮写着「选一首」、那句「可以多选」的提示要等你选到第二首才冒出来。等于只有
已经知道能多选的人才发现得了。

这一版把它摊开：

- 行首改成**方框**（能多选的那种形状）；只在替换分镜那类只准挑一首的场合才是圆的
- 选曲面板里**一进去就写着**：「可以选好几首互为备选：导出多条时按次序轮流用，
  标 ★ 的那首用于预览」
- 时间线配乐轨的说明也补上同一句，块上那个数字就是这一段选了几首

规则没变，这里一并写清楚：**配乐不参与笛卡尔积**。导 100 条、这一段选了 2 首，
就是第 1、3、5…条用第一首，第 2、4、6…条用第二首——各 50 条，除不尽就一个多一个少。
预览只放 ★ 那一首。

顺带修一处对不上的地方：★ 那一首被删掉之后，预览会退回**第一首**（原来的代码是
夹到最后一首，和它自己的注释、和面板里的行为都不一致）。

**新增：任务可以整条复制了。**

你说的：「给一个复制的能力，它可以复制一个一模一样的任务，但是两个任务要完全隔离。」

任务卡的 `···` 菜单里多了「**复制任务**」。复制出来的那条叫「××× 的副本」
（再复制就是「的副本 2」），切分、标签、挑好的素材、配乐、字幕、配音，
**全都照搬**。

**完全隔离**是这个功能的全部前提，所以不是只抄一份 JSON：素材、配音、人声轨、
首帧图、封面都**各存一份**，任务里记的每一条路径都改指到新任务名下。往后你改一条、
删一条，另一条一个字节都不动。你自己的原片不在数据目录里，一个字节也不碰。

不抄的东西：导出历史、这次分析花掉的钱、等待时长、上次的报错——那些属于「那一次」，
不属于方案。导出中间产物、预览代理、变速切片这些派生的也不带，用到时自然重建。

会占盘，所以**点之前先问一句**，把新名字和大概多少 MB 摆出来。还在分析的任务不给
复制——那时产物只有一半，抄出来的副本不能用。

Agent 侧同一件事：`ishkafel task-copy <任务ID> [--name 新名字]`，回一段 JSON，
带新任务的 `id` / `seq` / `copiedFrom` / `bytesCopied`。技能手册里也写了。

## 0.1.224

**改：字幕的时间改回成片时间码（分:秒.帧），不再是相对这一镜的秒数。**

你说的：「字幕时间用完整的时序帧……问了下同事还是这个更符合使用习惯。」

上一版做成了「相对这一镜的秒数」（`0.5`），现在两个格子里都是**成片时间码**
（`00:23.00`）——和属性面板别处、播放器下面那个读数是同一套，照着哪儿都能对上。
卡片底下也带上那句图例：`.10` 是**帧号**不是小数。

输入照着显示的样子敲就行（`00:23.00`）；只敲秒和帧（`23.00`）也认，全角冒号、
前后空格都认。认不出来就把原值放回去，不猜。

**存的仍然是相对这一镜的时间**——所以你把整个台词语义单元挪到别处，这两个
时间码自己就跟着重算，不用你去改。

顺带：**时间线上拖出来的时间现在吸到帧上**。卡片是按帧显示的，拖出来的值落在
帧缝里的话，你看到的帧号和实际存的会差一帧。

**修：拖字幕那一段「拖不动」，要连着拖两三下才看见它动。**

你说的：「字幕拖拽的时长拖拽好卡，连着拖两三下才会动。」

不是卡，是**画布压根没重画**——每一下其实都拖到了，只是你看不见。

时间线是一整块画布，重不重画由一个判定函数说了算，而它比的都是「值」。
拖动改的是视图自己那份临时数据（松手才落库），落库之后改的又是字幕轨
（**不动切分**）——两样都不在那个判定里，于是从按下到松手一帧都不重画，
要等下一次鼠标划过时蹭出一次重画，你才看到前面几下累积的结果。

现在拖到哪儿了、字幕改成什么样，都进了重画判定。顺带量了一下重画的代价：
真实规模（21 句 ASR、24 个镜头）下一帧 0.43 毫秒，离 16.7 毫秒的预算很远，
不需要为它加缓存。

同一个毛病还影响另一处：**在属性面板里改完字幕，时间线那一句原来也不会
立刻更新**（同样要等鼠标划过），现在一并修好。

## 0.1.223

**修：替换镜头的声音跑到了片头，还把前面的口播压小了一半。**

你说的：「U1 的 S1 我没有替换，S2 我替换了。预览的时候没有任何问题，
字幕也好声音也好。导出以后我发现 S1 好像被去了背景音，人声也变得特别小，
跟预览的时候不一样。最关键的是，S2 的背景音竟然在 S1 的开始播放的时候
出现了。」

两个独立的错，都在「把一层声音叠到成片上」那一步：

- **位置**：软件先给替换素材的声音变速，再把它挪到该出现的时刻，最后按坑位
  长度裁一刀。问题是最后那一刀会把「挪」出来的空白整段吃掉，这一层于是掉回
  第 0 秒。以前没暴露是因为输出时还有一道自动补空隙兜着——而链上一旦有变速
  （镜头替换必然变速），那道补偿就不发生了。所以表现成「没变速的配乐位置是
  对的、变速的素材声跑到片头」。现在改成**先裁成自己该有的长度，再整块摆到
  位置上**。
- **音量**：两层声音叠在一起时软件用的是「取平均」而不是「相加」，于是从片头
  起口播就被压掉一半，直到那一层结束才弹回来。而叠加是一层套一层的，替换的
  镜头越多，最前面那段被压得越狠——三层就只剩八分之一。现在按你定的规则改成
  相加：每一块摆在它自己的位置上，各层保持自己的音量，谁也不因为多了一层而
  变小。

**改：「替换分镜的声音」这一层，预览也播了。**

你说的：「让我预览和导出的成品一定要保持一致。」

这一层此前只在导出那条路上有，预览压根没有它——你在预览里听着一切正常、
挑好了组合，一导出发现多一层声音，还找不出原因。现在预览会把它播出来，
位置、时长、音量都和导出的一模一样。

选了「人声」「背景声」那两档时，预览放的仍然是素材的原混音（导出才会真的
分开），这一点会在预览横幅上点名说清楚，不再让你自己猜。

**修：装了新版、老任务直接重新导出，之前会听到一模一样的老毛病。**

上面那两个错修好之后还剩一道坎：软件会把算过的中间产物按内容指纹存下来复用
（不然每次导出都要重跑几十次合成）。而那个指纹只认「用了哪条素材、放在第几
秒、多大声」，不认「用的是哪一版规则」——于是盘上那些按错规则合出来的声音，
入参一个没变、指纹也一个没变，重新导出照样被原样取出来用。

现在规则版本进了指纹。**老任务直接重新导出就行，不用新建。**

**新增：某一段叠出来过载了会点名告诉你。**

各层改成相加之后，音量都开大就可能叠过头、听着发破。软件**不会偷偷帮你压
音量**——那等于替你做了判断。它会在导出结果里点名是哪一镜或哪首配乐，
你自己决定调哪一层。只有真的叠出了听得见的过载才说；原片母带本来就压到顶的
那种不算。

**新增：字幕的时间能改了——卡里能输，轨上能拖。**

你说的：「前面的时间我也要编辑……按照秒数也行，就按现在的秒数，并且是相对于
这个镜头的秒数」「只把下面字幕这个轴改一改，改成多段的……而且这个地方完全
可以拖动」。

**属性卡里**：原来那两个 `0.0s→0.5s` 是只读的，现在是两个可以直接输的格子。
单位是**秒、相对这一镜的开头**（不是成片时间码）——这一层人关心的是「这句话
在这一镜里第几秒出现」。和文字框一个规矩：**光标离开才提交**，敲的过程中不会
一遍遍重烧字幕。输了个越界的数会被夹回来，并且把格子里的字也纠回来
（不然你看到的是一个没生效的值）；输了个看不懂的就原样放回去，不猜。

**时间线上**：放大到每一段都够宽时，字幕轨从「整镜一块」展开成**一句一段**，
像 S1、S2 那样并排。拖左边缘改起点、拖右边缘改终点、拖中间整段平移
（长度一分不变）。**各段独立**，不像视频轨那样两段共用一条边界。

规矩和你说的一样：**不能重叠**（顶在邻段的边上就停住）、**可以挨着**、
**中间可以留空**、**不许拖出这一镜**（字幕是烧进这一镜的切片里的，出了界
没地方烧）。松手才提交，拖的过程中不重烧；没拖动就不提交。

**缩小的时候画面和现在一样**：一句字幕常常只有半秒，没放大时就是几个像素，
十几个小块排在一起是一条噪点。所以**最窄的那一段**够宽才展开，否则维持
原样（整镜一块 + 数字角标），双击照旧进弹窗改。

光标会说话：压在段的边缘是双向箭头，压在中间是手型。

两个入口（卡里输、轨上拖）走的是**同一套夹取规则**——两处各写一份的话，
能输出来的和能拖出来的迟早不一样，而这种不一样你只有在成片里才看得见。

**改：时间码终于说清楚自己是什么了。**

你说的：「没有说清楚这个是原视频的帧数时间轴，还是原视频的秒数时间轴。
如果是帧数了，是多少帧一秒？也没有。这对我造成了很大的困扰。」

`00:17.28` 里的 `.28` 一直是**帧号**，不是百分之一秒——30fps 下它等于
17.933 秒。按小数读差 0.65 秒，而界面上没有一个字提过这件事。四处都补了：

- **属性面板的时间卡底下多一行说明**：「时间码 分:秒.帧 · 30fps
  （00:17.28 = 17 秒第 28 帧）」。给例子而不只说「是帧」——例子才是当场
  能对照的东西。
- **「开始 / 结束」改成「成片开始 / 成片结束」**（镜头卡上原来叫「镜头开始 /
  镜头结束」，同样改）。它们一直是**成片时间轴**上的位置，而下面那行
  「取自原片」是原片时间轴——一个标了来源一个没标，最容易让人以为是同一件事。
- **顶栏写上帧率**：`1080×1920 · 30fps · 75.3s`。时间线、逐帧步进、属性面板
  全按原片帧率数帧，而这个数以前在界面上一个字都没有。
- 播放器下面那个 `00:00.00 / 01:15.09` 悬停给出同一句说明（那一行摆不下）。

**改：导出帧率默认跟着原片走，选低了会说一句。**

剪辑软件里「工程帧率」和「导出帧率」本来是同一个数（剪映就是导出设置即
工程帧率）。我们把它拆成了两个——时间线按原片帧率数帧，导出帧率却写死 30。
原片 60fps 的话，你在时间线上按 60 数帧，一导出变成 30，中间一句话都没有。

现在默认取原片那一档（29.97 取 30，不是 25）；你手动选到比原片低时，
那一栏底下会写「原片是 60fps，导成 30fps 会每 2.0 帧取一——时间线上是按
原片帧率数的帧」。**不拦，只说**。

（时间线上方的标尺仍然只有 `分:秒`，没有帧位：那是粗刻度，带上帧会变成
一串噪音。）

## 0.1.220

**修：上一版那个「原片这一镜的声音」，只在预览里生效，导出时会消失。**

自查出来的，不是你报的。0.1.219 把这条设置接到了预览和音轨装配上，
但**两条导出路都没把它传下去**——你在属性面板里选的档位，播放器里听得到，
导出来的片子里一点没有。同一天上线的功能只做了一半。

顺带挖出一个更早的：**命令行导出从来没传过「替换分镜的声音」**。也就是说
Agent（`ishkafel export`）导出来的片子里，替换素材那一层的声音一次都没响过，
而界面导出的会响——同一个方案，两条路出来的不是同一条片子，哪儿都不报错。

三处都补上了，并加了一道**静态守卫**：任何一处调用导出，只要漏掉
`materialAudio` / `sourceAudio` / `backgroundPath` / `vocalsPath` /
`subtitleTrack` / `voiceAudio` 里的任何一个，测试当场红。这类漏不是逻辑
写错，是加参数时忘了改调用点，只有扫描拦得住。

**Agent 侧一并补齐：**

- `ishkafel task --json` 现在报 `stems: {vocals, background}`——分离轨到底
  在不在盘上。以前查不到，Agent 只能先把档位设上、再去撞导出拦截，白跑一趟。
  报的是「能不能用」而不是路径：存了路径不等于文件还在。
- 手册补上导出文件名的新规则：**撞名不覆盖、往后排**（`A-居家写实线(2).mp4`），
  并提醒导完要看 `results[].path`（实际落地的名字），别照着方案名去猜。

## 0.1.219

**新增：把被替换那一镜的声音拆成两条轨，各选各的。**

你说的：「我需要把参考视频的声音也拆开。这样我就可以用替换分镜的原声，
加上参考分镜的人声或者背景声，排列组合由我自己选……很有可能我需要的是
原片 S1 的口播和替换分镜的原声。只是我不想做这么定制化，我要把它拆成功能。」

以前只有「替换分镜的声音」能拆成四档（不播放/人声/背景声/原声），
原片那一层是个隐含的常量——只有被配乐盖住时才会自动换成纯人声，你手上
没有开关。现在**属性面板里多了一张「原片这一镜的声音」**，同一套四档，
两层各选各的，最后混在一起。

几处是刻意这么定的：

- **没点过 = 自动**，行为和以前一模一样（原混音；铺了配乐就换纯人声）。
  直接把默认写死成「原声」的话，你什么都没动、铺了配乐的段落声音反而变差。
  **一旦你点了任意一档，就完全按你选的来**——冲突只提示，不替你改。
- **只在换过素材的镜头上出现**。没换素材的镜头照旧，连控件都不给
  （你的原话：「如果没替换分镜，那连原片这一分镜的声音该怎么操作都不应该有」）。
  整体替换的单元同理；换过音色的单元会写明「这一段的人声来自生成的配音」。
- **一句话中间会突变，会当场点名**。一句台词常跨好几镜：这一镜剥掉了原片
  现场音、旁边那一镜没换素材还是原混音，说到一半背景音就变了。卡片上会写
  「这一句里 S2、S3 没换素材」——这类别扭你听得出来却找不到原因。
- 「不播放」垫的是**等长静音**，不是少拼一段（少一段后面全体提前，画面和
  声音就此错开，而且哪儿都不报错）。
- 两条轨各带各的音量：原片这一层默认满音量（它是主体），素材那一层默认 25%
  （垫在口播底下）。预览走多轨 EDL、表达不了单段音量，所以**预览按满音量放，
  音量在导出时生效**，卡片上写着这句话。
- 选了人声/背景声却没有分离轨：**导出在进门口就拦下**并说清怎么补
  （工作台点「重新分离」，或 `ishkafel analyze <任务>`）；预览先放原混音，
  但会在提示条上说出来——不说的话你听到的和导出的不是一回事。

**改：新建任务的「替换分镜的声音」默认改成「原声」。** 旧任务一律不动——
升级一版不该让它们导出来的片子突然多一层声音。

顺带：

- 顶栏那个「素材原声」按钮改名「镜头声音」，点开是**两栏**，一次把两条轨的
  全片打底都设了。分成两个按钮的话你得开两次，还看不出它们是一组的。
- Agent 也能拧这个旋钮：`ishkafel unit source-audio <task> [--unit N --shot M]
  --audio none|vocals|background|original|follow|auto [--volume]`，
  手册和 `task_view` 的 JSON 里都补上了。


**修：往同一个目录导第二批成片，上一批被悄悄盖掉了。**

你说的：「导出素材时，如果默认是上次导出的路径，视频名字就是一样的……
新素材导出后名字会和上一批重名，然后上次导出的就会消失掉。」

确认是真的。导出目录默认沿用上次导到哪儿（同一个项目往往一直往同一个位置
出片），而文件名只由方案名决定——界面上枚举出来的组合没有名字，一律叫
「变体1、变体2……」。两头凑到一起，第二批的文件名和上一批一模一样，
底下的 ffmpeg 又一律带 `-y`，撞上就直接覆盖。既没有确认，也没有提示，
上一批成片就没了。

现在撞名一律往后排：`变体1.mp4`、`变体1(2).mp4`、`变体1(3).mp4`，
谁都不许被顶掉。三处都补上了——审片台的矩阵导出、编导台的脚本成片
（它的文件名带到分钟，同一分钟内导两次照样重名）、以及同一批里两条方案
起了同一个名字的情况。剪映草稿那条线本来就是这么做的，这次是把导出对齐。

**顺带**：导完在结果里把这一批的文件名摆出来（「变体1(2).mp4、
变体2(2).mp4」），改过名的时候人能照着去目录里找，不用自己猜。

**修：「挑差异最大的」挑出来的十条，同一个位置全用同一条素材。**

你说的：「一个镜头选了5个替换，但是导出10条全是选用的其中同一个镜头……
既然我选择了挑差异最大的，那最终产生成片时，重复度肯定越小越好。任何一个
替换的位置都尽量不重复。比如我选了5个，然后挑了10个……应该是5个各出现两个。」

复现出来了，而且比看到的更糟——三个毛病叠在一起：

1. **候选根本没进池子。** 组合超过 100 条时，原来是掐里程表的前 100 条，
   等于「最低位转个不停，别的位一动不动」。一个镜头挑 5 条、另一镜挑 40 条，
   算出来 200 种，取前 100 条时那 5 条里有两条一次都没出现过。
2. **一个单元里的多镜被合并成一条素材。** 挑选算法的特征只留了每个单元
   最后一镜换的是什么，前面那几镜换没换完全看不见，自然也就轮不上。
3. **「差异」只按素材长得像不像算。** 同一批拍摄切出来的 5 条素材彼此
   相似度接近 1，算法认为「换了等于没换」，于是索性一条用到底。

现在：

- 名额不够时按位均衡取样——每一位上的候选轮着来，各自出现次数最多差一次，
  两位候选数相同也不会齐步走（否则 5×5 挑 25 条只会出现 5 种搭配）；
- 位置精确到**视觉镜头**，不再按单元合并；
- 挑选先看**配额**：这一位有 n 条候选、要出 k 条片子，谁都不许超过 ⌈k/n⌉ 次，
  5 条候选出 10 条就是各 2 次。配额之内才比距离。看着像不是重复用同一条的
  理由——人既然挑了 5 条，5 条就都得露面。收尾还做一轮局部交换，
  把贪心走进死角剩下的偏斜换平。

顺带三件相关的：

- **顺手修了一个隐患**：原来会先把一个单元内各镜头的候选铺成完整笛卡尔积
  再取前 100 条。13 个镜头各挑 5 条就是 12 亿种，跟名额没关系，内存直接吃满。
  现在按需生成，只造要的那 100 条。
- **「这几条各用什么」挪到了导出之前**：挑得对不对只有人能判断，等导完再列
  等于让人对着十个文件自己比对。位置也标到镜头（`U2·S2`），一个单元里
  好几镜各换各的时不再分不清。
- 只有两三条组合时，「挑差异最大的」下面那行档位（3/5/10/20）一个都放不下，
  原来会摆出一行空的「挑 ▢ 条」。现在直接写「共 N 条，全部导出」。

## 0.1.218

**改：点字幕弹出的样式面板不再给整屏蒙一层灰，也不再拦住底下的操作。**

你说的：「是我点字幕之后，你有一层遮罩，不是字幕的衬底。」

那是模态弹窗自带的那层 black54。而这套参数恰恰是**照着画面判断**的：
画面被压暗，挑颜色、看衬底盖没盖住原素材自带的字，全都不准。更别扭的是
它还拦住一切点击——字幕只画在被替换的镜头上，要看效果就得先把播放头挪
过去，而面板开着挪不了，只能「关面板 → 拖时间线 → 再开面板」。

现在它是一块靠右摆的浮层：画面亮度不变，面板开着照样能拖时间线、按播放、
直接拖画面上的字幕。

**顺带改个名**：那一行原来叫「遮罩」，在剪辑里那是蒙版的意思，容易误会。
它其实是字幕背后垫什么（无 / 毛玻璃 / 黑底条），改叫「衬底」。

## 0.1.217

**修：一口气把字幕拖到位就松手，那一拖会白拖。** 上一版新加的「直接拖字幕」，
如果拖动和松手落在同一帧（快速一甩），存下来的还是拖之前的位置。

## 0.1.216

**改：调字幕样式真的实时了——不转圈、不跳帧、一次 ffmpeg 都不跑。**

你说的：「我调整字幕样式的时候应该实时显示，而不是每次都要有一个加载的
动效，然后跳转到第一帧，这个跳转太煞笔了。」

上一版只把「跳回第一帧」堵住了，转圈和延迟还在，因为根子没动：**预览里
那行字原来是烧进视频文件的**。替换镜头要变速铺满坑位，字就顺手让 ffmpeg
一起烧进那段切片——于是你每动一下滑杆，都是「重渲一遍切片 → 换一次播放源
→ 播放器重开文件」。转圈几秒、位置归零，都是这么来的。

现在预览的字幕**画在画面上，不烧进文件**：拖位置、拖字号、换颜色、换衬底
都是即刻重画，播放不停、位置不动。导出和剪映那两条路照旧烧录，一个像素
都没变。

**顺带：字幕可以直接用手拖了。** 画面上那行字本身就能上下拖着放，松手就存
——比在面板里拧「距底 21%」直观。点它一下也直接打开样式面板。

**还有一条要说清楚**：字幕只画在**被你换掉画面的镜头**上。没换过的镜头，
台词字幕烧在原素材自己的像素里，我们再叠一层就是两行字打架——所以那些
镜头上什么都不会出现，这是对的。想看样式改动，得先替换一个（长一点的）
视觉镜头。

## 0.1.215

**修：调字幕样式的时候，播放头还是会被打回片头。**

上一版把「推方案」排了队，以为就完了 —— 真机上再试，拖一下位置，画面照样
从 00:45 跳回 00:00。

真正的原因在换源那一下：`open` 返回时播放器可能还在读文件，**这个当口发出的
seek 会被加载过程整个吞掉**，位置就停在片头（`media_kit_playback.dart` 里
早就写着这条注释，只是换源恢复位置那段没照着做）。现在换了画面源就先等它
真加载完，再把位置拉回去。

这条只影响预览：拖字幕位置、拖字号、换颜色换遮罩，每动一下都要重推一次
方案，也就每动一下弹一次回片头 —— 想看清字幕挪到哪儿了，得先把播放头拖回去，
等于没法边调边看。

## 0.1.214

**修：调换单元顺序之后，替换素材面板上的数量还停在老位置。**

你说的那条 —— 时间线上那一格明明没挑过素材，进「替换素材」里镜头标签却写着
「×1」，点进去又什么都没有。

替换方案本身已经跟着单元走了（上一版改的），但**候选面板自己另存了一份按
位置排的**，而它只在「单元个数变了」时才重建。调换顺序个数不变，于是它一直
用着老位置那份 —— 时间线读的是新的、面板读的是旧的，两边对不上。

现在面板看的是「哪几个单元、按什么顺序」（单元的编号连起来），一变就按编号
把手里那份重新铺一遍。

**顺带修：连着推两次预览，播放头会被打回片头。** 换源要重开文件，中途位置
是 0；两次推叠在一起时，后一次记下来的「人看到哪儿」就成了片头。现在推方案
排队，一次跑完再跑下一次。

**改：字幕样式边调边看**（见 0.1.213），这一版把上面那条一起修了，
调的时候画面才不会跳回开头。

## 0.1.213

**改：字幕样式改成边调边看，不用再盲调。**

你说的「现在能调整了，但是没法实时显示位置，有点在盲调的感觉」。

面板原来只在点「就这样」时才把样式交出去，所以拖位置、拖字号的时候预览
一动不动 —— 只能凭「距底 21%」「23‰」这两个数字猜，点完才看得见，不对
再开一次。而这套参数的用途本来就是照着画面判断的（尤其挑遮罩：素材自带
烧录字幕时，得看它盖没盖住）。

现在动一下预览就跟着变：位置、字号、颜色、遮罩都一样。**点「取消」会退回
原样**，调的过程中不落盘。

拖动途中做了节流（停手约 0.2 秒才重渲）—— 预览里那几段字是烧进切片的，
每动一格跑一次 ffmpeg 会把机器拖死。

## 0.1.212

**修：连续合并两次，那截空白又回来了；退回首页再进去才正常。**

上一版修的是「轴该不该换」的判断，这一版修的是「那个判断根本没跑」。

换轴那一步写在时间线区的布局回调里，而**布局回调只在尺寸变了才重跑**。
改切分（合并两镜、拆一镜、拖边界）尺寸一个像素都不变，于是那一步永远不跑，
时间线继续用着改动之前那条轴 —— 镜头位置全是旧的，单元尾部空出一截。
退回任务列表再进来是整棵树重建，所以就又好了。

现在时间线区自己监听编辑器，切分一变它就重算。属性栏那边栽过同一件事，
结论也是一样的：**轴要在自己的重建里现算**。

## 0.1.211

**修：合并视觉镜头之后，那个单元的镜头轨尾部空出一截。**

你说的「合并为什么还是这样子」。上一版修的是数据（单元边界被挪错），
这一条是**画的时候**的：

时间线靠一个判断决定要不要换轴 —— 而它只比单元的起点、长度、取自原片的
哪一段，**从来不看镜头**。合并只动单元内部，这四样一个都不变，于是判成
「轴没变」，时间线继续用着合并之前那条轴。

而镜头在成片上的位置正是从轴里那份镜头列表算出来的：新列表 12 个、旧列表
14 个，画的时候拿新下标去问旧列表 —— 位置全是旧的，多出来的两格地盘没人画，
就是你看到的那截空白；点下去命中的也是另一格。

拆分镜头、拖镜头边界是同一个毛病，一起好了。

## 0.1.210

**台词语义单元有自己的编号了，挂在它下面的数据全部跟着编号走。**

你说的那件事：

> 这个台词语义单元应该有一个自己的编号，但不是 U1 U2 U3，因为它位置排序是
> 有可能会变的。它这个编号下的所有数据都是跟着这个编号走。

现在每个单元在切分出来那一刻就有一个**永不变**的编号：挪动、改边界、改台词、
重新打标都不动它。拆分时左边留用、右边发新的；手动加的发新的。界面上照旧显示
U1/U2/U3 —— 那是位置，给人看的。

改成按编号记的四份：

- **挑好的替换素材**
- **换的音色**
- **手改的字幕**
- **生成好的配音文件**（文件名从 `unit_<下标>.mp3` 改成 `unit_<编号>.mp3` ——
  这条是上一版查出来、当时修不了的那个：方案里的指向被搬走了，盘上那个 mp3
  还叫原来的名字，取到的是**另一个单元的配音**）

于是**挪动顺序、删掉单元都不需要再搬任何东西**，四个「跟着挪」的函数一起
退休了。半年里因为它们漏搬过三次，每次都是不报错、只有把片子导出来看一遍
才发现。

**你现有的任务打开时自动迁移**，每一份数据仍然挂在原来那个单元上；指向已经
不存在的单元的那几条直接丢掉——挂到别人身上比丢掉更糟。存盘时仍按单元顺序写，
所以同事那台就算还是旧版本也读得对。

**配乐是故意留在按位置的**：它记的不是「某个单元的什么」，是一段区间
（这几段连着铺一首曲子）。挪动确实会改变这一段盖住谁，那是要跟你说清楚的事
（界面照旧会提示「盖的范围变了，请回配乐轨确认」），不是搬一下就完的。

## 0.1.209

**接着修上一版那条：落脚点要自己盯着焦点。**

0.1.208 真机跑下来发现只在输入框那头交一次焦点是不够的 —— 那一下发生在按下
鼠标的瞬间，紧接着时间线自己的手势又把焦点清掉，空格和方向键照样全哑。

现在落脚点自己盯着：只要页面上没人拿着焦点就接回来。另外它一开始摆错了层
（套在动作表外面），按键能匹配上却没人执行，已经挪进去了。

## 0.1.208

**修：焦点交出去之后落空了，空格和方向键一起哑掉。**

上一版让输入框「点到别处就交出焦点」。真机上跑下来发现只做了一半：焦点交出去
之后没人接住，Flutter 不知道该把按键送到哪个快捷键表上 —— 空格、←→ 逐帧、
⌘Z 撤销**整套键位一起没反应**，一直要到手动点中一个能拿焦点的控件才恢复。
这比原来那个「空格被当成打空格」更糟。

现在工作台和编导台各有一个落脚点，而且它**自己盯着**：只要页面上没人拿着焦点
就接回来 —— 光在输入框那头交一次是不够的，紧接着时间线的手势又会把焦点清掉。
守卫也改成盯这一手，不再只看有没有失焦。

## 0.1.207

**补：编导台的输入框也交出焦点。**

上一版只接了工作台那两个（单元台词、这一镜的字幕）。编导台的脚本正文和屏幕字
是同一套写法、同一个毛病：打完字点到别处焦点不走，空格永远到不了播放器。

四个都接上了，并加了一条守卫钉住它 —— 少接一个，空格就在那一个上失灵，
而这种失灵不会有任何报错。

## 0.1.206

**修：手改过的字幕，从来没跟着台词语义单元走。**

挪动单元顺序、或者删掉一个单元之后，你给 U3 某一镜改好的那句字幕会烧到 U2
的画面上。不报错，只有把片子导出来看一遍才发现。

按单元记的数据一共五份：替换方案、配音、配乐区间、手改字幕、以及生成好的
配音文件。前三份一直在搬，字幕这份从头到尾漏在外面 —— 现在补上了，走的是
和另外三份**同一个**下标映射，并入了那条穷举测试（原来盯四份，现在盯五份）。

**修：打完中文之后，空格不能播放/暂停了。**

macOS 上点了别处输入框不会自动失焦，于是焦点还留在台词框里，空格一直被判成
「在输入框里打空格」放行掉。现在点到别处就交出焦点 —— 既把这一格提交掉，
也把空格还给播放/暂停。输入法拼音阶段的空格（选词上屏）不受影响。

四个常驻页面上的输入框都接上了（工作台的单元台词、这一镜的字幕；编导台的
脚本正文、屏幕字），并加了一条守卫钉住它 —— 少接一个，空格就在那一个上失灵，
而这种失灵不会有任何报错。

**另附一份清点**：`docs/2026-09-09-台词语义单元身份清点.md` —— 有多少数据是
按位置下标记的、哪些没跟着单元走、以及「给单元一个稳定编号」的方案。

## 0.1.205

**修：删掉一个手动加的台词语义单元，会把整条片子的单元边界重铺一遍，
而单元里的视觉镜头没跟着走。**

同事报的两条 —— 「合并视觉镜头之后后面变成缺失的」「选中一个镜头去替换，
预览跳回 U1 的 S1」 —— 起因都不是合并、也不是替换，而是**在那之前删过一个
手动加的单元**。

那条路一直走的是空白任务那套：把所有单元的起止重新铺成连续的一条。空白任务
这么做是对的（它的时间轴本来就是分子一个个排出来的，分子里也没有视觉镜头），
但有原片的任务里，单元的起止是原片坐标，单元里的视觉镜头也是。只挪单元、
不挪镜头，两层就此各说各话：

```
U2 单元 16033-35868   镜头 55467-75302   ← 对不上
```

镜头轨于是画到别处去了 —— 你看到的「单元尾部缺一块」，就是那几个镜头跑到
别的位置上了；点中的和播的不是同一段，也是同一个原因。

**已经坏掉的任务会在打开时自动修回来**：一个单元里的视觉镜头本来就首尾相接、
正好铺满这个单元，所以镜头的首尾就是这个单元的真身。只在「长度一致、只是整体
被平移」时才修；长度都对不上就不猜，原样留着。

## 0.1.204

**修：手改完标签，属性栏没跟着变。**

你说的那条 —— 只看到「手改过」三个字，下面还是原来那几个，得点开「选标签」
才看得出确实改了。

原因是属性栏一直按**模型打标那一刻的维度**分行显示。你自己挑过之后，模型那份
维度就描述不了眼前这几个标签了：你加进来的「痛点」「外壳」在它那份里查无此人，
只能被归到「其他」，而它们明明就在植源分子库里。

现在**手改过就不分维度了**，属性栏原样摆你挑的那几个 —— 和「选标签」弹窗里
勾着的完全一致。没手改过的照旧按维度分行，那时分维度才是用来核对模型判得对不对。

## 0.1.203

**修：改标签时，同时挂在两个标签组里的标签会被写两遍。**

真机上「痛点」既在植源分子库里、也在植源动作里。进「改标签」什么都不动、
原样点一下确定，这一镜的标签就从 4 个变成 5 个 —— 多出来的那个是「痛点」
的第二份。改几次就是一串重复。

这份标签既是界面上展示的那一排，也是替换裂变的检索键，重复项两头都会带进去。
现在同名标签只出一次。

## 0.1.202

**改：替换裂变里的视觉镜头替换，一律整条素材变速铺满原镜头时长。**

你看到的「本该变速的分镜视频没有变速，而是被剪切了」——那是上一版为了把倍速
压回 1.0 做的：从长素材里截坑位那么长的一段。代价是画面被剪掉大半。

现在按你定的来：**选中的那条素材整条压进原镜头的时长里，一帧都不丢。**
20 秒的素材配 0.5 秒的坑位就是 40 倍快放，软件不替你截，但把这个代价如实
标出来——候选卡和已选托盘上直接写「40×」，1.25 倍以内绿色、2 倍以内橙色、
再往上红色。Agent 那边 `candidates --probe` 的 `speedIfPicked` 也是同一个数。

素材开头有转场或黑帧、想从中间开始用，仍然可以指定起点；**跳过那一截之后，
剩下的照旧整条铺满坑位**，不是只取一个坑位的长度。

顺带修掉一条一直没人发现的分叉：**预览不认取段起点**（只有导出和剪映认）。
三条路现在共用同一个答案——预览里看到什么样，导出来就是什么样。

整体替换（U 层）不受影响：那一层换的是整段画面加声音，时长本来就跟着候选走。

**修：改完视觉镜头标签，属性栏里那几个标签不跟着变。**

「改标签」写的是镜头自己的标签，属性栏画的却是打标那一刻模型的原始回答——
它是过程量，你手改一个字都不会动它。于是你删到只剩一个，界面照旧摆着原来四个。

现在维度只当分组依据，内容一律以当前标签为准：删掉的立刻消失，你手加的
（模型词表里没有的那些）归到「其他」一行，一个都不会被吞掉。

## 0.1.201

**修：挪动台词语义单元时，挑给它的素材没跟着走。**

你报的那条——把自定义单元拉到最前、给它挑素材、再拉到后面，素材留在了原来那一格。
补充的那条（自定义单元"已经有镜头替换"所以不让整体替换）是同一个病根。

查出来是**同一件事有两套算法**：

- 单元本身和替换方案：先插入再删除，**往后挪时落点少一格**；挪到相邻的下一位
  更离谱——完全是个空操作，你拖了一下列表纹丝不动
- 配音和配乐：按下标重映射，语义是对的

两套一起跑，结果自然对不上。现在合成一份，并加了**穷举**测试盯着：任意单元数、
任意挪法、任意被标记的单元，四份按下标记的数据（单元、替换方案、配音、配乐）
必须落到同一格。

**顺带修掉一条更隐蔽的**：重排和删单元这两条路只改了内存里那份替换方案，
**没写回任务、也没落盘**。挑素材、剔除候选那几条路一直是「改内存 → 写进任务 →
落盘」三步一起做的，唯独这两条漏了后两步——于是当时看着是对的，重开任务
素材就退回挪动之前那一格，而且不报任何错。加了守卫（反向验过）。

## 0.1.200

**双击时间线上的字幕块，就地改。**

字幕轨只有 22px 高、窄镜头的块可能只有几十像素宽——把块体本身变成输入框做不出
能用的东西。所以双击弹一个贴着它的浮层：定宽、一镜有几行就列几行，背景不压暗，
你能一边改一边看时间线上那一块在哪儿。Esc 或点浮层外关掉。

**单击不变**，还是选中那一镜——不打断你现在的习惯。

浮层里那张卡**就是右侧属性栏那一张**，同一份实现。两个入口分开写的话迟早
行为不一样（一边离开才提交、另一边每敲一下就提交），人只会觉得「时好时坏」。

**字幕块上标行数**：轨上只画得下头一句，多于一行就在右上角标个数。
块窄到放不下两样时**文字优先**——角标只说「还有几行」，文字才告诉你是哪一句。

**双击判定改用事件自带的时间戳**，量的是你手上那两下的真实间隔，而不是
「软件处理到这两下之间隔了多久」（中间夹着选中、重建这些活，UI 忙的时候后者
会偏大，偏大就意味着明明双击了却被判成两次单击）。

## 0.1.199

**修：调完单元顺序，声音会变。** 你报的那条——把 U1 挪到别的位置、或者在它后面
插一个单元，U1 的背景音就不对了；放回原位又恢复。

根因和之前那一串是同一个病：**「哪几段被配乐盖住」是用原片时间表达的**。
它拿列表下标去取那两格的原片起止，拼成一个原片时间区间，再和每一镜的原片
区间比重叠。列表顺序和原片顺序一致时恰好对，一调序就指到别的段上——顺序换得
狠一点还会首尾颠倒（起点的原片时间比终点还晚），于是一格都命中不了。

这个判断决定这一段用**纯人声**还是**原片原声（含背景音）**。判反的后果：
要么老背景和新配乐一起响，要么背景音凭空消失。你听出来的就是这个。
**预览和导出两条路都中招**，现在都改成按单元下标问。

**改字幕时不再每敲一个字就重烧一次。** 一句十来个字就是十来次 ffmpeg，
横幅一直在闪，而中间那些半截状态（「李」「李斯」「李斯特」…）没有任何意义。
现在**离开这一格才提交**，回车也算改完。删一段、加一段仍然立刻生效——那不是敲字。

## 0.1.198

**我自己传了一条视频从头跑一遍回归，点出两个新问题，都修了。**

**一、底部那行把两个数写反了。** 加一个台词语义单元之后写着
「时长 75.3s（原片 85.3s）」——原片是 75.3s，成片才是 85.3s。
根因：加单元时把「原片时长」这个字段改成了手加单元链上去的假末尾。
加一段进来变长的是**成片**，原片一帧没多。

**二、连点三次「+1 帧」，属性栏只动一帧。** 盘上确实走了三帧，界面停在第一帧。
成片时间轴是在外层算一次再传给各面板的，而逐帧微调只通知面板重建、外层不重建
——面板拿到的是**上一次算的那条轴**，数字就停在改动之前。这正是你反复撞见的
「改了没反应」那一类。现在面板只收原料、在自己的重建里现算（实测 0.4µs），
结构上不可能再变旧，并加了守卫盯着。

顺带：播放器的总时长也改成跟着编辑重算，否则拆分/微调之后末尾又会对不齐。

## 0.1.197

**帧的算术做对了。**

- **`±1 帧`真的走一帧。** 以前是「当前毫秒 + 33ms 再吸附回帧点」，而帧点在
  毫秒轴上是非等距的（30fps 下 0,33,67,100…，间距 33/34 交替）。走 30 步只有
  990ms，而 30 帧是 1000ms——你逐帧调边界调得多了，就会莫名其妙少走一帧。
  现在按帧号加减。
- **29.97 和 30 分得开了。** 广播帧率天生是分数（29.97 其实是 30000/1001），
  以前一律转成小数存，`round(1000/fps)` 对这两种帧率都得到 33ms，软件里完全
  区分不了。现在保留原始分数。

**素材帧率那件事是我判断错了，特此更正**：我在方案里写"跨帧率素材没有按帧
重采样、会漂"，实际查证下来**本来就是对的**——ffmpeg 那一步已经显式归一到
项目帧率，目标帧率也在缓存指纹里。所以拖一个 60 帧的素材进 30 帧的片子，
一直是对的，不需要改。方案文档已订正。

Agent 侧同步：任务 JSON 补 `fpsExact`（`30000/1001` 这种精确形式）。

## 0.1.196

**时间轴重构一期：把那条会算错的路彻底删掉。**

这段时间你连着遇到的四条——整格画不出来、点不中、双击播不了、末尾对不齐——
是同一个病灶发出的四个症状。前几版都是在改调用点，这一版改的是病灶本身。

病灶：数据存的是「取自原片哪儿」，界面和播放要的是「在成片哪儿」，两者靠
`原片时刻 → 成片时刻` 这个换算连着。而这个方向**本身没有唯一答案**：手加的
单元没人覆盖、开区间边界必然落到相邻那一段；列表顺序和原片顺序一致时恰好相等，
所以平时看不出来，一调序就失效——而拖动调序是一等功能。

这一版把这条路整个删了。「这一段在成片哪儿」现在只能按**列表下标**问：
绘制、命中、双击播放、配乐段、换方案时的位置记忆，全部改过来了。
四条守卫盯着它不复活，每条都故意打断验过。

**顺带修好三处一直是错的地方**（都属于同一个病灶，只是没人报）：

- **胶片条整条错位**：原来把每一格的原片时刻直接换算成像素，调过序就铺到别人
  身上。现在按单元分段放——手加的那一段留空，因为原片里本来就没有它。
- **跟随滚动跑偏**：播放头本来就是成片位置，却又被当成原片时刻换算了一次。
- **换方案时位置跳走**：原来锚在「原片时刻」上，而垫黑场那种段根本没有真实的
  原片坐标。现在锚的是「第几个单元、往里多少、当时它有多长」，
  整体替换换短再换回来时「停在一半」还是一半。

这一版**不动存档格式**，老任务照常打开。帧仍然是显示时换算的，跨帧率素材
仍然靠重编码兜底——那是二期的事，见 `docs/2026-09-08-成片时间轴重构-TRD.md`。

## 0.1.195

**修：调乱顺序之后，最后那个台词语义单元点不中、也播不了。**

你说的「U6 不能正常播放」，根子不在播放——是那一格**根本点不中**：点下去
选中态停在上一格，自然也双击不出播放。

和上一版修掉的「整格画不出来」是同一个错误，只是这次犯在另外两处：命中判定
和双击播放都拿 `unit.endMs`（开区间）去换算成片位置，而它落进的是相邻那一段。
手加的单元被拖到最前之后（它在原片上的占位排在末尾），最后那个单元的右边界
被算成了手加单元的成片起点 0，区间左右翻转——画不出来、点不中、双击时播放
区间变成「从 104 秒播到 0 秒」。

现在绘制、命中、播放三处一律**按列表下标问成片轴**，并加了一条守卫盯着：
再有人拿原片毫秒去换算像素就会立刻挂（守卫是把接线改回旧写法反向验过的）。

**顺带修掉一个更深的错位**：还没挑素材的那几段在预览里是「空的」，而 mpv 的
EDL 没有「空档」这个概念——留洞会被直接压掉，从那儿起后面所有内容都提前
一截。表现是时间线画到 01:47、播放器只走到 01:37，跳到最后一格落在片尾。
现在给这些段垫一小片黑场（画面 + 静音，按规格和时长缓存复用），时间就对上了。
**垫黑场不是把问题盖过去**：播到那儿照样停下来点名，你仍然知道这一段是空的。

## 0.1.194

**修：调乱顺序之后，原片里最后那个台词语义单元在时间线上整格消失。**

镜头轨上还画着它的 S1、S2，单元轨那一段却是空的——两条轨对不上。

画块体时用的是「原片毫秒 → 成片毫秒」这一步换算，而它是按「谁的原片区间盖住
这个时刻」找的。`endMs` 是开区间，落进的是**相邻那一段**。列表顺序和原片顺序
一致时两者正好相等，看不出问题；一旦手加的单元被拖到最前（它在原片上的占位
排在末尾），原片里最后那个单元的右边界就被算成了手加单元的成片起点 0，
矩形左右翻转，整格什么都画不出来。同一个换算也吃掉了最后一镜（它的 `endMs`
等于所属单元的 `endMs`）。

现在单元块、镜头块、字幕块、配乐框选一律**按列表下标直接问成片轴**，
不再拿原片时间绕一圈。

## 0.1.193

**还没挑素材的那几段，不再被悄悄跳过——播到那儿会停下来点名。**

上一版为了不去原片里取一段不存在的时间，把这些单元整个跳过了。结果换成了
另一种静默降级：它在成片里凭空消失，后面的单元全部前移。你把手加的单元拖到
最前面，一按播放直接从 U2 开始，时间线画到 01:47 而播放器只到 01:36——
看起来就像软件把你加的那一段吃了。

**跳过的是画面，不是时间。** 现在：

- 这一段照样占着它在成片里的位置，后面的单元不再前移，**末尾和时间线对齐了**
- 打开任务就有一条常驻横幅点名：「U1 还没选素材，这几段放不了——去
  『替换素材』给它们挑，或者把它们删掉」
- 按播放走到那一段会**停下来**并再说一次，不是画面冻住让你猜
- 再按一次播放就**跳过它继续往下看**——已经提醒过了，你要看后面的就让你看

插在中间的同样如此：播到那个位置停下来，点名是哪一段。

**播放器的总时长改成成片总长。** 以前用的是原片总长，手加单元、整体替换都会
改变片长，于是末尾永远差一截。

## 0.1.192

**界面上的时间数字，从今天起一律是成片时间，而且每次现算。**

以前左栏和属性栏给的是**原片**位置，时间线和播放器给的是**成片**位置。
同一镜，属性栏写 `00:45.03`，时间线画在 `01:03`——两个数谁也对不上谁，
而且没有任何地方会报错。手加的台词语义单元更离谱：它被塞在原片末尾，
左栏于是显示 `01:36.07–01:46.07`，而原片总共只有 96.2s，那是个纯假数字。

现在：

- **主数字全是成片位置**，也就是「这一段在最终片子里落到第几毫秒」。
  你加一个 15 秒的单元、删掉、再加一个 20 秒的，所有数字当场跟着动——
  这是对的，因为片子真的变了。
- **要看原片位置的地方单独一行**，写着「取自原片 00:00.00 – 00:02.19」，
  灰色小字。它是给你回原片对素材用的参考，不和成片位置挤在同一个字段里。
- **手加的单元不再显示原片时间**——原片里根本没有它。
- 单位保持 `分:秒.帧`（`01:36.07` = 1 分 36 秒第 7 帧，不是百分之一秒）。
- 手加单元的长度按整帧对齐：带个零头的话，后面每一段都跟着偏，拼到片尾
  越差越多，而界面显示的是帧，你看到的就是「每一段都对，加起来差了一帧」。

**存的还是原片毫秒，一个字节都没多存。** 成片位置是算出来的，不落库——
存进去的话，加一个单元就要重写每一个单元再落盘，写一半崩了就烂在盘上。
实测全量重算 0.4µs（60 单元 2700 镜也只要 3.5µs），而时间线光画那些省略号
每帧就要 2.1ms。这笔账在现有开销里根本看不见。

**Agent 侧同步**：任务 JSON 里每个单元、每一镜都给两套，基准写在名字里——
`sourceStartMs`/`sourceEndMs` 是原片，`composedStartMs`/`composedDurationMs`
是成片。整体替换那一段长度跟素材走，素材还没下到本地时**整块不报**并点名是
哪几个单元卡住了，不给一个差不多的数——Agent 会拿它当真数用。

## 0.1.191

**这一版是我自己在机器上一条条点出来的**，不是只跑测试。下面每条都附了真机结论。

**字幕参数调了没反应——预览根本没接那套样式。** 顶栏「字幕」里调的字号、位置、
描边，导出一直是按它来的，而**预览的变速切片压根没接**，永远用默认那套。两边
对不上，人怎么调都看不到变化。现在预览也用任务里的样式，调完立刻重渲。

**字幕样式弹窗正好压在播放器上。** 调字号、位置全靠看预览判断，而弹窗把画面
盖住了——只能关掉才看得见，关掉又没法再微调。现在靠右摆，预览完整露出来。
（第一版改法在外面套了一层 Align，真机上一看还是居中：对话框内部自带一层
居中，会把外层撑满。改用它自己的 alignment 才生效。）

**点时间线上的字幕块，直接选中那一镜。** 以前轨上画着那几段字，点下去什么也
不发生，人得自己在镜头轨上数出是第几镜、再去右边找编辑框。

**手加的台词语义单元不能正常播放。** 原片里没有它，它的时间只是时间线上的
占位、落在原片时长之外；预览却照着这段区间去原片里取画面——取的是一段根本
不存在的时间。判据从「这条任务有没有原片」改成「**这个单元**有没有原片」。
挑了素材的正常播；素材还没下到本地的会说「这一段还没准备好」，不是黑屏。

**手加的单元每打开一次任务就白烧一次 AI 调用。** 它没有台词也没有画面，
打标只会返回空，却一直排在待打标队列里，永远打不完。它的标签本来就是手填的。

## 0.1.190

**修：字幕那三条全是坏的。** 上一版把字幕轨发出去了，但三条路各断在不同地方，
只有属性面板里那份数据是对的。

**一、中文打不进去，只能粘贴。** 编辑框每次重建都在现造一个新的输入控件，
而中文输入法要先把拼音摆在候选区、选好字才上屏——每敲一个字母都会触发一次
重建，候选区当场被清掉，拼音永远拼不完。粘贴是一次性整段塞进来、不经过候选区，
所以只有粘贴是好的。

**二、改完字幕，烧录出来没变化。** 导出这条路根本没收到那条轨：对话框、
「导出全部」、命令行三个入口都把它丢了，一路按 ASR 原样烧。

**三、改完字幕，预览里也没变。** 预览里被替换的那一段是提前烧好字的切片，
它同样自己按 ASR 算；更麻烦的是判断「要不要重渲」时只看候选素材和坑位长度，
改了字也认定「入参没变」直接跳过——就算接上了轨，看到的还是上一版。

根子是同一件事有四处各算各的。现在收成一个出口，谁想知道「这一镜烧哪几行字」
只能问它，并加了三条守卫盯着：不许有第二处自己算、导出的每个入口都得接上、
命令行也得接上（Agent 走的是同一条路）。守卫是把接线打断反向验过的。

## 0.1.189

**换过素材的镜头，字幕可以手改了。** 时间线上多了一条「字幕」轨，位置在
`视觉镜头` 和 `配乐` 之间；选中那一镜，右侧属性面板就能逐句改文字、删一段、
加一段，手改过的块在轨上标成蓝色。改完不满意点「改回自动」，退回按 ASR 算的
那一份。

为什么要这个：字幕默认按 ASR 的**词级时间戳**分给各镜，而「哪里断句好看」
是编导的判断，规则算不对——真机上「了」这个字的声音落在下一镜，那一镜的字幕
就以一个孤零零的「了」开头。ASR 没错、切点也没错，只是没人能替编导决定这个字
归哪句。

**字幕是字幕，台词是台词**：改字幕不动单元的台词，打标、检索、换音色照旧
用台词那一份。

轨上只有**换过素材的镜头**才出块——没换的那些，字幕烧死在原片像素里，我们
既读不出也不重渲，摆个编辑框只会让人以为改得动。整体替换的段落不给字幕轨。
切分变了会问你「清除还是保留」这一镜的手改。

**属性面板里的标签可以手动改了。** 台词语义单元和视觉镜头都能删掉不要的、
从妙啊的标签库里加上要的，限在这条任务选中的标签组内。手改过的标签**不会被
自动打标覆盖回去**——包括你手动删空的那些。

改完会提醒一句：之前的候选是按旧标签搜出来的，可能要去「替换素材」重新搜。
换不换由你决定，软件不替你重跑。

**修：音频波形轨整条画在画布外，而且滚不到。** 加字幕轨时预留高度还按五条轨
算（290px），画的时候却无条件占位画到 330px。更糟的是外层滚动容器的子高度取
`max(视口高, 预留高)`，预留高比视口还矮时子高度就等于视口高度，**根本不产生
滚动**——那条轨永远够不着。窗口高度接近最小值时会撞上。

顺带把字幕轨改成常驻（空着也在），不跟着内容出现/消失：会显隐的话，一条还没
换过素材的片子看着就像「这个功能我这儿没有」。标题条上写清楚
「只有换过素材的镜头才烧字幕」。

## 0.1.188

**替换掉的分镜，可以挑放它自己的哪一路声音了**——不再是「要/不要」两选一，
而是四选一：

| 档位 | 放的是什么 |
|---|---|
| 不播放（默认） | 只有原片这一段的声音 |
| 人声 | 素材里说话的那一路，背景音被分掉 |
| 背景声 | 素材的现场音（水声、喷雾声、环境音），说话声被分掉 |
| 原声 | 素材原样的整条声音，不分离 |

上一版只有「原样保留」一档，于是素材自带的口播也会一起放出来——软件只能提醒你
「会和台词打架」。现在直接有解了：**要现场感选「背景声」**，说话声被分掉；
**要那句话选「人声」**。那条提醒也跟着收敛了，只在真的会打架的两档才出现。

顶栏是全片打底，单个镜头可以覆盖：**跟随全片 / 四档之一**，各自带音量。
「跟随全片」和「不播放」不是一回事——全片放原声时跟随等于放，而「不播放」是要
这一镜真的闭嘴。

**选了人声或背景声会当场开始分离**（一条素材十几秒），界面上说清正在分、
分完自动用上；同一条素材换到别的镜头再选不会重跑。

**分不出来就停下来点名，绝不悄悄放原声**——你选的是另一路声音，悄悄换掉的话，
要把片子导出来听一遍才可能发现，而那时可能已经交付出去了。没装分离工具会直接
指路去「设置 → 运行环境」，并告诉你可以先改成「原声」/「不播放」。

**拖完单元顺序，时间线现在跟着重画了**。上一版拖动之后左栏改了、时间线纹丝不动
——那一格还画在拖之前的位置。原因是时间轴只在「总时长变了」时才换，而调顺序
恰恰不改变总时长。

**人声分离修好了——这台机器上它其实一次都没成功过。** 软件一直把视频文件直接
交给分离工具，而那个工具认不了 mp4，只会回一句「格式无法识别」；界面上你看到的
永远是「分离失败」。现在先抽一道音频再交过去，实测十几秒分完（第一次要下模型，
会久一点）。原片的人声轨、素材的人声/背景声，走的都是这一条，一起好了。

**Agent 侧同步**：`ishkafel unit audio --audio none|vocals|background|original|follow`；
任务 JSON 里补上了单元的 `hasSource`（是不是手动加的）、镜头的声音档位覆盖、
以及全片打底——存了却不报的话，Agent 查到的空看起来正好像「没问题」。

## 0.1.187

**台词语义单元可以拖顺序了，两种模式都能。** 把 U2 拖到 U1 的位置，它就成了
U1——列表顺序就是成片里的先后。整句话跟着搬：台词、原声、字幕都跟着它走，
导出时按新顺序重剪。

挑给这个单元的素材、给它配的音、盖在它上面的配乐，也都跟着一起搬。有一处
得说清楚：**配乐是按连续几段铺的**，把中间某一段挪走会打断它——这时会弹一句
提示让你回配乐轨确认，而不是悄悄改成另一个范围（那是你照着内容选的曲子）。

**上传了参考视频的任务，现在也能添加台词语义单元。** 以前只有「没有参考视频」
那种模式能加。新加的单元原片里没有对应画面，所以成片会比原片长——这是有意的，
不会去挤原片已有的段落。加完接在末尾，想排到前面拖过去就行；不想要了也能删
（分析切出来的那些不给删，那等于把原片少放一段）。

新加的单元**自己打标签**——和「没有参考视频」那种模式一样，在右侧属性栏里点选。
这不是顺手给的便利，是它唯一的活路：原片里没有它，也就没有台词，模型没有任何
东西可以据以打标；而标签就是去妙啊搜画面的检索键。不能手填的话，这个单元永远
搜不出素材，加了也只是个一直挂着「待填」的空壳。

同时属性栏里那些对它不成立的东西也不再摆出来（台词框、换音色、拆分/并入）——
它没有台词可念也可拆。这几样以前是按「是不是空白任务」判的，现在按**这个单元
有没有原片来源**判：同一条任务里现在两种单元并存了。

新加的单元**必须挑素材**：原片里没有它，不挑就是真的缺一段。没挑就点导出会被
拦下来并指名是哪几个，不会拿黑帧顶上，也不会等你把整批片子导完才发现。

**删单元时配音没跟着搬**——修掉了。删掉 U2 之后，本该念 U3 的那条配音会跑到
U2 身上。不报错、不进日志，只有听出来才知道。

**替换掉的画面，可以留住它自己的声音了。** 视觉镜头替换换的是画面，那一段的
口播照旧来自原片，所以候选素材自己的声音一直是丢掉的。现在给一个可选项：
让它作为额外一层叠回来——口播、素材原声、配乐三者同时响。要的通常是那个
现场感（水声、喷雾声、环境音）。

顶栏「素材原声」是全片打底，单个镜头可以在右侧属性栏里覆盖：**跟随全片 /
单独开 / 单独关**，各自带音量。三态不是两态——全片开着时「跟随」等于开，
而「关」是要这一镜真的闭嘴。

**默认全片关着**，升级这一版不会让你已有的任务导出来的声音变样。

有一条要留神：**很多素材自己带口播**，放出来就是两个人同时在讲。软件知道这件事
（素材的语音转写），开的时候会就地提示一句，但不拦你——有时候要的就是那句话。

**Agent 侧同步补齐**：新增 `ishkafel unit` 命令，加/删/调顺序/填标签/素材原声
五件事都能做，支持可视模式。人在界面上能拧的旋钮，它一个不少。

## 0.1.185

**打标打完了，界面却一直说「未打标」**——修掉了。连带着「替换素材」搜不出
东西，也是同一个原因。

切分一好就放人进工作台干活，打标（占整段分析七成的时间）在后台接着跑——这是
有意的，不该让人对着「分析中」干等三倍时间。但工作台拿的是进门那一刻的任务，
打标跑完只写了盘，**开着的这一页永远不知道**。于是属性栏一直写着「未打标」，
而盘上的标签一个不少。

替换裂变是被它连累的：搜素材用的检索键正是台词语义单元与视觉镜头的标签，
界面这份数据里标签是空的，自然什么也搜不出来。

现在标签一落盘就自动补进正开着的工作台，不用退出重进。

**顺带修掉一个还没人撞上的丢改动**：打标结束时会拿「打标开始那一刻」的单元
整份写回去。人在这七成时间里拖的边界、改的台词，会被悄没声儿地抹掉——不报错、
不进日志。现在只把标签合并回当前的单元，边界变过的一律不认。

**审核页能看见、也能改标签了**。每个段落的标题旁边列出这一段的标签：整段替换
给台词语义单元的标签，逐镜头替换给那个镜头的标签——它们就是这一段候选的检索键。
看不见的话，人只能凭感觉判断候选像不像，猜错了也只会反复剔除，而标签打偏了这个
根子一直没人动。

点「改标签」可以就地删掉不要的、加上想要的，**只能从这条任务自己的标签组里挑**：
标签是检索键，给出标签组以外的词等于让人选一个这个项目根本没素材的标签，
搜完才发现是空的。

**装好工具不用再重启 app**。设置页的「重新检测」以前点了没用——工具路径在启动
那一刻就被记住了，包括「没找到」这个结论，之后再也不看第二眼。装好 ffmpeg 后
顶部那条横幅不消失、功能不恢复，也是同一个原因。现在这些都当场生效。

## 0.1.184

**人声分离不再重复干同一件活**。上一版把「找不到 ffmpeg」修好之后，立刻暴露出
一个被它盖住的问题：同一条素材会被同时分离十几遍，还都往同一份文件里写——
机器被拖到几乎不动，产物也不可靠。

现在同一份产物在跑的时候，后来的请求就等它，不再另起一个。

## 0.1.183

**「重新分离」点下去报「未检测到人声分离工具」，可工具明明装着**——修掉了。

真正缺的是分离工具自己要用的 **ffmpeg**。从访达/程序坞启动的 app 拿到的
环境里没有 Homebrew 的目录，分离工具去找 ffmpeg 找不到，而这个错误被说成了
「工具没装」。照着提示反复装，装的却是另一个东西，自然永远好不了。

现在：

- app 起的每一个外部工具都能找到 ffmpeg
- 真缺 ffmpeg 时就直说是 ffmpeg，不再指错方向

## 0.1.182

**修一条「怎么重新分析都好不了」的老毛病**：有的片子铺完配乐后一直挂着
「没有分离出纯人声轨」，按提示重新分析多少遍都没用。

原因是两条任务共用一条源片时，人声轨被记成了共享的东西。人声轨其实**归任务
所有**——先做的那条任务被删掉时，它名下的产物被一起清走，另一条任务就指着
一个空地址；而分析结果是缓存过的，重新分析直接命中缓存返回，根本走不到
重新分离那一步。

现在：

- 每条任务都有自己的人声轨，谁删谁的都不影响另一条
- 复用切分时仍会为当前这条片子单独分离一份，进度条上会看到「正在分离人声」
- 提示改说真话：工具没装就指路去「设置 → 运行环境」装；工具是好的，就直接给
  一个**「重新分离」**按钮——就地补这一份，十几秒，不用再等几分钟的重新分析

## 0.1.181

**把「打包」和「上线」拆开**——它们是两件事，词也分开用。

| 词 | 做什么 | 谁做 |
|---|---|---|
| 打包 | 版本号 +1，构建出产物 | 开发随时做，做多少次都行 |
| 上线 | 推到线上，所有人下次打开就看到更新提示 | 产品负责人点头才做 |

产物存在 ≠ 用户能拿到。上线是对外的、收不回来的动作，而中间那些验证用的、
只改了日志的、还没验完的版本，打包留在本地就够了。

- `./scripts/pack.sh` 不再自动上线（此前每改一行就往所有人机器上推一版）
- 新增 `./scripts/publish.sh`：先把这一版改了什么摆出来，问一遍再推
- 两个词写进术语表，架构测试盯着这条规矩

## 0.1.180

设置 → 关于里加了「有问题找谁」：产品负责人**方泽文**，头像 + 飞书。

用着别扭的地方、做不了的事、想要的功能，都去飞书上找他反馈；他统一收集，
评估之后排进后续版本的开发计划。说不出去的抱怨会烂在心里，问题也就永远
不会被修——把人和去处摆出来，反馈才有一条真实的路。

放在「关于」里：来这一页的人多半正是遇上事了要查版本、要找人，而版本号
那张卡本来就写着「反馈问题时附上版本号」，正好接上。

## 0.1.179

补标签这条路加上诊断日志：「扫了 N 条任务，M 条欠着（哪几条）」。

上一版修完之后，排查「到底有没有扫到、扫到了几条」只能靠猜——查不出问题时，
这是第一个要排除的。没有打标服务（AI 凭据未配置）时也会说一句，
而不是静静地什么都不做。

## 0.1.178

**修：上传视频后不打标——打标跑到一半 app 关了，就永久丢了。**

打标是切分落库之后转入后台跑的（让人 26 秒就能进去看切分，不用等占七成
时长的打标）。这个优化本身是对的，错在它**只做了乐观路径**：app 一关——
崩溃、手动退出、升级重启——打标就没了，而且：

- 任务状态已经是 ready，看起来一切正常
- 没有任何地方记得「这个任务还欠打标」
- 界面不提示，也没有重试入口，下次打开也不会自己补

真机上丢过一次：一条刚上传的片子 35 个镜头**一个标签、一句描述都没有**，
人看到的是「上传完视频进去发现没标签」，找不到原因，那一趟的钱也白花了。

- **每次启动扫一遍，欠的接着打**。判据是镜头有没有画面描述——AI 打过标
  一定会写一句，而标签可能本来就一个都不合适（那是打过的合法结果）
- 只补欠的那几个单元，打过的不重打（重打就是白花一次钱）
- 补的时候**界面上说清在补哪一条、到第几个**——这是软件自己干的活，
  不占 Agent 的播报通道
- 补完前会重读任务再写：补标签期间人可能正在工作台里改这条片子，
  拿手上那份旧的整个盖回去会把他的编辑抹掉

## 0.1.177

验证自动更新的最后一段：**用旧版真机看一眼「有新版本」的提示长什么样**，
再点一下走完下载、替换、重启。这一版除了版本号没有别的改动。

## 0.1.176

**自动更新正式开张**——发布落点建好了，这一版起就能一键升级。

- 在火山 TOS 上建了私有桶 `ishkafel-release`，安装包存在这里
- 建了只读子用户 `ishkafel-updater`，权限只有这个桶的 `GetObject`。
  编进产物的是它——**验证过：能读，写被拒 403**。产物里的凭据是明文的，
  给到最小权限，泄露的后果就只是「别人能下载安装包」
- 发布用的是另一对可写凭据，只留在打包这台机器上，不进产物
- `pack.sh` 打完包自动发布

装了这一版以后，往后每次新版本都会在主界面顶栏提示，点一下就能升。

## 0.1.175

**对照最初那句话复查，补上四处没做到的。**

原话是「可以提示它，然后点击」——而更新入口只放在了设置 → 关于里。
人不会天天进设置页看有没有更新，等于这个提示不存在。

- **主界面顶栏直接提示**「有新版本 x.y.z」，启动就查一次，点一下就能升
- 更新流程收敛成**一个对话框**，主界面和设置页走同一个——两处各写一套，
  迟早一处改了另一处没改
- **升级收尾失败要说出来**：说明书没装上却不吭声，人以为已经是新的，
  Agent 照着旧手册调新命令，报错还查不到根因
- 上传路径补了**真的发一次请求**的测试（本地服务器收），方法、路径、
  签名、正文都验过——刚在 `codesign -q` 那儿栽过一次，同一个教训

## 0.1.174

**真机把整条更新链路跑了一遍，抓到一个会让所有人都升不上去的 bug。**

- `codesign --verify --deep -q` —— **codesign 根本没有 `-q`**：它 usage 报错、
  退出码 2，于是**任何包都被判成「签名验证没通过」**。单元测试全绿，因为
  注入的是假 runner，我让它返回什么就是什么。拿真 zip 走一遍
  下载 → 校验 → 解压 → 验签 → 替换，才把它逼出来
- 替换脚本的退出码原来分不清「没换上去」和「换好了但没打开」——最后那句
  `open` 失败会让整体看起来像升级失败。现在换好就算成功，打不开人自己点
- 上面两条都补了**真的把脚本跑起来**的测试（不是只检查它长什么样）：
  换上去、搬不过去要回滚、旧进程还活着就不动手

## 0.1.173

**发版这一半也做完了：打包完自动发布，不依赖任何外部命令行工具。**

- 新增 `tool/publish_release.dart`：算指纹 → 传包 → 传清单，用预签名 URL
  直接 PUT。本机装没装 `tosutil` 都能发
- `pack.sh` 在配了可写凭据时自动发布；没配就跳过，不打断打包
- **先传包、后传清单**：反了的话清单已经指向新版本而包还没上去，
  这中间点更新的人会下到 404
- 清单也改成私有 + 预签名读——app 本来就带着钥匙，不用再维护一个公开
  地址、也不用操心 ACL。配置项从 6 个减到 5 个
- 发布用的**可写凭据和产物里的只读凭据分开**：写凭据进了产物，
  拿到包的人就能往发布目录里塞东西
- 搭建和发版写进 `docs/2026-09-02-自动更新的搭建与发版.md`

## 0.1.172

**应用内自动更新：有新版本时点一下，自己下载、替换、重启。**

发到今天几十版，每一版都要重新发包、对方重新解压、重新装命令行、重新换
说明书——而**版本对不上是最难查的一类问题**（旧 CLI 调新参数、旧手册教不
存在的命令，都撞过）。让软件自己升级，这些就都对得上了。

- 设置 → 关于里新增「软件更新」：查到新版本会列出这一版改了什么、要下多少 MB
- 更新前先确认（重启会打断正在跑的分析、配音、导出）
- 下载带进度；**下完先核对 sha256 和代码签名，再动现有的 app**
- 替换交给独立脚本：等旧进程退出 → 旧的先改名留着 → 新的就位才删 →
  重新打开。中途失败会把旧的搬回来，不会出现「升级把软件搞没了」
- 解压用 `ditto` 而不是 `unzip`——后者丢扩展属性，代码签名会失效
- **重启后命令行工具和 Agent 说明书自动跟上同一版**，说明书变了会提示
  「让 Codex 重新加载一次」。这条对手动换包的人同样生效

安全上的一条硬规矩：**安装包必须私有存放**。产物里 `--dart-define` 注入的
AI 凭据是明文躺在二进制里的（`strings` 一抠就有），包一旦公开读，谁拿到
链接都能提出来烧钱。所以只有 `latest.json`（版本号、指纹、更新说明）是公开
读的，包本身由 app 拿一对**只读**凭据现换预签名链接。

## 0.1.171

**修：写脚本时中文输入法打不出汉字（粘贴却可以）。**

根因是全页快捷键把空格绑成了「播放/暂停」——而中文输入法在拼音阶段按空格
是**选词上屏**。组合期间文本框并不「消费」这个空格（它没插入字符），于是被
页面级快捷键抢走，拼音永远上不了屏。粘贴走 ⌘V，不经过空格，所以一直是好的。

- 输入框上方包一层按键放行：空格、←→、Esc、⌘A、⌘Z/⇧⌘Z **停在输入框这一层**
  由它自己处理，不再冒泡到页面级
- 编导台的全页快捷键每一条都先问「人是不是正在输入框里」，是就让路
- 一起修好的还有：脚本行、**屏幕文字（打中文字幕的地方）**、找镜头的搜索框
- 「人在打字吗」这个判断原来工作台和编导台各有一份，收敛成一份
- 架构测试盯着：页面内的输入框漏包一个、快捷键漏加一条判断，当场红

## 0.1.170

**可视模式：界面稳稳停在 Agent 正在动的那一行，不再来回弹。**

Agent 在同一行上会连着播好几条（找镜头 → 看参考片画面 → 搜到候选 →
提交），此前每一条都触发一次「粗滚」。粗滚按平均行高估落点，而行高差得远
（一行可能挂着 9 个镜头卡片）——精调刚把这一行对准，下一条播报就把画面
拽回估算点。人看到的是它不停地从这一行跳回靠前的位置，盯不住它在哪儿干活。

- **只在换行时滚**：同一行上的后续播报，画面一动不动
- **已经构建出来的行，粗滚不出手**：它的位置由行自己精确对齐，
  粗滚的唯一职责是把还没构建的行带进构建范围
- Agent 收工后清掉记录，下次回来重新对齐一次

判据：Agent 从第 1 行做到第 27 行，界面往下走 27 次，不是上下弹一百次。

## 0.1.169

**自动铺一版：参考里这一行有几个分镜，就铺几个。**

此前它只看这一行的第一个参考分镜，整行只找一条素材塞进去——参考片里 9 个
镜头的一行，铺出来是一整条，节奏全丢了。

- 每一个参考分镜（原子）各自按**它自己的画面描述**去搜，一个原子一条素材
- 时长按**参考的比例**切：参考那一镜占这一行三分之一，铺出来就占三分之一
  （均分会把 0.5 秒的快切和 3 秒的定格拉成一样长）
- 素材短了照旧放慢补满；行内所有镜头时长之和仍然锁死等于配音时长
- 某一镜没搜到会**点名**（第几镜），其余照铺，不静默少一个
- 花钱的预告按**分镜**算：一句里 9 个分镜就是 9 次检索、最多 9 次识图，
  按句报会少报一大截
- 进度报到镜：「第 3 句（第 5/9 个分镜）」——一行铺半分钟，人得知道它在动

## 0.1.168

**参考片提取改回和替换裂变同一套切分**——分子怎么切，行就怎么切。

上一版的分行是另写的一套「一句一行 + 事后关联镜头跨度」，那等于永远走
语义分组的降级路径，还打破了「原子必须在分子内」的约束：一个完整镜头
横跨三行，同一个画面被切成 0.6 秒、0.4 秒、1.6 秒三张卡，比原子还碎。

- 提取现在走完整链路：ASR → **AI 按台词语义分行**（和替换裂变同一个分组器）
  → 边界吸附（帧／镜头／静音谷）→ 单元内部按画面切点切出参考分镜
- **一行 = 一个台词语义单元**（分子），一个卖点讲三句话就是一行；
  **行里面的参考分镜 = 视觉镜头**（原子），整个落在这一行里
- 没有台词的长段落（开头吸睛段、中间空镜、结尾定格）照旧单独成画面行
- 缺方舟凭据时退回一句一行，**并在日志里说出来**——那是降级不是正路
- 已经提取过的任务不会自动升级，重新 `script extract` 一次即可

## 0.1.167

**把「会改数据却没有舞台」的命令补齐**——上一版只修了配音和提取台词两条，
从头核一遍命令清单才发现还有五处在闷头改数据：可视模式下界面不会跟过来，
人看不见它动了什么。

- `script voice-file`（用我自己录的配音）、`subtitle`（字幕样式）、`bgm`
  （铺配乐）、`analyze`（分析原片，最长的一段等待）、`apply segment/tags`
  （切分与打标回填）——现在都会把界面带到该看的那一页，一步步说清楚
- **`apply plans` 修了一个会直接失败的坑**：界面的锁不会自己放，人打开过
  这个任务又退回了列表，锁还留着，于是命令判定「界面占着锁」而走委派——
  但请求是发给那条任务工作页的，列表页不接，结果干等 90 秒报「界面没有
  回应」。现在委派前会先把界面叫回这条任务
- 新增架构测试：会改数据的命令必须有舞台，只读/管理类才准进白名单
- 手册里那份「支持可视的命令」清单改成了**凡是会改数据的都支持**

## 0.1.166

**可视模式：每一步开工前都先确认「人看得见吗」。**

此前播报和界面是两回事——横幅一句句念「正在给第 10 句配音（10/20）」，
界面却停在任务列表，二十句从头到尾没有一格出现在屏幕上。播报没说谎，
可视化却没发生：它成了换个地方显示的日志。

- **界面会如实报出自己停在哪一页**（新的 `ui_where.json`），Agent 每走一步
  都读一眼：不在这一页就先把界面带过去，已经在就什么都不做——不会把页面
  销毁重建，也就不会再出现「画面弹回第一行」
- **被打断之后接着干也会重新确认现场**。人在打断的间隙多半已经把界面切走了，
  这一步就是为它准备的（不只开头确认一次）
- **新增 `ishkafel ui open <任务> [--module …]`**：把界面叫到现场。此前
  可视模式只有出口（`ui tasks` 把界面支开）没有入口，Agent 一旦支开就再也
  回不去
- **`ui tasks` 不再被当成「腾锁的标准做法」**：撞上界面的锁时命令会自动请它
  让位，而**人留在那一页看着**。手册和命令帮助都改了说法——主动支开界面
  等于亲手关掉可视化现场
- **配音、提取台词、导出、下载配乐这几步过去绕开了舞台**（裸写在场状态，
  只有字没有导航），现在一律走 `AgentStage`；架构测试盯着，以后加新播报
  绕不过去

## 0.1.165

把今天改过的东西补齐到 Agent 那条路上——**界面能做的，命令行也要能做；
行为变了的，手册要说**。

- **命令行也能拿参考镜的首帧去搜了**（`script shots --by ref-image`）。
  此前那套「本地帧传成查询帧」的逻辑只接在界面上，命令行只能拿**候选素材**
  的 fileKey 搜——手里攥着要复刻的那一帧却用不上，而这正是复刻最该用的
  一条路。`--materials` 在这个模式下指的是**第几个参考镜**（从 1 起）
- 手册补上今天变了的四处行为：拿参考镜首帧搜怎么用；**提取台词时就会定下
  音色**（不留空等人想起来，因为没配音就没有时间根）；**没配音的行会被拒绝
  挑画面**（坑位时长是 0，挑了也是瞎挑）；**从参考片提取时会自动产生画面行**
  （开头吸睛段、中间空镜、结尾定格——没人说话但画面是有的，照样要配画面）
- 加了一条测试盯着这件事：今天改的行为逐条对照手册，漏一条就红

## 0.1.164

**可以用自己录的配音了**（每一行的「我自己录」按钮，或
`ishkafel script voice-file <任务> --line N <音频>`）。

合成语音的情绪是有天花板的——预置音色、句与句之间没有上下文，原片那个人
激动地在争吵，合成出来往往还是平的。不再往下磨了，直接让人自己念：念成
什么样就是什么样。

装上之后：

- **这一行的时长由这段录音说了算**。这条线的时间根本来就是配音时长，
  所以镜头分配、断句、字幕打轴全部跟着重算
- **录音里说的和脚本不一样时，以录音为准**。人已经念出来了，那就是事实，
  改脚本去将就录音，而不是反过来要求他重录——改了会明确告诉你改成了什么，
  这一行要是有按字划出来的分镜，也会提醒去看一眼还成不成立
- **不会被「生成配音」盖掉**。人工配音有自己的身份，换全片基调也不碰它
  ——不然人辛苦录的下一秒就被合成的顶掉了
- 和合成的**可以混着用**：只重录不满意的那几句
- 音频会**收进任务名下**：人给的那个文件随时可能被移走、改名、删掉，
  而它现在是这一行的时间根
- 听不出内容**不算失败**：时长照用，但会说清「断不了句、字幕只能整句一屏」

## 0.1.163

- **「画面相似」报「上传查询帧失败」——上传其实成功了**。调 miaoa 上传时漏了
  `--json`：不加它的话 stdout 是给人看的一行「✓ 已创建 IMAGE #575」，而
  ossId 跑到 stderr 里去了。于是拿不到 key、报成失败；更糟的是拿不到 key
  就不写缓存，**人每点一次就真的重传一张图**
- **认不出来的素材库错误，现在会把原始报文带出来**。分类不了的本来就是没见过
  的情况，这时候只说「请稍后重试」等于把唯一的线索扔了——而日志只进 stderr，
  GUI 启动的 app 根本看不到（这次排查就是另写脚本在终端重跑一遍才看到真正的
  报文）

## 0.1.162

两件都是模型层的修正。

**一、参考镜不再被台词边界切碎**

真机上 45 个参考镜里 19 个短于 1 秒，最短的只有 **3 毫秒**——每句台词的开头
都挂着上一个画面的尾巴。因为参考镜一直是「台词区间 ∩ 视觉切点」算出来的，
而视觉镜头的真实边界跟台词边界毫无关系。人看到的是：播放参考镜很短、
拿它的首帧去搜「画面相似」搜出来不是同类、它的画面描述根本不是那一段的画面
——三个现象一个根因。

现在两层各自成立：视觉镜头由画面切点决定完整边界，台词只跟它**关联**、
不**切割**。台词区间（决定成片时间）一个字没动。

**没有台词的画面段落成「画面行」**——参考片开头的吸睛段、中间空镜、结尾的
产品定格，此前统统落在台词区间之外，等于不存在，复刻出来的片子会直接少掉
这些段。

老档读出来退回原来的算法，不丢数据；但已提取的任务不重新提取的话，
参考镜还是碎的。

**二、配音带上「这一句原片是怎么念的」**

原片那个人激动地在争吵，复刻出来情绪却非常扁平。查下来：合成语音的接口一直
有「语气指令」这个参数（用一句自然语言说该怎么念），而**脚本成片这条线一处
都没传**——每句都是默认语气，扁平是必然的。而能力早就有：替换裂变的换音色
一直在用它分析原片的念法。

现在配音前先听一遍参考片这一句，把念法带给合成：

- 「早就跟你们说了，我长痘就是全家衣服混洗有细菌…」→ *带点嗔怪的急切吐槽
  语气，稍快语速*
- 「一口价！」→ *高亢干脆的带货吆喝语气，短促有力*
- 「妈妈我不痒了。」→ *软萌幼童的嗓音，带着轻松喜悦感*（原片这句是小孩说的）

**按内容指纹缓存**：同一句重配不再重新分析（重跑一句 1.6 秒，首次要十几秒），
台词改了指纹跟着变、不会放出上一版的念法。分析失败**不静默**——退回默认语气，
但会点名是哪几句。手写脚本没有参考片，照旧不带指令、不花钱。

## 0.1.161

修「画面相似」的两个表现问题（功能本来是生效的，只是界面没跟上）：

- **点了它，模式却被改回「按画面描述」**。整张参考镜卡本身就是可点的
  （点它 = 用这一镜的画面描述搜），而「画面相似」这个按钮嵌在卡里面——
  点它会顺带触发卡片的点击，模式随即被覆盖。于是搜是按图搜的，
  界面显示的却是另一个模式。现在这一下点击被拦在按钮上，不再冒泡
- **已经在按图搜了，底下还提示「去点画面相似」**。那句提示原来是无条件
  弹的：不管有没有查询帧，点这个 tab 都提示一遍。现在有查询帧就直接切回
  这个模式重搜——人明明刚点过，还提示他去点，是把他当没做过

## 0.1.160

- **参考镜的首帧现在能直接拿去搜了**（参考镜卡上的「画面相似」）。
  妙啊的三种向量检索里，以图搜视频是复刻最该用的那一种——要复刻的画面
  就在手上，直接拿它找同类，比让 AI 先把画面写成一句话、再拿那句话去匹配
  别人写的另一句话准得多。中间那层文字转译很不稳：同一个镜头两次写出来的
  措辞不一样，搜出来的东西就跟着变。
  而这条路此前是断的：以图搜视频只吃 OSS key，参考镜的首帧却只在本地；
  入口也只长在候选卡上——手里攥着要复刻的那一帧，却得先用文字搜一轮、
  从结果里挑一条差不多的，再拿它去找相似。
  现在本地帧会先传成查询帧（落在专门放它们的文件夹里，**按内容指纹只传
  一次**），再去搜。传不上去就直说，**绝不悄悄退回文字搜**——人点的是
  「画面相似」，给他一批文字搜的结果，他不会知道自己看的根本不是相似画面
- **两处统一叫「画面相似」**：参考镜卡上一个、候选卡上一个，同一件事
  不该叫两个名字

## 0.1.159

这一版全是同一件事的不同侧面：**软件提供能力，Agent 调用能力并自己判断**
——软件不替它做主，也不背着它自己干。

- **Agent 在场时，界面不再自作主张「自动铺一版」**。那个功能是给人自己用
  的：一次确认，软件把空位全填满，人只做否决和替换。而 Agent 干活时它插
  一脚，人看到的就是「Codex 明明停了，界面还在一句句地铺」，按「我来接手」
  也停不下来——因为那压根不是 Agent 在做
- **自动铺片现在能叫停**。它一次要跑几十句配音加几十次识图、十几分钟起步，
  此前跑起来没有任何停下来的办法，人只能看着它把钱烧完。
  按「我来接手」时，界面自己在跑的那摊也一起停
- **人按过「我来接手」之后，界面不再自动让位**。让位机制是为「人在旁边看
  着它干活」做的；人一按那个按钮，意思正相反。不记这一笔的话，Agent 下一
  条命令一来这一页又乖乖把锁让出去——**接手按钮等于白按**
- **检索的标签归 Agent 调了**（`script shots --tags "痛点,产品展示"`）。
  人在找镜头面板上做的第一件事往往是把不合适的标签去掉、把想要的加上——
  标签是所有检索维度的公共筛选层。此前 Agent 只能被动接受参考镜打出来的
  那几个，加不了也减不了。`tag-groups` 也把每组的词表给出来了：
  不知道有哪些标签可选，就无从「加上想要的」
- **结果一格格长出来，不再是全做完忽然刷一下**。界面靠读盘跟上进度，而
  命令行此前攒到整批才写一次：打标改成每打完一镜就落盘，挑镜头改成每落
  一行就写一次盘。人反复说的就是这件事——「11 个分镜，找一个就该多出一个」
- **提取台词时就把音色定下来**。这条线的时间根是配音时长：没有配音就没有
  rootMs，挑镜头时拿到的坑位时长是 **0**——这一镜该多少秒、素材够不够铺、
  要不要变速，全都无从判断。真机上看到的是「本片 · 未定音色」而 Agent 已经
  在挑画面了。现在提取完就落一个默认音色并说清怎么换（趁还没配音）
- **没配音就不许挑画面**，并把道理讲清楚：挑了也是瞎挑
- **写台词能输中文了**。此前每敲一个键就写一次文档、重建整个编导台，
  而中文输入法在敲拼音的阶段同样会触发——组合状态被打断，字根本上不了屏。
  改成停手（或离开这一行）才落盘，跟旁边那个字幕输入框一个规矩

## 0.1.158

- **素材下载改成一次六条**（原来只有两条）。它们之间没有任何依赖——
  几兆的小片子，一条脚本几十条，两条两条地下要等很久：人刚铺完片想点开
  看一眼，得到的是「素材还没下载好」
- **那句提示也说清还差几条**：只说「还没下载好」，人不知道是差一条还是
  差四十条，也不知道该等三秒还是三分钟

## 0.1.157

**「它在忙，可界面什么都没发生」——找镜头那一段的两半都补上了。**

`shots` 只是检索，`apply shots` 才把画面真正放进去。Agent 把 25 行全搜完
再批量提交，于是那一整段时间里播报热火朝天（「找到 946 条、20/20 条能用、
正在挑」），而每个坑位从头到尾都是「+ 找镜头」——和什么都没干一模一样。

- **那一格自己转起来了**：正在给这一行找镜头时，占位卡变成蓝框 + 转圈 +
  「正在找…」。检索要花一会儿才有东西填进来，这段时间里一动不动的加号
  和「没在干活」没有区别
- **手册改成：可视模式下挑完一行就提交一行**，别攒到最后一起灌。
  人就能看着画面一格格填上去——慢一点，但那正是他要看的东西。
  人不在场时随意，批量更快

## 0.1.156

- **新命令 `ishkafel status`：接手一条被打断的活儿，先看它。**

  活儿被打断是常态——人按了停、命令超时、app 关掉了、隔了一天回来接着做。
  重新接手的 Agent 手里什么上下文都没有，只能一条条 `show` 去翻，然后
  照着流程从头再走一遍：重新打标（十几分钟、几十次识图）、重新配音
  （一轮 TTS）——**全是白花的钱**。

  它报三样：**每一步做完没有、做了多少**（照流程排）；**下一步照着敲的
  那条命令**（带真实 id 和行号）；**现场情况**——这条任务此刻被谁占着、
  它正在干什么、最后一次改动是什么时候。

  最后那个时间是给「人动过手」用的：人在打断期间多半自己挑过镜头、改过
  断句、换过音色，拿它和自己上次操作的时间比一比，对不上就先看清人改了
  什么，别一上来就覆盖。

  ```
  #1 滴露植源喷雾_参考视频复刻（脚本成片）
    参考镜打标还差 7 镜
    ✓ 台词 25 句  · 参考镜打标 40/47 镜  ✓ 本片音色 vivi 2.0  ✓ 配音 25/25 句
    · 挑镜头 1/25 行  · 配乐 没铺  · 导出 还没导过
    下一步：ishkafel script tag-ref scmth03lp1 --line 22
  ```

  手册开篇就写着「不管刚开始还是接着上次干，第一条命令都是它」

## 0.1.155

- **手册直说清楚哪几条命令要跑好几分钟，以及超时了该怎么办**。
  Codex 这类 Agent 的单条命令有超时，而 `tag-ref` 整片要十几分钟、
  `voice` 三五分钟——它超时后以为失败就重跑，第二个撞上第一个的锁，
  于是**在等它自己**，来回好几轮。手册现在给了时长表，并说明：
  超时不等于失败，先 `script show` 查一眼干到哪了；这几条都是接着上次干的，
  重跑不会重头来也不会重复花钱，但**在它还跑着的时候重跑纯粹是白等**

## 0.1.154

- **修上一版闯的祸：每条命令一开始就卡住。** 0.1.153 把「撞到锁」改成
  「等它干完」，但漏了一种情形——**界面留下的锁不会自己放**。界面开过某个
  任务、后来退回了列表，那把锁还在；这时让位请求没人接（接单的是那个任务
  的工作页），于是落进「等它干完」的循环里干等二十分钟。
  比原来一撞就退还糟。现在：没人接让位请求 = 没人在看这个任务 = **直接接管**
- 等另一个 Agent 进程时**每半分钟再说一声**，并带上它正在干什么。
  等十几分钟一声不吭，和卡死没有区别

## 0.1.153

**锁不该把活儿挡在门外。**

- **另一个 Agent 进程占着锁时，等它干完，不再一撞就退**。真机上撞的是这个：
  `script voice` 要跑几分钟 25 句 TTS，调用方的命令超时了，它以为失败就
  再起一个；第二个撞上第一个的锁，于是报告「我会等待锁释放后自动续跑」
  ——**它在等它自己**，来回好几轮。而配音其实早就跑完了。
  现在会等（默认二十分钟），而且**等的时候出声**：「在等它干完，不是卡住了；
  如果那是你自己刚起的进程，别再起一个」。真等超时了，话也换成
  「如果那是你自己起的进程，先看看活儿是不是已经干完了」
- **配音开工先说清这一轮要配几句、已经好了几句**。不说的话「续配」看起来
  和「重来」一模一样：第一次被锁挡住只配了 17 句，后面几次在补剩下的 8 句，
  而人在旁边看到的是「它又在配音了」，以为白烧了一轮钱

## 0.1.152

- **「铺一段配乐」会把全片音色、语速、三条声轨的音量总控全部抹掉**。
  换配乐段时构造新文档只带了字幕和参考片，漏了另外三个字段。人看到的是：
  编导台顶上写着「本片 · 未定音色」、每一行都是「选择音色」，可每行又都有
  精确到 0.1 秒的时长、而且正在正常导出——**时长来自已经生成好的音频，
  音色标识却没了**。声轨音量被打回默认更隐蔽：成片的声音配比变了，
  而没有任何地方会提示。
- **已经被它弄坏的任务会自己缓过来**：读档时如果基调是空的、而配音清一色
  同一个音色，就把它认回来。认回来的值和音频一致，所以不会反过来触发重配
  （重设一次基调要白烧 25 句 TTS）。音色本来就混着的不猜——那说明是逐行设的
- 加了一组测试：每个「改一样东西」的方法都要过一遍，只准动它该动的那样

## 0.1.151

- **导出这一步在界面上只有一行滚动的字**。它是全流程最长的一步（几分钟）、
  直接出交付物，最该让人看着——而此前它只往在场状态写了一句话，
  没有模块、没有焦点，于是界面根本不进那个任务：人盯着任务列表，
  看着「正在导出：渲染第 10 行第 1 镜」滚过去，画面纹丝不动。
  现在导出会把界面带进编导台，并**跟着正在渲染的那一行走**

## 0.1.150

三条会让整轮白跑的洞：

- **命令行导出根本接不了配乐**。不是「下载失败」——是这条路压根没给导出
  传配乐文件，于是只要方案里铺了曲子，导出必然报「配乐在本地找不到，
  到配乐色带上重试下载」，而那正是手册说不该必要的手动操作，命令行也
  确实没有任何命令能拉它。验收 Agent 全程走到最后一步卡死在这儿。
  现在导出前自己把曲子下下来，并且**边下边说**（「正在下载配乐『xx』
  （1/2）」）——一首两三兆，网络慢时是实打实的等待
- **报成功、实际没存**。第 20、24 行拿到 `{"ok":true,"applied":1}`，
  盘上却是 0 个镜头；界面同时诚实地写着「还没挑镜头」。`applied` 一直是
  从**提交内容**算的，跟落盘结果没关系——就算修好今天这个吞镜头的分支，
  明天换个原因照样静默落空。现在落盘后回读一遍，空了就报失败并给出补救
- **导出失败在界面上完全不可见**。此前只写到命令行，而在场状态随即被
  清掉——人盯着屏幕看到的是「转了一会儿，然后什么都没发生」。
  现在失败会以警告的样式播报出来，并且停留几秒

## 0.1.149

围着一件事修：**等待的时候，人得知道它在干什么、走到哪了、有没有卡死。**

- **打标那十四分钟不再是黑屏**。参考镜打标是全流程最难熬的一段——25 句、
  47 个参考镜、每镜一次付费识图。此前它一声不吭：界面停在任务列表、
  任务缩略图还是黑的，一刻钟里没有任何东西动过，而钱一直在烧。
  现在每打一镜说一句，**带整片的分母**：「正在看第 12 句的第 2 个参考镜
  （全片 23/47 镜）」，界面同时跟到那一行。
  分母按整片算而不是按这一行——只报「这一行第 2/3 镜」的话，
  人还是不知道整件事走到哪了；要的是一个一路涨到头的数字
- **画面不再动不动弹回第一行**。Agent 每跑一条命令都是一个新进程、都会
  唤醒界面一次，而界面收到唤醒就把编导台**销毁重建**，滚动位置全丢。
  手册教的又正是「一次做一件事」，于是二十几条命令跳二十几次。
  现在已经在这一页就不重建了（审核页早就这么防，这两个模块漏了）
- **「在下载」和「坏了」现在分得开**。此前下载中只有角落里一个 9 像素的
  小转圈，人看到的就是一块黑图加一个红按钮——和出错长得一模一样。
  现在下载中是整块的半透明遮罩加转圈，鼠标停上去写着「正在下载这条素材…」；
  真失败了也说清「没下下来，点一下重试」

## 0.1.148

手册四处说不清的地方，都是验收 Agent 照着走会被带偏的：

- **候选字段给了真名**：实际输出里是 `materialId` / `sceneDescription`，
  手册写的是 `id` / `description`。照手册写 jq 拿到一整列 null，
  第一反应是「检索坏了」，而不是「手册写错了」
- **一屏少于 4 个字的例外写清楚了**：整行本来就只有两三个字（「活的。」
  「看看啊！」）时它就一屏，不受这条限制。此前手册没说，Agent 看到软件
  自己产出 2 字屏，以为它在双标
- **说明白自动断句会把词劈开**：中文没空格、时间戳是字级的，没标点可依
  时只能挑「停顿最大」处切——真机上「……还在用清水擦电 / 饭煲」就是
  这么来的。导出前得过一遍
- **说明白不给 alloc 就是平均分**：两镜行各一半，切换点正好落在句子中间，
  念到一半画面跳了。配音的字级时间戳就在手边，按语气停顿挪一下即可

## 0.1.147

- **接着上一版真机一看：让位之后界面变成一块黑板**——渲染的是「等它结束
  再进」那张空白拦截页，人还是什么都看不见。让位现在只放锁、不拦页面，
  只读跟随照旧；Agent 收工时把锁拿回来（不然人接着改，改到保存那一下
  才发现写不进去）

## 0.1.146

接着上一版按同一轮验收往下修。这几条都是「Agent 走完了全程，但过程对人
不可见、或者差点交付废片」：

- **最慢最贵的那几步，人终于看得见了**。此前解锁的唯一办法是让界面
  **退出**那个任务，而一退出就什么都看不见——于是识别台词、逐镜打标、
  逐句配音（几分钟、几十次识图加几十句 TTS）全程对着一块不动的板子。
  现在界面把写锁**让出来但留在页面上**，转成只读跟随：它改哪一行界面就
  滚到哪一行、数据当场刷出来。人要抢回去点横幅上的「我来接手」。
  （编导台此前接不了任何委派，这是它的第一条。）
- **配乐在命令行上根本用不了**：提交那份候选清单，手册里叫 `offered`，
  代码只认 `candidates`——配乐那节又恰好只提 `offered`，于是照手册写必错，
  报的还是「配乐 108 不在候选里」（而 108 就是它刚给的）。验收 Agent
  两种写法都试了都拒，最后放弃——**交付的成片全片没有配乐**。
  现在两个名字都认；「一条候选都没给」和「这个 id 不在清单里」也分开说了
- **画面没看成的素材现在会当场点名，并给出补救的命令**。刚挑好的素材
  多半还没下载，于是首轮提交时画面自查整批跳过、`framesSeen` 全是 null。
  手册反复讲「null 是没看成、不是没问题」，却从没说过怎么让它看得成——
  验收 Agent 只能自己摸出「先 peek 拉到本地、再原样重提一遍」。
  而**正是那一轮才查出有条素材底部烧着别的片子的台词**，位置恰好是我们
  要烧字幕的地方：照第一轮直接导出，交付的就是两层字打架的废片

## 0.1.145

按一轮完整的「参考片复刻」验收修。这一轮 Agent 走到了终点（成片导出），
但可视模式基本是废的：

- **Agent 一过第 4 行，界面就再也不动了**。右栏一屏放得下三四行卡片，
  而 `ListView` 是懒构建的——焦点落在第 11 行时那一行的 widget 压根还没
  创建，「滚到眼前」等不到任何回调。人看到的是：播报条一直说着「正在给
  第 20 句挑镜头」，画面钉在第 1 行纹丝不动，**可视模式退化成一条日志**。
  现在焦点变化时先粗滚过去把那一行带进构建范围，它建出来再精确对齐
- **正在被操作的那一行现在看得出来了**：更粗的蓝边 + 柔和外发光，
  和「人自己选中」区分开。滚过来的同时人眼就被带过去，不用去读播报条上的字
- **建完任务之后，所有可视动作都被静默挡掉**。建脚本成片任务那条路会
  等**人退出编导台**才返回，而分发用的互斥标志压在那个 Future 上，于是
  永远不放——验收 Agent 建完任务紧接着跑 `ui tasks`（那正是建任务回执里
  写的下一步），连着两次白等 90 秒，而界面明明就在眼前开着。
  它的第一反应是「我的环境坏了」。现在回执一发出就放行下一条

## 0.1.144

- **可视模式下会彻底卡死：建完任务就没路可走了**。`ui new-task` 建完，
  界面停在那条任务上占着写锁，而下一步（脚本成片的 `script extract`、
  替换裂变的 `analyze`）非写这条任务不可——「建完立刻干活」这条最自然的
  路走不通。以前的绕法是「打开另一条任务」把界面支开，那条路有个隐含
  前提：**恰好还有第二条任务**。一条任务都没有的时候（新用户第一次用、
  或者刚把旧任务清掉）就是死局。
  现在有 `ishkafel ui tasks`：界面退回任务列表、锁跟着松开。
  建完任务的回执里也直接写明「界面正占着锁，出路是这条」，
  不用 Agent 自己猜
## 0.1.143

按验收 Agent 那轮「参考片复刻」走不下去的实录修。它拿一条真片子做参考片
复刻，卡在打标那步再没往前走过，中间白烧了两轮 TTS、白删了两个任务。

- **参考片复刻在命令行上从第一步就断**。`script extract` 整片提取时，
  行级的参考视频字段按设计就是空的（它只给「单独给某一行传参考」用），
  该回落到文档级。界面一直是这么读的，命令行三处（`tag-ref` /
  `frames` / `shots`）都没回落——于是凡是提取出来的脚本，这三条命令
  一律说「手写的脚本没有参考镜」
- **那句报错还把人往错的方向引**。「没有参考片」和「参考片读不到」是
  两回事：后者在真机上更常见（原片在「文稿」目录下，命令行工具的访问
  授权还没给）。现在分开说，并给出真正该走的那两条路
- **手册里 `script from-video` 这条命令根本不存在**，真名是 `extract`。
  Agent 照着敲拿到「未知命令」，合理地推断成环境坏了，于是重装、删任务、
  重建任务，整轮白干
- **配音不认全片基调，白烧两轮**。人定了音色，`script voice` 看都不看，
  拿音色目录里第一个配完 25 句；发现不对再逐行写一遍、再配一轮。
  现在认基调；三级都没定就**拒绝开工**——音色不对整片得重配、每句都
  花钱，不该撞运气（界面早就这么卡了）
- **换了全片音色，界面上那个状态点还是绿的**。人以为都好了，导出才发现
  前后音色不一样。命令行那边同样看不见基调变化，改完基调触发不了重配

## 0.1.142

- **建任务的过程中人要是退出了列表页，会撞上一个崩溃**：建这一段是异步的，
  跑完时页面可能已经不在树上，代码还拿着它的 context 往下走。现在先看
  页面还在不在——**任务是真建成的，回执和撤场照发**，只是没法再替人
  进去；漏掉回执的话 Agent 会一直等到超时，最后报一个假的失败
- 静态检查全清零（`flutter analyze` 从 30 条 info 到 **No issues found**）。
  主要是把 `if (x case final v?) 'k': v` 这类写法换成语言自带的
  `'k': ?x`——同一件事，少一层拐弯

## 0.1.141

- **界面上的自我介绍还停在只有替换裂变的时代**，脚本成片整条线在包里
  是隐形的。手册早就把两条线摆成平级了，界面没跟上——同事拿到包，
  打开设置读到的是「导入一条成片 → 换画面」，于是「从台词直接造一条
  新片」这件事他根本不知道软件能做。三处都改了：
  - 关于 →「这个工具做什么」：先说清机制（台词决定时间，画面填进去），
    再把两条线并排摆出来
  - Agent 说明书那张卡：不再说「做替换裂变的手册」，并补上脚本成片
    那句能直接对 Agent 说的话（「照这份稿子做一条新片」）
  - 命令行工具卡里「操作手册在 docs/AGENT_SKILL.md」——**那是源码路径**，
    拿到 zip 的人机器上没有 docs/ 目录。改成指向下面那张卡片
- 新增架构测试守住这件事：界面介绍漏掉任何一条线都会红

## 0.1.140

- 代码清理，功能与 0.1.139 一致。把换音色装配搬走后留下的十来个无用
  import、以及 `holder ?? agentLockHolder` 这类参数改成非空后残留的
  死代码清掉了——留着会让人以为这里还接着东西。
  **这一版的意义是让发出去的包和仓库里的代码对得上**：0.1.124～0.1.139
  这十六版此前一直没入库，包在 git 里找不到对应提交，出了问题没法对照、
  也没法回退

## 0.1.139

- Agent 说明书补上**编导嘴里的说法**：人说「用可视化模式」「我看着」
  「你做给我看」都是要开可视——他不会说 `--visual`，那是 Agent 自己的事。
  同时写明：对着这份手册干活的人多半是编导或剪辑，不会告诉 Agent 敲哪条
  命令、设哪个环境变量，**那些是 Agent 该自己解决的**

## 0.1.138

- **命令行工具也签上名了**。它和 app 同名（都叫 `ishkafel`），所以它读
  文稿目录下的原片时，系统弹的是一模一样的「ishkafel 想访问文稿文件夹」
  ——人根本分不清这次是谁在问。上一版只签了 app，这半还在天天弹。
  （`--deep` 只管 Frameworks 和嵌套 bundle，Resources 下的独立可执行文件
  它不签，所以打包时要单独签一次）

## 0.1.137

- **整体替换的那几段，成片里没有台词字幕**——这件事此前谁都看不见。
  镜头替换会把台词字幕重渲上去（原片字幕烧在被换掉的画面里），整体替换
  原样接上、和原坑位对不齐，字幕烧不上去。一条片子两种模式混着用，
  字幕就是断断续续的。现在导出确认页、`export` / `jianying` 的输出、
  `task --json` 都会说清是哪几个单元。
  **它还决定了「素材自带烧录字」有多严重**：会烧字幕的段落是两层字打架、
  片子废；不烧的只是片子里放着别人的文案
- **界面开着这条任务时，`export` 不再直接拒绝**。这是提交方案那个死结在
  导出这条路上的原样重现：可视模式要求界面停在这个任务上，而导出要求它
  不能停在这个任务上——人最想看着的一步，恰恰因为「人在看」而做不了。
  现在改成委派：**界面把导出对话框打开、参数填好，最后那一下由人点**。
  不替人点到底是有意的——导出跑几分钟、直接出交付物、还花钱

## 0.1.136

- **app 现在用固定的自签名证书签名**，隐私授权（「ishkafel 想访问文稿文件夹」）
  不再每次重新编译都重问一遍。此前是 adhoc 签名——没有稳定身份，macOS 只能
  按二进制哈希认它，编译一次哈希变一次，系统就当成另一个 app。
  签名后的身份要求只认 bundle id + 证书指纹，不含二进制哈希。
  （证书是本地自签的，不进 git、不联网、不涉及任何账号；没有它就退回 adhoc，
  不影响构建。**新身份第一次运行仍会问一次，点「允许」之后就不再问**）
  打包时往 app 里塞命令行工具会破坏封签，那一步的重签也改用同一张证书——
  此前它退回 adhoc，等于把前面签的白签了
- **导出前把这批素材的问题说清楚**（`export` 和 `jianying` 两条路）。
  此前一声不吭：一条明知烧着 5 条字的素材，`apply` 刚报过警告，
  紧接着 `export` 零提示、退出码 0、127M 成片落盘，烧录字原样进了片子。
  **不拦**——要不要继续是调用方的判断；但绝不能不说
- **字幕样式盖不住烧字时一并说出来**。软件手里同时握着三条信息：这条素材
  烧着字、当前预设是 whiteOutline、它自己写着「whiteOutline 盖不住烧字」
  ——三条凑齐就是「这条会打架」，而它一句都没说。现在会说，并给出路
  （换 whiteBox / blurBox），也说清底条不一定盖得全
- `task --json` 和 `script show --json` 补上画面自查的汇总
  （查了几条、没查几条）。**「没查过」和「这个版本没这功能」长得一模一样**
  ——都是键不存在，拿到输出的人无从判断该不该信任它「画面干净」

## 0.1.133

按验收报的 7 条洞修：

- **委派提交方案的返回，和直写给的一样全**。此前走委派时（界面开着这条任务）
  只回 `{ok, via, message, plans, units}`——检查跑了、界面上那条橙色警告也
  报了，**只有 Agent 拿不到**。而委派正是人在旁边看着时走的那条路，
  人扭头问「它刚才说啥了」，Agent 答不上来。两条路现在共用同一份报告
- **`materials` 里多了 `frameChecked` / `frameUnchecked`**：只看
  `burnedText` 在不在，分不清「都干净」和「一条都没看成」——两种情况这个键
  都不出现，而一个是「没问题」、一个是「不知道有没有问题」
- **手册两处照着跑会撞墙的错**：`apply plan`（实际是 `plans`）；
  以及 `jq '.burnedText // "画面都干净"'`——`//` 正是「null 就用右边那个」，
  **手册用一整段警告不要犯的错，就写在它自己给的命令里**
- **`review list` 说清每条是什么**：此前只给 `unit/shot/material` 三个数字。
  人在审片台上看得见画面、能一眼看出「这条烧着字」，Agent 拿到的是个数字——
  人问「第 3 条是什么」它答不上来。现在带上名字、台词、画面描述、时长、
  烧字、品牌
- **`candidates` 带上已经知道的画面自查结果**：此前只能先提交、等 `apply`
  报错再回去重挑（「人挑一次，我挑两次」）。**不为检索现查**——一页 50 条
  各跑一次视觉调用太贵，只报缓存里已有的
- **画面自查结果跨任务复用**：素材库里的素材会被反复用到，一条看一次就够，
  不是每个任务看一次。缓存有配额，超了删最久没用过的
- **脚本成片那条线补上画面自查**（此前整条缺席）。这条线**更需要**：
  它自己要给每句台词烧一行字幕，素材再自带一层就是两层字叠在一起；
  而且没有原片当参照，品牌错位更难发现。素材落地后自动查（和实测时长
  同一个时机），`script show --json` 报出结果，手册补上对应那一节

## 0.1.132

- **播报条分三类，人一眼分得开**。此前进度、判断、发现问题挤在同一种样式里，
  人只会当成一串流水账划过去——而「这条素材烧着别家的字」恰恰是他该当场
  喊停的一条。
  - 进度（▸ 灰白）：「正在给 U2S4 挑素材」——它走到哪儿了
  - 判断（💡 蓝）：「标签命中 5318 条太宽，改用画面描述再搜一轮」
    ——**它为什么改主意了**。这一类最值钱：人肯把花钱的活交给静默模式，
    靠的正是「我看懂过它是怎么想的」
  - 发现问题（⚠ 橙、加粗）：人可能要当场喊停。**不会被后面的流水账挤掉**，
    也不跟着变淡——人走开一会儿回来，要找的就是它
- Agent 委派界面提交方案时，发现烧字或品牌错位会**当场播报**，
  不用人自己去托盘上一条条翻
- 修：**委派提交方案那条路根本不收素材**。取段用不到素材时长（20 秒素材塞进
  0.5 秒坑位会整条压缩成快进）、画面自查一次不跑——于是出现最难发现的组合：
  **人在旁边看着的时候，检查反而不做**，而那正是他最信任的一次。
  两条路现在共用同一个收素材入口，有架构测试盯着不许再分叉
- 检索时「改主意」也会播报出来（标签筛不出东西、命中太宽改用画面描述），
  归在判断那一类

## 0.1.130

- 挑素材时**顺带看清画面里露的是谁家产品**。产品露出的镜头不能跨品牌换：
  台词说「滴露新款消毒液」而画面是若也洗发水直播间（背景还有 logo 墙），
  片子自己打自己的脸——2026-08-28 真机上就这样交付过一条成片。
  而 `--exclude-projects` 反而放大这个风险：排掉原片项目之后，语义检索会把
  最像的别家品牌素材顶上来，那个参数只懂项目 id、不懂品牌。
  - 判据不需要知道「本片是什么品牌」：**一条片子里出现两个牌子，本身就是错的**
  - 界面：品牌打架时，涉事的每条素材在托盘上标红，说清露的是哪个牌子。
    **按整条片子算，不是按当前这一屏**——人是一个单元一个单元挑下来的，
    每一屏里都只有一个牌子，错位只在合起来看时才现形
  - 导出确认页：点名是哪几个牌子、各自来自哪几条素材
  - 命令行：`apply plan` 输出多一个 `brandConflict`，`task --json` 的
    已选素材里带上 `productBrand`
  - **和看烧字是同一次调用**：多问一个问题几乎不加钱，多跑一次调用是成倍的
- 原片分析时也记下每一镜露出的产品品牌，作为「候选对不对得上本片」的参照
- 品牌判据补上第二条：**候选全是别家的**（彼此不打架、但整条跑到别家去了）。
  参照是原片自己的产品露出镜头——分析打标时顺手记下每一镜露的是谁家产品，
  不额外花钱。任务名和项目名靠不住（会改名、会合并项目），描述里也从来不写品牌
- `ishkafel task <任务> --json` 补上四个「存进去了却没报出来」的字段：
  `framesSeen`、镜头的 `productBrand`、已选素材的 `voiceover` 和
  `sceneDescription`。这个坑三天里栽了三次，后果一致而且隐蔽——Agent 照手册
  那句 jq 查永远拿到空，**而空看起来正好像「没问题」**。
  现在有一道按模型 `toJson()` 逐字段比对的测试兜着，以后加字段也跑不掉
- 画面自查改成**看头中尾三帧**（送进同一次调用，不是跑三次）。
  只看一帧会漏：真机上素材 114801 首帧是一排 Dettol 滴露瓶、尾帧右上角也有
  一瓶，唯独中段那一帧是微波炉内部——只看中段就把它记成了「无产品露出」。
  界面挑素材那一刻素材本体还没下、只有一张首帧图，所以记下「看了几帧」，
  提交方案时素材已在本地，会拿三帧再看一次顶掉单帧的结论

## 0.1.126

- 挑素材时会看一眼**素材画面上本来就烧着字**没有（字幕、贴片文案、品牌角标）。
  这类素材换上去之后我们还要再烧一行台词字幕，两层字叠在一起、内容还毫不
  相干，片子直接废——而素材库给的画面描述里一个字都看不出来（真机撞到过：
  描述写「成人给小孩按摩额头」，画面底部烧着别家品牌的「冰冰凉凉的好舒服呀」）。
  - 界面：托盘胶囊上出现橙色警告角标，鼠标停上去说清烧的是什么字
  - 导出确认页：点名是哪几个位置的哪几条，不是笼统一句「有素材有问题」
  - 命令行：`apply plan` 的输出里多一个 `burnedText` 字段，同样点名
  - **「没查过」和「画面干净」严格分开**：`burnedText` 为 null 是没看成
    （素材还没落到本地、或没配方舟凭据），空数组才是看过且干净。
    绝不把没看成当成放行
  - `ishkafel task <任务> --json` 的已选素材里也带上这个字段
    （原本没投影出来，照手册那句 jq 查永远是空——空结果看着正好像「都干净」）

# 更新记录

面向使用者写：说清**这一版能多干什么、少踩什么坑**，不复制提交信息。
版本号与「设置 → 关于」里显示的一致——同事发截图过来时，那是唯一能对齐的锚点。

---

## 0.1.124

- 修：Agent 说「正在给 U2S3 挑素材」，右栏却写着「这个单元保留原片」——
  两句话打架。现在跟随时会像人一样先切到「镜头替换」

## 0.1.123

- 重新分析同一条片子时，进度会明说「这条片子分析过了，直接用上次的切分」
- 修：删任务的用法提示里写的命令跟真实命令名对不上，照着敲会说「未知命令」

- **同一条片子重新导入，切分结果不再变来变去**。以前同一个文件导三次会切出
  3 / 4 / 5 个单元（切分是 AI 干的、有随机性），于是之前挑好的素材方案
  在新任务里全对不上号，等于白挑。现在按文件内容记住切分，重导直接复用，
  顺带省掉一次分析的钱
- 页面开着时 Agent 提交方案，过程会一步步说出来了（以前只有结果悄悄变了）
- `ishkafel task` 报出这条任务属于哪条线

## 0.1.120

- **修：新建任务可能建错东西还报成功**。macOS 弹出「想访问文稿文件夹」的
  授权框而没人点允许时，任务其实没建成，命令行却把**上一次**建的任务报了出来。
  现在界面会把真正建出来的那条报回去，建不成就明说
- 页面开着时提交方案不用再把界面挪开了——**改成请界面代为提交**，
  人能眼看着方案落到时间线上。以前「要看就不能提交」是个死结
- 新建任务后给的下一步命令跟着任务类型走（以前恒定给脚本成片那条，
  替换裂变任务照着跑会被拒）
- 播报条会说清在哪条任务上干活（两个会话同时跑时人分得清）

## 0.1.119

- 挑素材时直接给出「原片这一镜长什么样」的图。以前只给一句文字描述，
  要复刻的那一帧得自己另外抽

## 0.1.118

- 命令行也能清理残留产物了（`ishkafel clean`）。以前只有设置页能点，
  而 Agent 跑久了攒下的几个 G 全是它自己造的

## 0.1.117

- 替换裂变的配乐，命令行也能铺了（`ishkafel bgm`）：铺一段、改音量、删掉。
  以前只有界面能做，而配乐是进成片的东西

## 0.1.116

- 几处提示只告诉人「点某个按钮」，而用命令行的人没有按钮可点——
  现在两条路都给出来

## 0.1.115

- 替换裂变的换音色，命令行也能做了（`ishkafel voice`）。以前只有界面能换，
  而导出会拦下「选了音色却没生成配音」——等于 Agent 被那道拦截堵死

## 0.1.114

- 挑素材时查时长快了近 4 倍（一页 50 条从 72 秒降到 19 秒）：以前一条条
  串着量，现在并行

## 0.1.113

- 挑素材时会告诉你「选它之后画面怎么放」：多数是 1.0 倍（软件从素材里
  截一段用），以及这一段可以在素材的哪个区间里挪

## 0.1.112

- 修：命令跑到一半被中断后，它占的锁要等一分钟才失效。锁上没记进程号，
  「写锁的进程还在不在」这条快捷判断对命令行完全无效——现在记上了

## 0.1.111

- 快了 15 倍：提交方案时量素材时长，以前每条都去联网量（102 条要一百多秒），
  现在素材已经下到本地的直接读本地，同样一批 7 秒跑完
- 顺带更稳：网断或地址过期时不再连时长一起丢——丢了那几镜会退回快进

## 0.1.110

- 修：软件里预览短镜头还是快进的，而导出来的已经不是了——预览没跟上
  「从素材里截一段」的改动。现在预览、导出、剪映三处口径一致

## 0.1.109

- 修：导出的 mp4 已经不快进了，拖进剪映的工程却还是三十几倍快放——
  剪映那条路没跟上「从素材里截一段」的改动，两边口径不一致

## 0.1.108

- 提交方案时会说清「有几条素材量不出时长」以及后果——那些镜头会退回
  整条压缩、画面明显快放。以前是悄悄降级，人拿到片子才发现有几镜在快进

## 0.1.107

- 修：删掉脚本成片任务时，导出中间产物、预览切片、镜头首帧这些不会被清掉，
  在盘上越积越多（真机上量到近 900M 没人认领的文件）

## 0.1.106

- `ishkafel task` 现在会报已选素材的时长，Agent 能确认「短镜头有没有被截一段」
  这件事真的生效了（以前只能盲猜）

## 0.1.105

- **修：Agent 提交的方案，短镜头照样是快进**。上一版做的「从素材里截一段」
  要靠素材时长才算得出来，而那个数只有在界面上挑素材时才会存下来——
  Agent 走命令行提交的方案根本没有，取段全部失效。现在提交时一并收下来
- 修：Agent 新建任务后，底部播报一直停在「正在新建任务」不消失

## 0.1.104

- 内部清理：去掉改版留下的死代码
- 修：新建任务的模式参数填错时，报错还在教人用已经废弃的写法

## 0.1.103

- 修：Agent 干活的播报还是不显示。三个地方各定各的节奏——播报条要求每步停
  半秒才继续，编导台和工作台等 0.32 秒就放行了，于是播报整步被跳过。
  现在全软件用同一个节奏

## 0.1.102

- 可视化：Agent 干活的过程现在会一步步说出来——在看参考画面、按什么找、
  找到多少条、量完时长在挑。以前只在开头报一句，人看到的是「跳过去→结束」，
  中间几秒全黑
- 界面没开着时不再一步步干等回执（一条命令报五步会白耗二十几秒）

## 0.1.101

- 修：Agent 干活时底部的播报条经常一片空白。它扫在场状态时把回执、
  请求结果这些同目录的旁支文件也当成了任务，撞上一个就把真正在干活的挡住了

## 0.1.100

- 修：脚本成片的「找镜头」带了 `--visual` 也没反应——软件不弹出来、界面也不跟。
  那条命令收了参数却从来没上报过自己在干什么
- 加了一道检查：以后再有命令收了 `--visual` 却不上报，测试会拦住

## 0.1.99

- **可视化模式：界面开始跟着 Agent 走了**。以前它说在挑 U3S2 的素材，
  界面只把播放头挪一下，别的什么都不动；现在会选中那一镜、右栏切到
  「替换素材」——跟人自己点过去时走的是同一条路
- `ishkafel candidates --visual`：挑素材这一步终于看得见了。
  这和提交方案是仅有的两个真正在做决策的地方，以前都是黑箱

## 0.1.98

- 替换裂变现在能改字幕样式了（工作台顶部「字幕」按钮，命令行 `ishkafel subtitle`）。
  以前这条线写死用白字黑描边，人和 Agent 都没得选
- **遇到画面自带烧录字幕的素材有救了**：切成底条或毛玻璃能盖住。
  以前默认样式盖不住，原字幕从描边缝里透出来，成片上两行字打架

## 0.1.97

- **短镜头不再被拉成快进**。以前是把整条素材压缩进坑位——原片的快切镜头
  只有 0.4~1 秒，而素材库里的分镜普遍 4~30 秒，于是这些镜头全是 10~30 倍
  快放（一条 34 镜的片子里将近四成中招）。现在改成**从素材里截一段用**，
  倍速回到 1.0；素材开头常有转场，默认取中段
- 提交方案时可以指定从素材哪儿开始截（`trimStarts`），不指定就自动

## 0.1.96

- 修：命令行的「导剪映」遇到没下载的素材直接放弃，而导出成片是会自己下的。
  现在它也自己把素材备齐再写工程
- Agent 能自己验片了：`peek` 抽帧到本地（看候选、看成片），
  `candidates --probe` 给出「选这条会变速多少」

## 0.1.95

- 剪映工程生成完的对话框补上「打开剪映」（编导台一直有，工作台漏了）
- 生成过程有进度了：一百多个素材要归集到工程目录，以前界面干卡着不说话
- 素材没落到本地时不再让它跑到一半才失败——和导出成片用同一把闸

## 0.1.94

- **替换裂变可以导出到剪映了**（工作台底部「剪映」按钮，命令行 `ishkafel jianying`）。
  一个任务一份工程：同一个位置挑了几条候选就摞几条轨，上面那条盖着下面的——
  想用别的候选，在剪映里把上面那条关掉就行。等于把「挑哪一条」搬进剪映边看边切
- 工程里的轨道：分子轨（原片按台词语义单元切）、原子轨（原片按视觉镜头切）、
  候选轨若干、配乐轨、字幕轨。字幕是文本不是图，能直接改字

## 0.1.93

- 新建任务改成两步：先选**替换裂变**还是**脚本成片**，选完替换裂变才问
  「有原片还是不用原片」。以前四张卡并列摆着，看起来像四种玩法，
  其实本地文件和「不用原片从素材拼」是同一条线的两种起点
- Agent 那边同步：`ui new-task --mode` 的 `renew` 改叫 `replace`（旧名仍然认）

## 0.1.91

- 统一用语：「成片翻新」改叫**替换裂变**。两条线各有两种起点，起点不是模式——
  替换裂变可以从原片开始，也可以不用原片直接从素材拼；脚本成片可以从参考片
  提取台词，也可以直接写。文档、界面文案、Agent 手册与技能描述一起对齐

## 0.1.90

- 修：重启 app 后打开任务，顶上总有一条「这个任务被另一个窗口占着，当前为只读」，
  要么干等一分钟要么强制接管。写锁的那个 app 早就退了——现在进程不在了立刻放行

## 0.1.89

- 修：每次打开任务都闪一句「正在把 N 条已选素材存到本地」。素材其实没重下，
  是每次都把它们重排一遍队列——现在本地已有的直接算就绪，开任务就能用

## 0.1.88

- 候选选多了不再拦着不让导出。以前底部横幅写「无法导出，请先减少 U2 选中的
  候选素材」，而 U2 的因子有四百多万——那等于让人从头再挑一遍。
  现在照选，导出时由软件挑出 100 条
- 这 100 条挑得更有意义了：从候选最多的那个单元开始变，
  以前它夹在中间的话一百条里它一动不动

## 0.1.87

- 修：分析完成的任务仍挂着红色「上次分析被中断，请重新分析」。
  分析成功时没人清掉上一次的错误标记——点「重试」会白花一次分析的钱。
  已有的这种任务下次启动自动清掉

## 0.1.86

- 挑素材面板：标签命中太多时自动改用画面描述检索，并在提示行说明原因
  （以前人在界面上拿到的也是「最新 50 条」而不是「最像的 50 条」）
- 挑素材：可以排除指定项目的素材——翻新常要「换成别的项目拍的」，
  而搜出来最像的永远是原项目自己拍的
- 导出的文件名改用方案名（`A-居家写实线.mp4`），不再是「变体0/1/2」

## 0.1.85

- **修：导出必然失败**。字幕图渲染在并行的段落之间互相覆盖清单，导致
  「某某句没有产出图片」——三条方案一条都出不来。现在同一目录上的渲染排队进行
- 导出全部失败时最后一行不再写「完成」，改成「全部失败，一条都没出来」；
  失败按错误级别记，不再混在 warn 里
- 挑素材：按标签命中太多时自动改用画面描述语义检索并说明原因。
  素材库按标签搜是「最新 50 条」而不是「最像的 50 条」，几万条里取 50
  等于随机取样（真机上同一镜从「户外街道」变成了对得上的「书桌前长痘女孩」）

## 0.1.84

- 修：Agent 用的命令行工具拿不到 AI 凭据——人在界面上能分析，Agent 敲命令却在
  第二步就失败。现在正式包会把凭据一起带上，拿到包直接就能用
- 新增 `ishkafel doctor`：开工前一次问完凭据、素材库登录、ffmpeg，缺什么点名说
  （以前要等干到一半才暴露）

## 0.1.82

- 修：素材库里 id 是负数的分镜取不到详情，会显示「落地失败」
- Agent 用的命令行工具现在自己知道 app 装在哪，app 关着也能把它拉起来
- 说明书里「翻新还是重做」的选法改了：看的是原片的声音还要不要，不是要出几条
- 修：命令行工具因为引到了界面代码而编不出来（编译器直接崩，报错里连文件名都没有），
  现在有测试守着这条线

## 0.1.78

### Agent 干活时，底部会一句句播报它在做什么

以前只有一条横幅写着「Agent 正在操作这个任务」，下一步一到就被覆盖 ——
切太快等于没有。

现在底部浮出一条毛玻璃播报，**保留最近 5 条**：

```
✓ 正在新建任务
✓ 正在提取台词
✓ 正在定本片的音色与语速
✓ 正在给第 3 行挑镜头
▸ 正在给第 3~15 行铺配乐
```

最新的一条在最下（视线自然落在底部），往上依次变淡；做完的打勾变灰，
只有正在做的那条亮着 —— 一眼看出走到哪儿了。

**每条至少停 0.5 秒**你才看得清。这个节流放在界面这头：界面显示够时间了
才回执，Agent 收到回执才走下一步 —— 所以是界面在控节奏，不是让 Agent 空等。

播报文案也改了，现在会说清**对哪几行做了什么**：以前是「正在铺配乐」，
现在是「正在给第 3~15 行铺配乐」。

它跟着 Agent 跨模块走（新建任务 → 编导台 → 找镜头 → 导出），切页面不会断。
不挡点击 —— 你随时能接手。

## 0.1.77

### Agent 现在看得见「它自己挑的那一镜」

以前给 Agent 的已选镜头**全是数字和文字** —— 素材 id、名字、时长、倍速，
没有一样是能看的。配音更彻底：连音频文件在哪都不告诉它。

而人在界面上是**看着画面挑**的。

新增 `ishkafel script frames <任务> --line N`，一次拿到这一行能看能听的
全部：参考镜长什么样、**自己挑的那一镜长什么样**、配音在哪。

有个细节：已选镜头抽的是**成片里真正会出现的那一帧**（取段之后），
不是素材开头 —— 给错了帧，Agent 看到的就不是成片里的东西，跟你讨论时对不上。

### 你说「第三行这个分镜不对」的时候

手册里现在写清了该怎么做：**别急着换个词重搜**，先把「要复刻的目标」和
「现在的结果」两张图都打开看，找出差在哪，再问你一句「是嫌画面太暗，
还是这个动作不对」—— 拿着图跟你确认方向再动手。

瞎猜着重搜的代价很实在：上一轮实测连搜三轮，回来的全是别的品牌。

### 修：检索会跨品牌搜回别家的素材

任务明明选了「AI自然消毒液原子库」，搜出来却混进防晒乳和洗护。

两道约束同时失效了：项目是空的，而参考镜没打标所以标签也是空的 ——
**旧代码遇到空标签就直接放弃全部约束**，等于在全库里捞。

现在没打标时会退回用**任务标签组的整个词表**收窄；两道都没有的话，
会明确告诉你「这一轮是在全库里搜的」，而不是让你以为搜到的就是对的。

## 0.1.76

### 补上一个让上一版新功能整个落空的缺口

上一版我做了「让 Agent 看见画面」—— 参考片每一镜带首帧图。**结果在最该用
它的场景里，那个字段是空的。**

原因：从参考片提台词（`extract`）只出台词和切点，**参考镜的画面描述、标签、
首帧图全是空的**，而这三样正是挑镜头的三条依据。软件里这一步是「你点开
某一镜时才去打标」（打标花钱），命令行这条路压根没有入口。

所以 Agent 只能自己抽帧、肉眼看图、手写关键词去搜 —— 中间多了一层转译，
写偏一点就搜回一堆别的品牌的素材（这次实际发生了三次，只能换词重搜）。

现在补了 `ishkafel script tag-ref <任务> --line N`，打完那一行才有描述、
标签和图。手册里也把它写进了必经路径。

### 配音断不了句的行，现在会被拦下来

有一句台词太短（「加一点。」），语音识别认不出来，那一行拿不到逐字时间 →
**断不了句 → 字幕整句糊在屏幕上**。而这种情况以前只记一条日志，导出照过。

这正是「导得出来但片子是坏的」那一类，现在进拦截清单，并告诉你怎么办
（重新生成那一句，或者把太短的句子改一改）。

### 「改了但显示没改」

定音色、调音量这类改动以前返回「应用了 0 条」—— 读起来像什么都没落盘，
其实是生效了的。现在会直接说改了什么：「本片基调：音色 vivi 2.0」。

## 0.1.75

### 端到端跑通了，但暴露出四个卡点

让一个只有说明书的 Agent 拿你那条滴露视频完整走了一遍 —— **成片出来了**
（95.5 秒、1080×1920），但中间两处让流程整个停下来。

**一、挑镜头时它提交的候选一律被判「只能出 0.0 秒」，21 行全废。**

原因：候选素材里没有时长。界面上是拿预览地址现探的，命令行这条路漏了这一步 ——
而手册又要求「把候选原样带回来提交」。**照着手册做必然全军覆没。**
现在候选会带上时长了。

**二、配乐整个断掉，连「这几行不铺配乐」都提交不了。**

取候选曲子的命令根本不存在（手册的样例里有，但没说从哪来）；而且不给曲子 id
时被当成「id = -1」去查候选，于是空段也被拒。所以那条成片是**没有配乐的**。
现在补了 `script bgm-candidates`，空段也放行了。

**三、新建任务的闪退 —— 前两次根因我都猜错了。**

验收数据把规律定死了：只有「本进程第一次打开编辑页」且走新建那条路时才崩；
同样是第一次，走 `open` 从不崩。差别在于新建那条路中间隔着一个**弹窗**，
它关闭时界面正在重建，这一刻去初始化播放器就撞上了。

现在播放器改成**等界面画完第一帧再创建**，两条路进来都是稳的。

**四、`--name` 上一版说做了、其实没接上线。**

我只跑了静态检查没验行为。这次补了测试，并且加了 `ishkafel task-rename`
（界面上人能改名，命令行之前不能）。

### 这一趟花了多少

云端只烧了三处：提台词的语音识别（75 秒）、配音合成（466 字 / 21 句）
外加 21 次逐字时间戳、素材语义检索 24 次。另外下载了 401MB 素材到本地。

导出走本地 ffmpeg，不花钱。

## 0.1.74

### Agent 现在能用上全部五种检索方式

你问的「软件支不支持首帧图去搜索」—— **支持，而且界面一直在用**，
但 Agent 之前只能用其中两种。

| 方式 | 靠什么找 |
|---|---|
| 标签 | 参考镜带过来的标签 |
| 画面描述 | 自己写一句画面去搜 |
| **以图搜图** | **某条素材的画面找同类** ← 之前 Agent 用不了 |
| 口播词 | 素材自己说了什么 |
| 素材名 | 同一批常有命名规律 |

**以图搜图是复刻场景的主力**：画面里很多东西用文字说不清楚，而 Agent
手里就有参考镜的图。现在它的路子是 —— 先按描述搜一轮、抽图看一眼挑出
最像的那条，再拿它去找一批同类。

认不出的检索方式会直接报错并列出可用值，不会默默退回某一种 ——
那样 Agent 会以为自己搜的是 A、实际搜的是 B，而候选看起来都像那么回事。

## 0.1.73

### Agent 现在能「看见」画面了

以前给 Agent 的候选素材只有一句画面描述和一个会过期的网址 —— 它读不了远程图，
等于**只能靠别人总结的二手信息挑镜头**。

现在：

- **参考片每一镜都带本地首帧图**（`framePath`）。Agent 打开就能看见「要复刻的
  是什么画面」，而不是只读一句描述
- 新增 **`ishkafel script peek <任务> --materials 105378,105361`** ——
  把拿不准的那几条候选的画面抽到本地，亲眼看看再决定

按需抽，不是每次都下：一条素材几十兆，先按描述和标签筛一轮，剩下的再看图。

手册里也把这一步的定位写清了：**挑镜头是 Agent 的活，不是软件的活。**
软件的「自动铺一版」只会照着描述去搜，它看不懂视频；Agent 看得懂 ——
参考镜的画面、标签、那一段在说什么，全都拿得到。

## 0.1.72

### 修：新建任务导致的闪退（这次找对根因了）

上一版我以为是「软件刚启动、播放器没准备好」，加了几秒缓冲 —— 判断错了。
崩溃报告显示三次崩溃时软件已经活了 **9 分钟 / 23 分钟 / 89 秒**，缓冲一次
都没用上。

真正的原因：`ui new-task` 建完任务后**直接进新任务，没有先退回列表页**。
上一个任务的编辑页还留在栈上活着，新的又开一个 —— 两个视频播放器同时存在，
底层直接崩掉。

对照很清楚：`ishkafel open` 那条路一直不崩，因为它**先退回列表页再进**。
现在新建也这么做了。

### 修：Agent 会被自己刚建的任务锁在门外

`ui new-task` 建完把界面停在新任务上，于是那个任务被界面占着，紧接着的第一条
写入就被拒 —— 而「占着的人」正是它自己刚叫出来的。更糟的是原来的提示写着
「**或在 app 里强制接管**」，等于让一个只有命令行的 Agent 去点界面。

现在会明确告诉它：把界面挪开就行（`ishkafel open <另一个任务>`），并且说清
「这个占着的人很可能就是你自己刚叫出来的」。

### 补：手册里写了、却根本不存在的命令

`ishkafel voices`（列可选音色）—— 手册一直让 Agent「别自己挑音色，用这条
命令列给人看」，而这条命令从来没有过。结果它既不能挑、也不能列，整条配音
链路在纯命令行下是死的。

现在补上了。**并且加了一道检查：手册里写的每条命令都必须真的存在**，
以后不会再出现这种事。

### 补：两个「给了参数却被默默吃掉」的地方

- `ui new-task --name` 现在真的能给任务命名了
- `script shots --keyword`：没有参考片时，可以自己给一句**画面描述**去搜候选。
  以前提示说「只能按台词与标签找」，但那条路压根没有对应参数

## 0.1.71

### 修：新建任务会让软件闪退

上一版你可能见过「ishkafel 意外退出，是否重新打开」——那不是巧合。

`ui new-task` 会先把软件拉起来，然后立刻下单。软件冷启动 1.5 秒就能接单，
接着弹向导、建任务、进编导台初始化播放器 —— 而那时播放器的底层还没准备好，
直接断言失败、整个软件 abort。崩溃报告里两次的栈一模一样。

更糟的是崩完之后：后面所有可视模式的命令都会报「打不开 app」，而手册说
这种错别重试 —— 人不在场就是死局。

现在两头都加了缓冲：软件启动后缓几秒才接单，命令那边探到是冷启动也会多等。

### 修：可视模式写台词，会把已经写好的回滚掉

上一版没修干净。这次是**按内容判定**：界面写盘前先比一眼盘上被谁动过没有，
对不上就不写。

上一版靠的是「Agent 在场时界面不写」，但「在场」是每半秒轮询出来的 ——
那个窗口里界面照写不误。所以会出现「写第二句时把第一句冲没了，而命令全程
报成功」。按内容判定不依赖时序。

### 修：左栏脚本列不跟着刷新

Agent 改了台词，右边的卡片变了、**左边那一列还是旧的**，同一个界面上同一行
显示着两个版本。原因是那个输入框只在「换了另一行」时才同步文本。

现在只要不是你正在打字的那一行，外部改动都会同步进来。

### `ui new-task` 会告诉你建的是哪一条

以前只回一句「任务已经建好了」，调用方得自己去列表里翻最后一条猜 ——
两个 Agent 同时建就猜错了。现在直接给 id、编号、名字和下一条命令。

## 0.1.70

### 三个被外部验收抓出来的 bug

开了另一个 Agent，只给它说明书、不给源码，让它照着手册从零走一遍。
它抓到三个我自己测不出来的问题：

**一、`ui new-task` 报失败，但任务其实建成了。** 建完之后代码在等你从
编导台退出来才发回执，于是命令必然等到超时。Agent 照着退出码会去重试 ——
再建一个垃圾任务。现在建完立刻回执。

**二、可视模式改台词会静默丢。** 命令返回「成功」，盘上没变。原因是界面
被唤醒打开时内存里是旧数据，之后它任何一次自动保存都把旧的整份盖回去。
**这比报失败更危险**：报失败你会重试，报成功你会带着错往下走。

现在 Agent 在场时界面一个字都不写，而且**会跟着 Agent 的改动实时刷新** ——
以前只做了「滚到那一行」，没做「显示新内容」，所以你看着的是一块不动的板子。

**三、编导台在 Agent 干活时是一张空白拦截页。** 审片台早就改成「只读跟随」
了，编导台漏了。验收 Agent 的原话：「人在旁边看着，看到的是一块黑板」。

### 补了几个 CLI 的洞

- **`ishkafel task-delete <id> --yes`** —— 以前根本没有删除任务的命令，
  Agent 只能看着界面上的 ⋯ 菜单不敢点
- 手册补上：`apply lines` 的三个 op 分别是什么（`insert` 是「在这一行**之后**
  插」，以前只文档了 `set`）、行的两种类型、三套下标基准的区别、
  `ISHKAFEL_APP` 到底怎么找

## 0.1.69

### 新建任务：Agent 当着你的面建

以前 Agent 建任务是在后台悄悄建好，界面一动不动 —— 你只能从「命令说成功了」
去猜它做对没有。现在有一条专门给可视模式用的命令：

```bash
ishkafel ui new-task --mode script --tag-groups 1261,1262
```

软件弹出来 → **新建向导真的打开** → 来源和标签组真的填上 → 停一拍让你看清 →
真的点「创建」→ 进任务。

**不是「演」出来的**：向导是真的那个向导，创建走的是你自己点创建时的同一段
代码。演一套写一套的话两边迟早对不上 —— 这个软件已经因为「同一个东西两处算」
栽过三次。

参数不合格时**不会弹向导**（比如 renew 没给原片、一个标签组都没选）——
让你看着窗口弹出来又关掉，比不弹更糟。

### 手册里明写了「不要用 Computer Use」

你那个验收标准 —— Codex 不该自己觉得这些事情要靠截图点击来做 —— 靠的就是
这句话，加上一份「界面动作对应哪条命令」的清单。

真遇到没有对应命令的界面操作，它应该**说出来让你补**，而不是绕过去自己点。

## 0.1.68

### 修：Agent 在外面建了任务，界面上看不见

你用 Codex 建了任务 5，界面一动不动 —— 因为**任务列表只在进页面那一刻
读过一次**。CLI 在外面建的任务、改完的任务，正开着的界面完全不知道，
你只能退出去重进。

这不是「可视模式才需要」的东西：静默模式下你回头来看，也该看得到。
现在列表会自己发现盘上的变化并刷新。

### Agent 手册重写了定位

以前开篇就说「这是个成片翻新工具」，脚本成片被当成附属拼在最后。
**它俩是平级的两条线**，而且脚本成片根本不需要原片。

Agent 读完开头就形成「这是翻新工具」的印象，从台词造新片那半边会被埋没。
技能描述也一样窄 —— 有人说「帮我做条口播视频」，它根本不会想到这个工具。

现在开篇先讲清一句话：**台词决定时间，画面填进去**。然后摆出两条线
和「怎么选」，还点出了容易混的旁支（空白任务是拼画面、没有配音字幕）。

## 0.1.67

### 划词建镜开放给 Agent 了

「人能干的，Agent 都要能干」——划词这条线之前它完全干不了：读不到镜头
绑了哪几个字、读不到每个字落在哪、也提交不了。

现在它能做和你一样的事：

```bash
ishkafel script show <任务> --line 3 --json | jq '{words, takenWords}'
ishkafel script apply word-shots <任务> --file w.json
```

规则和界面上完全一致：已占住的字不能再划、可以不连续、必须先有配音、
素材必须来自候选。重叠时会点名**该先删哪一镜**，而不是含糊拒绝。

### 顺带发现：两类改动从来没写进 Agent 手册

`apply alloc`（分镜头时长）和 `apply bgm`（铺配乐）——手册里只讲了规则、
**没给命令**，所以 Agent 一直不知道怎么提交。现在补上了，还带了示例。

### 加了一道防线

新增架构测试：**每一类 Agent 能提交的改动，都必须在手册里出现**，
否则测试直接红。

这个坑今天已经踩了三次（三轨混音台、脚本成片整条线的手册没有交付通道、
划词建镜）。靠人记得是靠不住的，所以让「漏同步」这件事本身变成测试失败。
这道防线一加上就立刻抓出了上面那两类。

## 0.1.66

### 修：播到某一镜时，右边整块变成一片灰

不是布局错乱 —— 那是 Flutter 在正式包里显示「这块渲染崩了」的样子。

崩的是**取段条**。你第 6 句第 2 镜的素材只有 3.5 秒，取段起点却是
19.9 秒（素材长度的 5 倍开外），取段条按这个数算出「窗口左边缘正好顶到
条尾」，接着一句 `clamp(8, 0)` 上下限颠倒，直接抛异常。

那个不可能的取段起点是怎么来的：**挑素材那一刻素材时长还没探测出来**，
边界夹不了；等时长回来，那个数就悬在素材之外没人再管。

两头都堵上了：

- 探测到素材时长时，顺手把悬空的取段起点夹回来（还会留够这一镜要用的
  那一段）。你现有的数据打开就会自愈
- 取段条的算术从界面代码里搬出来单独测 —— **数据再离谱也不许崩**。
  界面可以显示得难看，不能整块消失

这个坑一直都在，只是以前没人会手动展开那一镜；上一版加了「播到哪展开
哪」，就一头撞上了。

## 0.1.65

### 修：划完词再加分镜，划的词白划了

你昨天划完词又用「找镜头」加了两个分镜，结果四个镜头全变成 2220ms
（8880÷4）—— **划的词被平摊掉了，而且不报错、不提示**。

原因是「同一个东西两处算」：重排镜头时长的代码散在 9 个地方，划词建镜那
一处认识划词镜，「找镜头」那一处不认识，于是后者把整行重新平摊了一遍。

这一版把它收敛成**唯一入口**，调用方不需要知道这一行有没有划词镜。9 处
全部换掉，老函数在 lib 里已经零引用。

你那一行不用手动修：新版本里任何一次触碰（加镜、删镜、改时长）都会自动
重算回 1280 / 2040 / 2780 / 2780。

### 修：划过词的那一行，点它预览不跳了

其他行都正常，只有正在划词的那一行点了没反应 —— 因为可选文本会把点击
吃掉，不冒泡到行卡片。现在点台词也算点这一行。

## 0.1.64

### 划词之后，字幕自动跟着画面切

这是划词的另一半：**一次操作同时定下画面边界和字幕边界**。

- 划词的镜头把边界让给字幕，画面切到哪儿字幕就换到哪儿
- 中间的自由段自成一屏 —— 那里放了几个镜头是画面的事，字幕不必跟着碎
- 一屏字太多照样再细分（可读性是硬要求），但细分点落在划词边界之内
- **你手动改过字幕的行不受影响**：改过就是改过，之后镜头怎么变都不再
  自动重切。画面说画面的、字幕说字幕的，这条一直都在

### 字幕盖不住语音时会被拦下来

你那条片子的第 3 行就是这个问题：

> 第 1 屏字幕只有 8 个字，但这一屏要念 26 个字、4.7 秒 ——
> 中间那段配音在响，字幕却停着不动。

现在这种情况会进 `blocking`，**导出前必须处理**。不是拦着你不让写短字幕，
是不让它悄悄漏到成片里 —— 你之前就是拿到片子才可能发现。

判据刻意宽松（少一半以上才报）：同音改写、去掉语气词、把「一百块」写成
「100 块」都是正常操作，不该被啰嗦。在你现有数据上跑，正好只抓出那一处。

## 0.1.63

### 划词建镜：选中几个字，画面时长自动对上

以前一句话过三个分镜，你得自己听、自己数「如果你觉得有点贵」读了几秒，
再手动把镜头时长调成那个秒数。现在不用了：

**在选中的那一行台词上划中几个字 → 「用这几个字加分镜」→ 挑画面。**
这一镜的时长就是这几个字的朗读时长。

- 素材 5 秒、这几个字只要 2 秒？照旧走切或变速，一个字都不用改
- 划过的字带底色，**再划不动了** —— 想改就删掉那一镜，字自动恢复可划
- 划头、划尾，中间那段留给自由镜：8 秒的句子划掉头尾各 2 秒，
  中间不管放几个镜头，加起来永远是 4 秒
- 划词的镜头按字序排位，**先划句尾再划句首也不会乱**

### 顺带修好了一个你看不见的偏差

切点从「最后一个字收音」改成「下一个字开口」——**标点的气口归前一镜**。

你那条 43 字的长句里，「贵」收音在 1440ms、下一个字「那」开口在 1560ms，
中间 120ms 是逗号的气口。按收音切，画面在停顿刚开始时就切走，急了小半拍；
按开口切，画面正好在下一句出声那一刻换。全句 5 个标点处的气口是
80~280ms，都在眼睛看得出来的量级。

句尾那 200~350ms 余韵也归最后一镜，所以整句的镜头加起来正好等于配音长度，
段与段首尾相接、中间不漏一帧。

### 改台词会清掉划词的分镜（清之前问你）

台词一改，字的位置全变，「这几个字配这个画面」就不成立了。所以要清 ——
但清之前会告诉你损失什么：「这一行有 3 个分镜是按字划出来的（其中 2 个
已经挑好素材）」，你可以反悔。

## 0.1.62

### 派生产物也按项目存了，共享目录彻底清空

0.1.61 把素材和配乐按项目存了，还剩两个跨任务共享的目录 ——
**预览代理**和**人声分离结果**。它们是算得出来的，但一样没人收：任务删了
还躺在盘上，而且按内容指纹命名，根本反查不出归谁。

现在全部跟着项目走：

```
materials/<任务id>/   素材
bgm/<任务id>/         配乐
voices/<任务id>/      配音
proxy/<任务id>/       预览代理
vocals/<任务id>/      人声分离结果
```

**共享目录只剩 `separator_models/`** —— 那是人声分离的模型，是工具不是
任务数据，本来就该共享。

### 第一次打开这一版会清掉旧的预览缓存

老的 `preview_proxy/`（约 104 MB）和 `material_vocals/`（约 15 MB）会被
整个清掉 —— 它们按内容指纹命名，没法分发到各个任务名下。

**这两样会自动重建**，不会丢东西，但要花时间：预览代理是首次播放时重转；
人声分离比较贵（要跑模型，分钟级），只有「整段替换 + 铺了配乐」的段落
才会用到。

### 顺带：清理逻辑简单了一大截

以前预览代理判不了归属，只能按 10 GB 配额硬砍；人声分离得拿「现存任务还
引用哪些素材」反推孤儿。**这两套机制现在都删掉了** —— 东西在任务名下，
删任务直接带走。

## 0.1.61

### 物料改成按项目存，不再有孤儿

以前素材和配乐存在**跨任务共享**的缓存里（`material_cache/`、`bgm_cache/`），
按素材 id 命名。好处是同一条素材用在几个片子里只下一次；代价是**任务删了
没人收** —— 盘上永远躺着一批不知道归谁的文件，谁都不敢删。

现在它们跟着项目走：

```
materials/<任务id>/…   这个片子用到的素材
bgm/<任务id>/…         这个片子用到的配乐
voices/<任务id>/…      配音（本来就是按项目的）
```

**删项目就一起删干净。** 同一条素材用在两个片子里会存两份 —— 这是「删得
干净」的代价。

### 启动时自动归位，并清掉历史孤儿

第一次打开这一版，会把老的共享缓存分发到各个任务名下，然后收掉共享目录。

在你的数据上演练过：**66 个素材里 53 个有人用，13 个是孤儿（88 MB）**——
那些是以前挑过又换掉的素材，一直没人清。配乐 2 个都在用。

迁移有三条保命规矩：
- 已经在目标位置的文件**不覆盖**（上次迁到一半被杀，重来时不会被半截文件盖掉）
- 每个用到它的任务都拿到副本，**才删源文件**；有一个没成功就留着下次再迁
- 读不到任务清单时**一个都不删** —— 宁可留着占地方，也不能把素材当孤儿清掉

### 顺带

路径从此**只此一处算**（`TaskMedia`）。此前 12 个地方各写各的
`material_cache/<id>.mp4`，改一个漏一个 —— 今天已经因为「同一个东西两处算」
出过静音和配音过期两个 bug。

## 0.1.60

### Agent 补课：最近四版新增的能力，它一直不知道

你问「每改一个功能，Agent 的能力有没有同步」——查下来**没有**。
0.1.56 起新增的东西，Agent 侧基本全部掉队，其中一条还是会静默出错的 bug。

**最严重的一条（已修）**：界面说「这几句配音过期了，要重配」，CLI 却报
`state: fresh`。因为 CLI 拿行上的值判断，不知道本片基调换过。Agent 读到
fresh 就不重配，**直接导出一条前后两个人说话的片子** —— 不报错、不拦截，
人拿到成片才发现。

**另一条更大的**：Agent 装完技能手册，里面 `ishkafel script` 一个字都没有。
脚本成片那条线（14 个子命令）的说明书躺在 `docs/` 里**没有任何交付通道**，
等于整条线对 Agent 不存在。现在和主手册一起交付。

补齐的能力：

| 它现在能 | 命令 |
|---|---|
| 读到本片音色/语速基调 | `script show` 的 `defaultVoiceId` / `defaultSpeechRate` |
| 读到三条轨的音量与静音 | `script show` 的 `sound` |
| 读到每行实际会用谁的声音 | 行里的 `voiceId`（有效值）+ `voiceFollowsDoc` |
| 定本片音色/语速 | `script apply baseline` |
| 改某几行的音色/语速 | `script apply line-voice` |
| 调三条轨的音量/静音/闪避 | `script apply mix` |
| 把方案写成剪映草稿 | `script jianying` |

手册里也写清了三条轨分别是什么（原声=素材自带的声音、口播=念台词的、
配乐），以及**收工前要检查有没有 stale 配音** —— 这一条不拦导出，所以更危险。

### 修：一条会被你「用软件」弄红的测试

剪映那条端到端测试直接读你机器上**活的**任务文件。你在界面上动一下那条
片子，它就红一次（这次是字幕从 62 屏变成 60 屏）。而且它在别人机器上会
跳过，等于只在一台机器上有效。

现在数据固定进仓库，素材用占位文件顶上：谁改自己的片子都不影响，同事
机器上也真的会跑。

## 0.1.59

### 新增：把方案直接交给剪映

编导台顶栏多了一个「**剪映**」按钮（在「导出成片」右边）。点它会把当前
这条片子写成一份**剪映草稿**：画面段、字幕屏、配音、配乐、素材库全部就位，
打开剪映就能接着精修。

- 素材会先补齐并落到本地，草稿里引用的都是本地文件
- **不会替你拉起剪映**：剪映没有给外部程序「打开指定草稿」的通道
  （各种办法都实测过了）。它自己扫草稿目录——启动时必扫（8 秒内认领），
  运行中每隔几分钟扫一次。所以生成完会把**草稿名**告诉你，去剪映草稿
  列表里找那个名字打开即可

> **这个按钮的点击流程还没在真机上走通过一次**，请你点一下试试。
> 它有 25 个单元测试，也拿 2 号任务的真实数据跑过端到端（143.76 秒 /
> 55 个画面段 / 62 屏字幕 / 27 段配音 / 4 段配乐 / 83 个素材），
> 生成的草稿在剪映里打开播放确认过——唯独「在界面上点这个按钮」这一步
> 没能自动验证（副屏的点击坐标映射不准）。

### 这一版还包含 0.1.48 起积压的全部改动

你手上一直跑的是 0.1.56（而且是从另一个工作区构建的），所以下面这些
你可能都还没见过：

- **本片基调**（0.1.57）：顶栏「本片 · 云希 · 1.25x」，铺一版前先定音色
- **多选批量**（0.1.58）：Shift / ⌘ 点选、⌘A 全选，底部操作条改音色/语速/删除
- **播放到哪展开哪**（0.1.58）：播到哪一镜，那一镜的操作栏就摊在手边
- **三轨混音台**（0.1.56）：原声 / 口播 / 配乐，各有总音量与静音
- **任务编号徽章**（0.1.55）：进任务也看得见 #N，点一下复制
- **审片台的 Agent 操作**（0.1.54）：人看着卡片墙指挥 Agent 剔除

## 0.1.58

### 改 5 句不用点 5 次了

选行用的是你本来就会的手势，**没有「批量模式」这种东西** —— 就像 Finder，
选一个文件和选十个文件，操作是同一套：

- **Shift + 点击** 选到那一行为止的一段
- **⌘ + 点击** 加选 / 取消某一行
- **⌘A** 全选，**Esc** 取消选择

选中两行以上，底部浮出一条操作条，只有三件事：**音色 / 语速 / 删除**。
它们的共同点是一行行点特别痛。字幕样式已经有「整片」了；配乐是段的概念、
不是行；时长每行都不同，批量设没意义 —— 所以都不放进去。

改完音色或语速，会问一句「这几句要重新配音吗」，重配**带进度、随时能停**。

删除会说清删掉的是什么：「其中 3 句已经配好音，删掉这些配音也一并没了」，
而不是只说「删除 8 行」。删错了 ⌘Z 撤销。

### 播放到哪，那一镜的操作栏就摊开在手边

以前播放只是把那一行选中，你还得自己点开才能改。现在播到第几行第几镜，
那一镜的取段、速度、原声就直接展开在眼前。

**你一动手，自动展开立刻让开** —— 不然你正改着第 3 行，播放头走到第 5 行
就把你手上那一栏收走了。按暂停就恢复跟随。

### 「配音」轨改叫「口播」

三条轨并排时，「原声」和「配音」太容易混。软件内部一直管它叫口播轨，
现在界面也统一了：**原声**（素材自带的声音）/ **口播**（念台词的）/ **配乐**。

### 修：换过音色的旧配音文件，一直没人清

每换一次音色就多一批死 mp3 躺在盘上。孤儿清扫只管「归属不到现存任务」的
整个目录，任务还活着时它一个都不碰 —— 所以这批文件谁都不管，批量换 28 句
就是 28 个死文件。

现在每次进任务顺手收一次。**只在进门那一刻收**：编辑当中删会让 ⌘Z 撤销
撤成死链（真机上出现过整行无声）。

## 0.1.57

### 这个片子听起来是谁在说话，现在有个地方定了

以前音色只能一行一行选。铺一版的时候软件拿目录里第一个音色去配 27 句 ——
不对就白烧一轮语音合成，然后你还得点 27 次改回来。

现在编导台顶栏常驻一枚「**本片 · 云希 · 1.25x**」：

- **铺一版之前如果还没定过音色，先卡住问你一次** —— 这就是「不浪费一次生成」
- 新写的行、新生成的配音，都跟着本片基调走
- 某一句要不一样，仍然在那一行单独改；改过的行会标出来
- 从顶栏改基调，或在某一行勾「应用到整片」，都是**定基调**而不是逐行写死：
  之后新加的行也跟着走，不会留在旧音色上

### 改了音色，已经配好的那几句会问你一句

这是之前会静默出错的地方：改完基调，前 10 句还是旧音色、后 18 句是新的，
**混出一条前后两个人说话的片子**，而且最容易一路漏到成片。

现在改基调的那一刻会问：「已经生成的 10 句还是旧音色，要不要一起换？」
选重新生成就批量跑，**带进度、随时能停**，停下时已经生成好的保留。

### 语速也一样有本片基调

和音色同一套规则。顺带修了一个隐藏的语义问题：以前语速「没设过」和
「设成原速」是同一个值，所以单独把某一句调回原速做不到 —— 现在分开了。

## 0.1.56

### 主预览下面变成真正的三轨混音台

你在行内把原声调到 0，回到主预览下面拉那根「原声」滑杆——**一点反应
都没有**。查下来不是冲突，是那根滑杆名不副实。

它调的从来不是「原声轨总音量」，而是**「配音行没单独设时，原声压到
多少」**（默认 0，防止原声和口播叠成两份声音）。画面行没有口播、压根
不参与这条规则，所以拉它对画面行永远无效。它只是叫着「原声」、
摆的位置像总音量。

现在按你说的那张混音台重做：

```
原声  ●━━━━━━━━━○━━  75%   🔇
配音  ●━━━━━━━━━━━━  100%  🔇
配乐  ●━━━━━○━━━━━━  45%   🔇

☑ 有口播时把原声压到 0%   ●━○━━━
```

- **三条轨各一根总音量 + 各一个静音钮**，对所有行生效，拉哪根都有反应
- 静音是「临时闭嘴」：滑杆位置不动，再点一下回到原来的地方
- 逐镜原声、逐段配乐是**相对值**，乘在总音量上——所以单独调过的镜头，
  拉总音量也跟着变
- 原来那个「口播时压低原声」独立成一行，回归它本来的名字，压到多少
  可调（不再只能 0 或全开）

预览、行内播放、成片导出三条路读的是同一份混音设置。

**老片子的声音一个字节不变**：三条总音量默认 100%、闪避默认开且压到 0%，
和升级前完全等价；老档里的 `sourceVolume` 原样读成闪避电平。

## 0.1.55

### 任务编号：进到任务里也看得见，点一下就复制

你跟我（或跟 Agent）说事情报的是「#12 的第 3 句」，可进了编导台/工作台/
审片台就找不到自己在第几号任务上了——工作台和审片台根本没显示，编导台
虽然有，但用的是最淡的一档灰，等于没有。

现在三个模块的顶栏最左边统一有一枚编号徽章，**点一下就复制**，直接粘给
对方。还没补号的任务显示 id 前段，复制的是完整 id。

### 修：画面行的原声调不响

真机 bug，根因是**界面在撒谎**。画面行（没有台词的纯画面行）：

- 滑杆显示的默认值取的是「全片原声基调」（默认 0，写着「静音」）
- 实际播放用的却是满音量

同一个东西两套默认。全片基调是 0 的时候，人看到滑杆写着「静音」，
就去拖它想修好——**一拖就把一个显式的 0.0 写死在那一镜上**，从此那一行
真的哑了，而且之后把全片原声调到 100% 也救不回来（逐镜的设定压着全片）。

这一版把「这一镜该出多大声」收敛成唯一一处：画面行默认满音量、配音行
默认跟随全片基调，滑杆、行内预览、整片预览、成片导出读的都是它。同时
把静音标成警示色，「恢复默认」做成看得见的按钮——被写死的那几镜，
一眼能找到、一点就救回来。

**已经被写死的镜头不会自动改**（也许你真想让它静音）：在那一镜的原声
滑杆右边点「恢复默认」即可。

## 0.1.54

### 审片台、导入、导出也能让 Agent 来做了

上一版说这三个模块「性质不同，优先级低」——那个判断是错的。判断标准不该是
「这个模块是干什么的」，而是**人会不会在这里指挥 Agent 干活**。

- **审片台**：人看着卡片墙说「第 3、7、12 条删掉」，Agent 去删。
  `review list` 看编号、`review drop/keep` 动手，编号写错整批不落盘
- **导出**：分钟级的活儿，进度实时显示在界面横幅上，人不用盯着终端
- **导入**：软件自己弹出来说「正在导入 xxx」，建好之后把人带到新任务上

### 人正开着界面时，Agent 不再是「写不进去」

以前人一打开审片台，任务就锁了，Agent 的写入直接被拒——可人正是开着它
在指挥 Agent。这一版改成**委派**：界面开着就把活儿交给界面去做。

这不只是绕开锁。审片台上剔掉的卡本来就只是**界面里的临时状态**，人按
「确认」才落盘——Agent 自己写盘会让人还没确认盘上就变了。委派之后，
人看到的和最终结果永远是同一份东西。Agent 那头也会如实回报「已经标好了，
按确认才生效」，不会报成「已删除」。

### Agent 占着任务时，界面不再是一张空白拦截页

可视模式下人正是为了看它干活才打开那一页的，拦掉等于把要看的东西挡在
门外。现在改成**只读跟随**：候选照常显示，它动哪张卡就滚到哪张卡，
横幅写着它这一步在做什么，它一收工页面自动恢复可操作。

## 0.1.53

### 可视模式不再只有编导台

上一版做了两种模式，但只有编导台会跟着 Agent 走。这一版把那套地基做成
**全软件共用**的：

- Agent 说「去编导台第 10 句」「去工作台第 3 个单元」，界面负责怎么去——
  没开就拉起来、人停在别的页面就先退回来、再进目标模块
- **工作台也接上了**：它是时间线式的，所以「看第 3 个单元」就是把播放头挪
  到那儿；顶部横幅除了说谁占着，现在还会写出**它这一步在动什么**
- 同一个模块里连着做几步不会重复弹窗，只有换模块才会把人带过去

各模块只负责「我怎么把那个位置摆到眼前」——编导台按行滚、工作台挪播放头——
共用同一份状态与同一套回执，不各造一套。

## 0.1.52

### Agent 干活的两种模式

以前只有一种：Agent 在后台改数据，界面上顶多显示一句「它在忙」。现在分成两种，
**由你对 Agent 说话时决定**：

**静默**——人不在场。它跑完，你回来看结果或直接看成片。不弹窗、不打扰。

**可视**——你说「我要看着你做」。这时候：

- **软件会自己弹出来**，并落到它正在做的那个任务上
- 它动哪一行，界面就滚到哪一行；它开哪个面板，界面就打开哪个——
  和你自己点开时是同一个界面
- **每一步都等界面真的展示完才继续**（滚动停下、面板展开）。不是定时等几百
  毫秒——机器快的时候不白等，慢的时候也不会一闪而过没看清
- 这期间你点不动软件（两边同时写会互相覆盖），但**你随时能在 Agent 那头喊停**：
  一按停止它立刻放手，界面马上能动，你可以自己改，改完再让它接着看

就像站在实习生旁边看他干活——随时能打断、帮一把、纠正一把。

## 0.1.51

### 脚本任务在列表里有封面了

排好 27 句、配好镜头的片子，在任务列表上和一个刚建的空任务长得一模一样——
都是黑块，看不出哪条做过。

现在脚本任务的封面取**成片的第一帧**（第一行第一镜的画面，从这一镜实际用到
的那一刻取，不是素材开头）。挑完镜头就有，换了第一镜会跟着换；还没配镜头的
空任务仍然是黑的——那是对的。

## 0.1.50

### Agent 现在能独立做一条片子，而且你看得见它在做什么

编导台的全部能力开放成了命令行，任何能跑 bash 的 Agent（Claude Code、
Codex、别的）都能驱动：

```
script new / extract / voice / export     建任务、识别台词、配音、导出
script show / shots / subtitles           看懂任务、拿挑镜头与断句的依据
script apply shots / subtitles / alloc / bgm / lines / shot-edit / screen-text
```

**它干活时，界面跟着它走**：顶部横幅写出它正在做什么，右栏滚到它正在动的
那一行，展开它正在调的那一镜——和你自己点开时是同一个界面。这期间你点不动
（两边同时写会把彼此的活覆盖掉），右上角有「我来接手」随时抢回来。它要是
崩了，一分钟后界面自动解除，不会被永久锁住。

### 哪些交给它、哪些不交，是按「能不能验证」划的

能验证输出的才交出去：挑镜头（素材 id 必须在候选里）、字幕断句（切点是词
序号，递增、在范围内、每屏字数够不够一查即知）、分时长（总和必须等于配音
时长）、配乐分段（区间要连续铺满全片）。

识别台词和配音**不交**——时间戳偏两百毫秒就足以毁掉整条片子，而这个错不会
报错，一路静默到看成片才发现。

**字幕断句是它比软件强的地方**：软件的自动分屏是「标点优先、其次停顿、
最后按字数硬切」的笨办法，本质在猜；而断句是语言判断。

### 提交不合格会整批拒绝，并一次点全所有问题

```
这一批没有落盘（整批拒绝，下面是全部问题）：
· 第 2 行的切点 18 超出范围（这句共 9 个字）
· 第 2 行第 2 屏只有 1 个字——闪一下就过去，看的人只会觉得晃眼
```

改一条提交一次是在浪费双方时间。

## 0.1.49

### 想把中间几句换首配乐，不用再切两刀了

配乐是把整片切成连续几段、每段一首曲子。以前想把第 8~10 句换成另一首，
得先在第 8 句切一刀、再在第 11 句切一刀、然后点中间那段选曲——三步，
而且两刀切在哪很容易点错。

现在点开任意一段的选曲面板，顶部就能直接改「这段配乐管第几句到第几句」，
改完相邻的段自动让位（被盖住一半的缩短，被完全盖住的消失），整条轨仍然
连续铺满全片。

## 0.1.48

### 三路声音彻底分家，画面行的原声终于能逐镜调

接着上一版：上一版把画面轨的音频关掉了，这一版把剩下两处同样的毛病也治了。

**问题是同一个**：一条声音轨上不能混不同格式的音频——播放器一遇到格式变化
就要停下来重搭链路，画面卡住、声音被反复拽回同一处。除了画面轨，口播轨上
也有两处：画面行用的是素材（mp4）、配音行用的是合成的 mp3；配音比画面短时
还会插一段格式不一样的静音。

现在的分工是：

- **口播轨只放配音**，画面行在那一段垫同规格的静音
- **素材原声走独立的一条轨**，配音行和画面行都走它——所以「这一镜的原声调到
  35%」才真的能生效，而且**拖着滑杆就听得到**，不用松手也不用等
- **配乐轨照旧**

进口播轨的每一段都预先转成同一规格并补足到坑位长度（短了垫静音、长了截断），
从此既不留空档、也没有格式接缝。

### 镜头卡上播的，和成片听到的一致

以前在镜头卡上点播放，素材原声是**满音量**播的——你把原声调到 35%，主预览变了、
卡上没变。现在卡上播的音量与成片完全一致，调音量时正在播的那一镜也当场跟着变。

### 编导台选配乐改成单选

选配乐的面板是和成片翻新那条线共用的：那边一个任务要导出好几条片子、每条配
不同的曲子，所以能选多首。**编导台是一条片子**，一段配乐就一首——代码里一直
只用第一首，多选出来的其余几首根本不会生效。现在编导台这边只能选一首。

## 0.1.47

### 修掉预览里口播「卡住反复念同一个词」

反馈的现象：播到第 9 秒，「还送」连着重复十来遍，然后才接着往下播；有时候
表现得轻一些，就是声音忽大忽小。**同一条片子导出成片完全正常。**

抓到的现场数据是这样的：

```
主时钟 9955ms  ← 连续 22 次纠偏里，它一动不动
口播轨 10383ms ← 也不动，怎么拽都拽不回去
```

**画面轨卡死了。** 它把几十条素材拼成一条流播放，而这些素材有的带音轨、
有的不带；播到「不带 → 带」的接缝时，播放器要重新搭一遍音频链路，画面就在
那里停住好几秒。画面是整条片子的时钟，它一停，口播就被反复拽回同一处——
听起来就是一个词反复念。这条片子里有 16 处这样的接缝，第一处正好在第 2 句。

这一版把声音彻底拆开：**画面轨不再解码任何音频**，接缝也就不存在了；
素材原声挪到独立的一条声音轨上，跟着镜头走。现在口播、原声、配乐三路
各走各的，互不影响。

修完实测：同一段播放，纠偏从 22 次降到 1 次，偏差稳定在 100 毫秒以内。

## 0.1.46

### 修掉预览里口播「忽大忽小、卡住重复」

反馈的两个现象其实是同一个毛病：预览播着播着，口播声音忽大忽小；有时候卡在
某几个字上反复念（「还送、还送、还送……」）。**同一条片子导出成片完全正常。**

根子在**变速镜头的预览切片短了几帧**。给变速镜头做预览时，会先渲一段
「时长正好等于坑位」的对齐切片；但视频时长只能是整帧的倍数、坑位是任意毫秒数，
ffmpeg 截出来就比要求的短了 15~49 毫秒。画面轨是整条片子的主时钟，它播到这些
镜头就提前收，位置一路偏快；口播轨按主时钟对齐，被反复往前拽——偏差累积到
一定程度就会来回越过纠偏线，于是声音一会儿被拽一下。

导出侧一直是**克隆最后一帧补足**的（所以成片没事），预览这边漏了这一步。
这一版补上，并且渲完当场量一次：切片比坑位短就记进日志点名。

实测这条片子里 7 个变速镜头有 5 个原来都偏短，现在全部够长。

### 改设置不再连累正在播的预览

以前改任何东西——哪怕只是调个语速、改句字幕、换个标签——都会在 0.6 秒后
把整条预览轨重铺一遍。正在播的时候重铺，画面和声音都会被扰一下。现在只有
真正影响画面/声音编排的改动才重铺。

- 重铺期间播放器下方会显示「正在重铺预览…」，不用再猜是不是没点中
- 改语速会当场告诉你「已设为 1.25x——点这一行的『重新生成』才会按新语速配音」，
  因为语速只在下次生成配音时生效

## 0.1.45

### 「生成草片」改成「自动铺一版」，配镜照着参考片来

**名字变了**：「草片」是行话，也没说清点了会发生什么。「铺」对应的是
把整片的空位填满、再逐句替换；「一版」是说它可以改，不是终稿。

**配镜的依据换了，这是这一版的重点。** 以前是「有标签就按标签搜，
否则拿台词去搜画面描述」——后者是个错配：台词说的是「如果你觉得有点贵」，
而素材库里存的画面描述是「手持喷雾瓶在厨房台面」，一句话和一幅画根本不在
一个维度上，搜出来的东西和这句话没关系（而且只取第一条）。

现在走的是这个产品真正的主线——**复刻参考片**：先看懂参考片这一镜长什么样
（多帧识图，一次拿到画面描述和画面标签），再拿这个描述去妙啊找像的画面。
和你在「找镜头」面板里手动做的是同一条路，只是批量跑一遍。

**手写脚本不再自动铺镜头**：没有参考片就没有依据，与其瞎捞一堆不相干的
画面让你逐个删，不如空着——空着一眼就看得出「这里还没做」。这时候会明确
告诉你：去右栏「找镜头」挑，或者先上传一段参考视频。

## 0.1.44

### 找镜头面板：参考镜头也能就地播，卡片放大

- 上面那排**参考视觉镜头**每一镜都能点开看了：点缩略图上的播放键，
  画面就在那个位置上播这一镜（播的是原片的对应区间），再点停
- 参考镜卡片放大（宽 168→232、整排高 96→132、缩略图 48→78）：
  它是这个面板的主线索——默认就拿它去找像的画面，看得清才知道要复刻什么
- 点卡片其余地方仍然是「用这一镜的画面去找」，播放键没有抢掉主操作；
  参考镜和候选卡共用一个播放器，同时只有一个在响

## 0.1.43

### 修掉预览里「台词最后几个字一直重复」

反馈的现象很怪：预览播到后面，台词的最后几个字一直重复，但**同一条片子
导出成片却完全正常**——画面对、声音也对。

原因在预览和导出拼声音的方式不一样：

- 导出是逐行渲染，每一行的声音都补齐到画面那么长，严丝合缝
- 预览是把几十段配音**首尾相接**成一条流播。这种拼法没有「空档」的概念——
  某一行的配音比画面短（比如把镜头调长了、或者用过「放慢充满」），
  那个空档会被直接压掉，从这一行起后面每一句的声音都提前一截。
  累到片尾，声音已经播完而画面还在走，播放器就被反复拽回末尾——
  听起来就是最后几个字一直重复

这一版把声音轨的空档用静音垫平，声音和画面严格一样长。另外加了一道自检：
以后任何一条轨上留了空档都会记进日志，不用再靠现象倒推。

## 0.1.42

### 找镜头面板：看得更多、能就地看片、搜不到时有兜底

- **弹窗按屏幕比例放大**：以前是写死的 920×660，一屏只看得到六七条，
  197 条候选要翻九页。现在按屏幕的 88% 铺开，一屏能看到十几条，
  每页也从 20 条提到 40 条，翻页次数减一半
- **候选卡能就地播**：卡片中间有播放键，点一下就在这张卡上播，再点停。
  不用再为了看一眼素材开关一次播放窗
- **一键清空筛选**：标签勾窄了搜不到东西时，「清空筛选」把约束全松开重来
- **多了「按名称」这条兜底路子**：主路径还是拿参考镜头的画面描述、标签
  去找像的画面；实在筛不到时，直接输入妙啊里的文件名把它捞出来。
  切到按名称会自动摘掉标签约束（都按名字找了，还挂着标签只会继续搜不到），
  切回别的维度时标签原样还回来

## 0.1.41

### 素材原声可以留下来了：原声 + 口播 + 配乐三者共存

以前分镜素材自带的声音是被直接丢掉的，配上背景音乐之后就更听不到了。
可素材里常有音效——喷雾声、开门声、翻冰箱的窸窣声，全丢掉片子会发干。

- **预览下方多了一根「原声」滑杆**：管整片的素材原声出多大，0 就是静音。
  拖动即时生效，口播、配乐、原声一起响
- **每一镜可以单独开小灶**：镜头详情里也有一根「原声」滑杆——这一镜有音效
  就推上去，那一镜背景嘈杂就压到静音，设过的显示「跟随整片」可以一键还原
- **默认仍是静音**：升级不会改变你已经做好的任何一条片子的声音，想要就推上去

两路声音各保各的音量，不会出现「一开原声，口播就变闷」。

## 0.1.40

### 画面铺不满的镜头，现在会被拦下来

真机撞到的一个坑，两副面孔，很难联想到同一个原因：

- 预览播到第 15 句，声音变成「现在我们家每个月每个月每个月……」一直重复到这句结束
- 同一句导出成片，声音正常，画面却在「每个月」那里**定格**，直到下一个镜头才动

根子是**这条素材只有 1.2 秒，却被分了 6 秒的坑位**。素材时长原本靠对着网络地址
去问，网络一抖就问不到；问不到就被当成「有多长都行」，于是想分多少分多少。
成片那边只能拿最后一帧撑满剩下的 4.8 秒（定格），预览那边画面提前放完、
配音被反复拽回同一处（听起来就是台词重复）。

这一版：

- **素材下载到本地就量一次真实时长**——文件都在本地了，没必要继续猜
- **铺不满就是阻断**：这一行不进预览，中栏明确写出「第 N 镜的画面只有 1.2 秒，
  铺不满 6.0 秒（差 4.8 秒）——换条长一点的素材、放慢这一镜，或把它截短」
- **导出同样拦下**，点名到行和镜。宁可不出片，也不出一个定格几秒的片子

## 0.1.39

### 配音「念岔了」不再蒙混过关

反馈的现象：一句话前半句正常，后半句两个词一直重复到结尾。这是大模型
语音合成的老毛病（解码陷进循环），也会表现成念到一半就断。以前这种音频
会照样存下来、照样进成片，只有靠人听才能发现。

- **生成时就自查**：合成完本来就会把音频转写一遍（为了拿到逐字时间），
  现在顺手把「念出来的」和「写的」比一比。念重复、没念完、语速离谱的，
  **自动重来一次**，第二次好了就用第二次，坏的那份立刻删掉
- **两次都念岔就明说**：给出是哪一句、岔在哪儿，并建议把台词拆短再生成。
  绝不把卡住的声音放进成片
- **老配音也体检**：已经躺在方案里的配音同样按这套判据检查，念岔的行上
  直接标红「这句念岔了」，鼠标移上去说清原因和怎么办——不用等你听到
- 判据刻意放宽：识别把「10瓶」听成「十瓶」、「厨垫」听成「橱垫」这类
  同音差异一律放过，只抓一眼就不对的（字数暴涨、明显截断、语速离谱）

## 0.1.38

### 导出不再因为「素材还在下」把你赶回去

以前点导出，只要有一首配乐或一条素材还没下完，就弹一句「还没下载到本地」
加一个「知道了」——你只能自己瞎等，再点一次试试。现在：

- **点了导出就是要导**：还在下的，进度条上写「正在准备素材 3/12」，
  下完自动接着导，不打断
- **只有真下不下来才停**，而且说清是哪一段、为什么（登录过期 / 网络不通 /
  素材库里已经没有这条），并且给能点的按钮：**重试下载**；
  卡住的如果全是配乐，还可以**去掉这几段配乐直接出片**
- 素材本来就是「挑中即下载到本地」的（别人在素材库那边删掉也不影响你出片），
  这次补的是**等待与失败的交代**

### 配乐下载状态看得见了

分镜卡一直有下载状态（转圈 / 失败标红 / 点一下重试），配乐色带以前什么都没有
——曲子下砸了全程无感，直到点导出才知道。现在配乐色带也有：
正在下就淡出并转圈，下砸了整段标红打叹号，**点一下就重下**。

## 0.1.37

### 字幕分屏：长镜头下的长台词，一屏一屏地出

同事反馈的场景：一个镜头很长（十秒盖过五六句话），字幕却整段堆在画面上。
这一版把「屏」变成字幕的基本单位——**切点跟着语言走，跟镜头没关系**。

- **自动分屏**：一句话按句读与字数上限切成若干屏，依次出现。每屏能装几个字
  由**当前字号**决定：字调大，屏自动切得更碎，字不会出画
- **镜头时长随便改，字幕自愈**：屏记的是「从哪个字另起一屏」，不是时间点。
  把一镜从 4 秒缩到 1.6 秒，字幕不会重复也不会丢
- **手动优先**：镜头详情里一屏一行，左边是它在成片里的入点，中间直接改字
  （只改这一屏，不用整段重打），行尾三个动作——拆成两屏、并回上一屏、
  这屏不出字。改过的行标「已手改」，一键「恢复自动」
- **打轴**：原位播这一镜时点「✂ 在这里换屏」，听到哪儿切到哪儿；
  切不动会说清原因（这里已经是屏首 / 这句配音没有逐字时间）
- **四处显示同一份**：镜头卡缩略（首屏 + 「+N屏」）、卡上播放叠的字
  （跟着播放位置换屏）、中间预览、导出成片——一字不差

### 修掉三个会让字幕出错的老问题

- **一个字一屏**：逐字停顿一样大时，自动分屏会把第一个字单独切出去，
  一句话炸成一列。现在屏尽量装满
- **价格被写错**：语音识别会把「69.9一」并成一个词，照词拼接就出「69.91瓶」。
  现在字幕一律**按脚本原文**出字
- **老配音整段堆字**：早期生成的配音没有逐字时间，以前只能整句一屏。
  现在照样分屏（按字数摊时间），界面上写明「按字数均分」，
  重新生成配音后就能手工分屏与打轴

## 0.1.36

### 编导台大更新：取段所见即所得、参考按镜头找、配乐色带、导出可选规格

**镜头取段**
- 取段条上的数字全部是**成片时间**：整条 = 这条素材按当前倍速能出多长，
  蓝框 = 这一镜在成片里的时长。「1.5 秒就是 1.5 秒」，与倍速无关
- 条上铺的是**素材真实画面**（帧格锁素材比例，窗口拉多宽都不拉伸），
  窗口外压暗——拖到哪看到哪
- 拖完松手自动播一遍**调整后**的这一镜（画面按倍速播 + 这一段的配音），
  播一次就停；连着拖只在最后一次之后播

**原位播放（不再弹窗）**
- 参考卡、镜头卡点一下就在卡上播：镜头卡播的是「成片里这一镜的样子」
  ——画面 + 这一句配音里属于它的那一段，卡底还叠着这一镜的字幕
- 三个声源（分镜卡 / 主预览 / 配音试听）互斥：谁开始播，其它自动停

**字幕**
- 预览下方常驻**字幕工具条**：位置、字号都是滑杆（连续、实时），
  六色、遮罩三档，右端「应用到整片」；调的时候预览实时变
- **每张镜头卡上直接显示这镜的字幕**（位置/颜色/遮罩按真实样式缩放），
  不打开面板就能一眼扫完整句每镜显示什么字
- 手改规则：手写或点「同上一镜」就脱离自动匹配（标「已手改」，
  可一键恢复自动）；没手改的永远跟着时长实时重算

**添加分镜（原「增删」）**
- 弹窗里的参考 = 这句在原片里的**各个视觉镜头**（进去就切、loading 等它，
  不再是关掉重开才对）
- **点哪个参考镜头，就用哪个镜头的画面条件搜**（它的画面描述 + 标签，
  按需 AI 读一次画面并缓存）；一个都不选就用这句的标签 + 脚本台词
- 参考里的台词只展示、不参与检索；每镜可「直接用原片这段」

**参考素材**
- 每句的参考槽位可以传**视频或图片**（只能一个，替换先确认）；
  手动传的视频不超过 15 秒，传完自动识别台词（仅展示）+ 切视觉镜头

**配乐（重做）**
- 行块左缘一条**配乐色带**：同段同色、相邻段异色。鼠标移上去，
  在要换歌的那一句点「✂ 从这句换一首」——30 句配 3 首只要切两刀
- 段首点色带：换曲 / 音量 / 设为这段不要配乐；顶栏「配乐」= 给整片配一首
- 主预览按段播配乐、按段应用音量

**导出**
- 导出前先选规格：分辨率 / 帧率 / 码率（可自定义）/ 编码 / 格式，
  「预计大小」实时算；记住上次的选择

**顶栏瘦身**
- 只留「配乐 · 生成草片 · 导出成片」。字幕样式归工具条；
  「从视频提取脚本」是空脚本时的一次性起步动作，回到起步引导里
  （它会覆盖整份脚本，不该常驻）

**修复**
- 第 2 句台词整段无声：撤销把数据回滚到了已被删除的旧配音文件——
  重新配音后不再立即删旧文件，配音文件丢失的行也会点名而不是装哑巴
- 镜头卡黑图：缩略图改用本地素材抽帧（原来的签名地址会过期）
- 右栏点击间歇性整体失效（Flutter 线程合并实验特性，已关闭）
- 字幕里的 69.9 被显示成 699（剥标点误伤数字小数点）
- 拖到底线时「调不动了」提示连环弹

---

## 0.1.35

### 编导台：字幕模型定稿——字幕写在镜头上、样式按句统一

- **字幕写在镜头上（讨论定稿）**：每个镜头带自己的字幕——镜头详情
  里的「字幕」框**自动预填**这镜时段说出口的字（听一遍对不对即可），
  改了以你写的为准，清空恢复自动；「同上一镜」一键让几个镜头共用
  一句。相邻镜头同句时字幕连成一条不闪断。预览与成片同一份派生
- **样式粒度 = 句**：位置/字号/颜色/遮罩每句统一一套——预览里
  **点字幕改的就是正在播的这一句**（所见即所改），顶栏字幕按钮改
  整片基调；句级面板新增「应用到整片」一键提升为全局默认
- 「分小行」那套（切点选择器）退役——镜头级字幕覆盖其全部场景
- 修：拖取段条到底线时提示连环弹（一次拖拽攒一队列 SnackBar）——
  节流为 3 秒最多一条

---

## 0.1.34

### 编导台：镜头取段直接拖、台词可以分小行指定给镜头

- **取段条（新）**：镜头详情里的「起点滑杆 + 时长步进」换成剪辑软件
  式的取段条——底条是素材全长，蓝色窗口是实际用的那一段：**拖窗口
  整体平移**换起点（在素材上滑动裁切）、**拖右缘把手**改时长
  （多退少补邻镜自动配合）。4 秒素材要哪 2 秒，拖一下就框好
- **台词分小行（新）**：一句台词（台词语义单元）里可以把文字分段
  指定给镜头——「家人们」给前两镜、后半句给第三镜。点开镜头详情
  「✂ 从这一镜分小行」，切点选择器会按配音说到哪个字自动建议，
  点个字确认即可。分组后块里每小行显示「这段台词 + 它的镜头」，
  段文字旁悬停「并回上行」随时合并
- 字幕跟着分组走：预览和成片里这段字横跨组内所有镜头连续显示
  （手动指定优先于自动按词切分）
- **取段条铺的是素材真实画面**：胶片形态——窗口外压暗、窗口内就是
  选中的那段画面，拖到哪看到哪；配「起点~终点 / 素材可用 / 选用」
  三个数字口径（对齐参考图）。帧按素材缓存，算过一次不再抽
- **修灾难级 bug**：右栏行带板的鼠标点击会**间歇性整体失效**（同一
  构建时死时活）——根因是 Flutter「UI 与平台线程合并」实验特性，
  已在 Info.plist 关闭，多轮重启验证不再复现

---

## 0.1.33

### 编导台：分子/原子各归其位——外面是分子列，原子在搜索里当条件

- **行块收敛回分子粒度**：参考不再在块里铺格子——一张整段参考卡
  （「参考 8.0s · 4 镜」），点击播放对照。上一版挤成一团的胶片条废弃
- **参考原子进找镜头面板（新）**：给这句话找镜头时，面板顶部横排
  这一句的参考分镜（原子）——每个原子带首帧和**它那个时段说的话**
  （新提取的脚本自动带词级时间戳）。**点选哪个原子，检索词就切成
  哪个原子的台词**，一个原子一个原子地找替代品；再点一下回到整句
- 原子上还有「直接用原片这段」：本地引用原片区间作镜头，和搜索
  结果一起进已选序列
- **块内左右结构**：左边参考卡（与镜头卡同规格竖屏摆放、灰调+
  「参考」角标、点击播放整段）、右边我的镜头行，中间一条呼吸缝——
  原文｜译文左右对照
- 修：参考缩略图抽帧失败会无限重试起 ffmpeg（每帧一次）——失败记账
  后不再重试

---

## 0.1.32

### 编导台：字幕按镜头切分、字幕直接上手调、参考变胶片条

- **字幕跟着镜头走（新）**：一句台词里有几个镜头，字幕就按说话的
  节奏分到镜头上——「家人们」只在第一镜出现，后半句归后面的镜头。
  依据是配音的**逐字时间戳**（新配音自动带上；老配音重新生成即可），
  预览和成片用同一套切分规则。没有时间戳的老配音仍整句显示
- **字幕就是操作对象（新）**：预览里的字幕**点一下**直接打开样式
  编辑；**上下拖**直接调位置（拖的是行级就改行级，否则改全局），
  不用再去顶栏找按钮
- **参考重做成胶片条**：这一句在原片里的完整区间是一个整体（竖排
  「参考」标签），内部按视觉切点分格——一眼读出一句话在原片换了
  几个镜头；格宽随镜头时长成比例，悬停出现播放与「用它」。
  参考（原文）在上、我的镜头（译文）在下，对照着配

---

## 0.1.31

### 编导台：预览里能看到字幕了（所见即所得）

- **实时字幕层（新）**：预览播放到哪一句，字幕就显示那一句——位置、
  字号、六色、形态（描边/黑条/**毛玻璃**）与成片同一套参数，改样式
  **即改即见**，不用导出成片才知道字幕长什么样。行级字幕覆盖同样生效
- 中栏舞台加深一级（比工作栏更沉），预览成为视觉中心
- 行块与镜头卡有了悬停反馈

---

## 0.1.30

### 编导台：换音色可一键应用到整片

- 音色选择器多了「**同时应用到整片**」勾选：整片统一声音是常态，
  不用一行行换 27 次。换完旧配音自动标黄，点「生成草片」一键全部
  重新配音（顶栏按钮也会自己变回主角提醒你）

---

## 0.1.29

### 编导台：草片完成有仪式感、主按钮分清主次

- **草片做完有个「好了」的瞬间**：进度收束成一个弹入的绿色对勾
  （「草片好了，这就播给你看」），一拍之后直接开播——不再是列表
  默默刷新
- **顶栏主次动态**：还有句子没配好时「生成草片」是实心蓝主按钮，
  全就绪后让位给「导出成片」——一屏永远只有一个主角

---

## 0.1.28

### 编导台：快捷键与撤销

- **⌘Z 撤销 / ⇧⌘Z 重做**：编导台里的每一步改动（删镜头、改时长、
  换标签、生成配音……）都能一键撤回，敢撤才敢试
- **空格 = 播放/暂停，← → = 前后跳 1 秒**：手不用离开键盘看片；
  正在打字时这些键归输入框，不会打架

---

## 0.1.27

### 编导台：视觉工艺一轮打磨

- **缩略图放大近半**（62→74 宽、卡带加高）：瓶身、人脸、场景在卡上
  认得出来了——看不清的画面等于不存在
- **字号层级收敛成三档**：台词 12 / 控件 11 / 元数据与提示 10，
  比最小档还小的 9px 全数出局；「用它」等核心动作加粗提亮，
  一眼读得出「可以点」
- **该有的过渡都有了**：镜头详情展开/收起 200ms 缓出（不再硬切跳变）、
  行块点亮/选中 180ms 过渡（播放跟随换行不再闪跳）、左缘播放条
  淡入淡出

---

## 0.1.26

### 编导台：预览成为主角——播放跟随行块、进度条可拖

- **播放跟到哪，行块亮到哪**：预览播放时右栏当前行的块自动点亮
  （左缘蓝条+微亮底色）并滚到眼前——看着自己的片子走，不用自己追
- **进度条可以拖了**：预览下面的进度条支持拖动定位，拖到哪从哪继续
- **非常规速度亮出来**：放慢充满产生的 0.68x 这类速度单独显示成
  高亮档，不再让四个预设灰档冒充「没变速」
- 文案继续人话化：「未分配→还没分」「相邻镜头自动让出/补上→多退少补，
  旁边的镜头自动配合」「变速后重新框选→换速度会把起点归零」

---

## 0.1.25

### 编导台：标签接入素材库词表、找镜头三维度检索

- **标签能改了，不只是删**：行块上的标签（还有找镜头面板里的约束标签）
  点开就是标签选择器——素材库的整个标签体系按组铺开、可搜索，
  「促单」想换成别的，搜到点上就行。本任务的标签组排最前
- **找镜头检索重做成三个维度**：**按台词**（这一句说什么，就找说同类话
  的分镜——打开面板默认就用它预搜）/ **按画面描述**（人来描述想要的
  画面）/ **找相似**（拿候选卡的首帧找同款画面）。三个维度随时切
- **标签与项目成为统一约束**：不再是「按标签搜」单独一个模式——
  勾选的标签会贴在**每一种**检索维度上一起生效，不勾就是不限

---

## 0.1.24

### 编导台：素材偏短自动放慢充满；参考分镜播放修复

- **放慢充满（新）**：镜头素材比这一句的配音短时，不再只留一句
  「素材可能不够长」——自动配镜会直接把镜头放慢到刚好铺满整句；
  已有的行在警告旁多了「放慢充满」，一点就好。放慢有底线（0.5x），
  再慢画面就拖沓了，实在不够会说清还差几秒，建议换素材或加镜
- **修复**：点参考分镜的播放弹窗每次都从头放（不是从这一镜开始）——
  现在等视频真正加载完再定位，循环也只在这一镜的区间里转

---

## 0.1.23

### 编导台：一键「生成草片」——片子自己诞生

- **生成草片（新）**：顶栏新增「生成草片」。点一下：没配音的句子自动
  配上音（用全片最近用过的音色，不弹 27 次选择器）、没镜头的句子自动
  从素材库找一个配上（有标签按标签找，没标签按台词找，绝不跟别的句子
  撞素材）、时长自动分好——**全部完成后草片直接播出来**，人只做否决
  和替换
- **花钱的事先说清**：动手前一句话讲明这次要做几次语音合成、给几句
  找镜头；已经全就绪时不再花钱，直接提示按播放看草片
- **从视频提取脚本后顺路到底**：提取完成会直接问「要不要生成草片」，
  答应的话连打标签一起做完，一路播到成片预览
- **进度看得见**：生成期间中央舞台显示「正在给第 x/N 句配音/找镜头」
  和正在处理的台词，不再是转圈不说话
- 个别句子配不上（比如素材库没搜到合适的）会点名说清哪几句要手动补，
  绝不静默少一段

---

## 0.1.22

### 编导台：配乐按行铺、字幕按行改

- **配乐段（行区间铺设）**：顶栏「配乐」现在是段管理——每段选起止行、
  选曲、调音量，可加多段多曲；**相邻两段同一首曲子时播放连续不重头**。
  预览与导出都按段混流；某段曲子没下载完导出会点名拦下
- **行级字幕覆盖**：每行块底多了字幕小图标——素材自带字幕位置不同时，
  单独给这一行改样式（位置/字号/颜色/遮罩），随时可恢复跟随全局；
  已自定义的行图标是蓝色
- 旧任务的整片单曲配乐自动迁移为一段全区间，设置不丢

---

## 0.1.21

### 编导台增强五连：毛玻璃字幕、参考分镜、行级参考、素材原声

- **毛玻璃字幕遮罩（新）**：字幕样式的「遮罩」现在有三选——无 /
  **毛玻璃** / 黑底条。毛玻璃是把字幕背后那块**画面**磨砂模糊，
  不是压一条黑底（导出与切片渲染全线支持）
- **参考分镜（新）**：提取脚本时顺带做视觉切分——一句台词里有几个
  镜头，块里就有几张参考卡（参考1、参考2…），各自可播放、各自
  「用它」一键作镜头
- **行级参考视频（新）**：手写的行也能挂参考——块里点「传参考」
  上传一段视频，之后与提取的参考同等对待
- **画面行素材原声（新）**：画面行不再静音——预览和导出都用所选
  素材自己的声音（变速跟着变调整速，素材没声音才垫静音）
- 修复：编导台/审核页开着时 `ishkafel open` 唤醒没反应（Agent 唤人
  会先收回列表再进目标任务）；「未进预览」的点名在行多时按原因
  分组汇总，不再糊满中栏

---

## 0.1.20

### 编导台布局重做：行带式分镜编辑板 + 参考视频列

- **右栏彻底重做**：不再是「点一行切换一次」的检查器——现在是
  **分镜编辑板**，所有行的工作块从上到下铺开、一块对应一句台词，
  从上往下扫一遍就是扫完整条片子。每块自带：台词、参考段、镜头序列、
  配音控件；镜头详情（时长/起点/速度）点卡在块内展开，再点收起
- **参考视频列（新）**：上传成片提取脚本后，每句台词在原片里的对应
  片段自动挂在块里——带「参考」角标的缩略卡，点击小窗循环播放；
  点「用它」一键把这段原片画面填成本行第一个镜头
- 点任意块 = 选中该行 + 预览跳播到这一句（就绪时）
- 布局调整：左 = 脚本写作区（可编辑），中 = 预览（窄而居中），
  右 = 分镜编辑板（主工作区）；左右台词一一对应
- 设计推演记录见 docs/2026-08-20-编导台行带式布局-设计推演.md

---

## 0.1.19

### 脚本成片全链路打通：写脚本到导出成片，一站到底

**找镜头（新）**
- 右栏「镜头」节：点「找镜头」打开检索面板——行标签自动预填成可勾选
  的筛选条（可一键 AI 自动打标），按标签搜、按画面描述搜、点候选卡上
  的「找相似」以图搜图；别的行已用的素材会标「第 N 行在用」防撞车
- 挑中的镜头按点选顺序排进行里，选中即下载固定到本地（下载状态
  在卡片上，失败可点重试）

**时长分配（新）**
- 挑完镜头自动按行时长均分；点镜头卡展开详情：时长 ±0.5s 步进
  （相邻镜头自动让出/补上，总长锁死）、起点滑杆（框选不必从头）、
  速度四档（变速改可用量、清框重选）；素材不够长会有橙色缺口警告

**整片预览（新）**
- 中栏是真播放器了：配音与镜头就绪的行自动拼成片预览（多轨同步，
  变速镜头预渲对齐切片）；没就绪的行逐条点名原因，不静默变短

**字幕与配乐（新）**
- 顶栏「字幕样式」：位置/字号三档/六色/底条开关，整片生效
- 顶栏「配乐」：搜曲试听、调音量，预览与导出都会带上

**导出成片（新）**
- 顶栏「导出成片」：每镜按框选与变速渲染、字幕按逐字时间戳跨镜
  烧制、配音与配乐混流，产出落在桌面「ishkafel-脚本成片」文件夹；
  有没就绪的行会被逐行点名拦下，绝不悄悄少一段
- 提取脚本完成后可一键给全部台词批量打标（先确认再花钱，逐句进度）

---

## 0.1.18

### 编导台 M2：配音链路点亮

- **行级配音**：右栏配音节可选音色（豆包 2.0 音色库，按场景分组可搜）、
  调语速（0.75x / 1x / 1.25x / 1.5x），点「生成配音」出声——没选过音色
  时会先弹选择器，选完直接生成，不用点两遍
- **显式生成，改字不花钱**：改台词/换音色/调语速只把状态标为「已过期」
  （行首状态点变橙、试听条提示「这是旧配音」），旧配音仍可试听；
  点「重新生成」才调用合成服务
- **配音时长即行时长**：生成后按实际音频量出时长（试听条显示），
  这是该行时间轴的根；逐字时间戳随配音落盘，后续字幕直接用
- 行首状态点三色：灰 = 还没生成，绿 = 配音是最新的，橙 = 内容改过
- 配音文件按任务归档，删任务随目录清走；重新生成成功后才删旧音频，
  失败时旧配音完好

---

## 0.1.17

### 编导台成型：从视频提取脚本 + 全新界面；任务有了短编号

**编导台（脚本成片）**
- **从视频提取脚本**：上传一条参考成片，自动识别台词并按语义分行，
  改几个字就能用（顶栏按钮随时可用；覆盖已写内容前会确认）。提取的
  每一步（读音频 → 识别台词 → 语义分行）都有进度交代，失败给原因和重试
- **界面重做**：空脚本进来先回答「从哪里开始」——提取脚本或直接写，
  两条路两张卡；顶栏带任务编号、名字与「更改已自动保存」状态；左栏
  行列表精致化（选中蓝条、悬停才出拖柄和删除、行号对齐、尾部「新行」）；
  回车新行后光标自动落进去，手不用离开键盘
- 未配置 AI 服务时，提取入口禁用并说明原因，不再是点了没反应

**任务短编号（#1、#2…）**
- 每条任务现在有个念得出口的编号，显示在任务卡和编导台顶栏。跟 Agent
  沟通说「把 #12 导出」就行，不用再报冗长还可能重复的任务名
- 老任务自动按创建时间补号；搜索框输入「#12」或「12」直达
- 命令行全线支持：所有 `<任务 id>` 参数位置直接给 #12 也认，
  新增 `ishkafel tasks` 列出全部任务的编号对照

---

## 0.1.16

### 新模块「脚本成片」（编导台）第一步：写脚本

- 新建任务多了第四种来源——**脚本成片**：不从成片出发，从脚本出发，
  配音配镜长出成片（与「成片翻新」互为镜像）。本版先落地写脚本这一步，
  配音、找镜头、预览、导出在后续版本逐个点亮
- **编导台**（脚本任务的工作页）三栏：左栏写脚本——一行一句台词，
  回车新起一行，空行就是画面行（无台词、只有画面）；行可拖动排序、
  删除；画面行可手填时长（不填则跟随所选素材）
- 脚本**随写随存**（不存在「保存」这回事），关掉重开一字不丢
- 任务卡：脚本任务显示台词句数与「进入编导台」引导；没有原片的任务
  （拼片/脚本）右键菜单不再出现点了必失败的「重新分析」

---

## 0.1.15

### 切分编辑交互重做：边界不可拖，切割跟着播放头走

- **切割**：播放头停在哪一帧，就在那一帧切开——不再要求「先选中、且
  播放头必须落在选中对象范围内」（「播放头不在所选范围内」这条提示
  已退役）。选中视觉镜头时切镜头层，其余情况切台词语义单元层
- **边界不可再拖**：时间线上单元/镜头的边界拖拽已移除。边界修正只用
  属性面板的逐帧按钮——**按住可连发快走**，每走一帧预览画面跟着跳到
  那一帧
- 合并功能不变；有替换素材的段落照旧锁定
- 所有编辑被拦下时的提示都说清原因和下一步

---

## 0.1.14

### 全方位加固：一次产品级审视后的四批优化

**导出正确性（重要）**
- 修复：选 720P / 60fps / HEVC 导出时，替换镜头段落此前按固定的
  1080P/30fps/H.264 编码，与其他段落规格不一致会导致花屏、错位甚至
  导出失败。现在所有段落严格按所选规格渲染，字幕大小也随分辨率
- 修复：空白任务铺了配乐后无法导出的问题
- 替换镜头的字幕不再带标点符号（对齐原片字幕的习惯）

**导出提速**
- 段落并行渲染 + 素材下载/探测去重，多段落任务导出明显变快
- 增量重导：改一个候选再导出，没变的段落直接复用上次的渲染结果

**磁盘不再白占**
- 人声分离的背景轨（每条 30MB、从来没人用）不再落盘
- 变速切片、人声分离等目录纳入统一清理：删任务带走、
  「清理残留产物」可清、预览代理有了 10GB 配额

**操作体验**
- 挑好的素材保存失败会提示并可一键重试（此前静默丢失）
- 删除配乐段、强制接管任务前有确认框
- 导出进度在人声分离阶段会说明「可能要几分钟」，不再像卡死
- 导出默认目录不再记住临时目录

**Agent 能力（配合命令行工具使用时）**
- Agent 提交的方案现在会进入审核页；人审核剔除后，Agent 再导出会被
  点名拦下，绝不把剔掉的素材静默导出去
- 素材检索支持画面描述语义搜（--keyword）、翻页（--page）、
  标签自动收窄；ishkafel task 可查看方案现状；退出码规范化

---

## 0.1.13

### 修复：替换镜头字幕的居中与字号

- 字幕改为正确的居中排列（此前折行的第二行会靠右）
- 字号与原片字幕对齐（原来偏大）

---

## 0.1.12

### 改进：替换镜头的字幕逐字对齐语音，样式对标原片

- 字幕不再按「整句」显示：哪个字的发声时间落在这个镜头里，哪个字才出现
  ——跨镜头的长句前后半句各归各的镜头，不会把上一个镜头说完的话带过来
- 长句自动拆成短段先后出现（优先在标点处断开），不再堆成三行大块字
- 样式对标原片字幕：粗黑描边 + 实心白字

---

## 0.1.11

### 新增：镜头替换保留台词字幕

替换掉视觉镜头后，原片烧在画面里的台词字幕不再跟着消失：被替换的段落
会用分析时的逐句转写**重新渲染**同一句字幕（白字黑描边、底部居中），
和口播声音逐字同步。保留原片的镜头不动；一句话横跨两种镜头时，两边
同一时刻显示同一句，内容连续。预览里看到什么，导出来就是什么。

字幕文字用 macOS 系统渲染（不依赖 ffmpeg 装没装字幕组件——新版
Homebrew 的 ffmpeg 已经不带，这条路在任何机器上都能跑）。

---

## 0.1.10

### 修复 + 加固：素材库连接收口——坏了也只坏得明明白白

上一版修掉的「素材面板误报未找到 miaoa」暴露的是结构问题：每个功能各自去
找 miaoa、各自翻译报错，哪处漏了哪处坏。这一版把所有 miaoa 调用收进唯一
的网关（路径解析、错误分类只此一份），并加了一条架构测试守门——以后新代码
想绕过网关直接测试就红。用户侧的变化：

- 检索失败不再只有一句红字：登录失效直接给「重新登录」按钮，点开登录、
  登录完自动重发检索；其他失败给「重试」按钮
- 所有报错文案统一成一套（登录失效/无权限/断网/内容已删各有各的说法），
  原始报文只进日志，不摊给用户

---

## 0.1.9

### 改进：Agent 说明书粘过去就自动装成永久技能

以前说明书开头是让 Agent「自己把文件存到技能目录」——存不存全看它自觉，
聊久了就忘。现在开头改成一条命令：`ishkafel skill --install`（Claude Code /
Codex 自动认目录；别家工具加 `--dir <目录>` 自报位置）。落盘由软件保证，
装到哪命令会打印出来，Agent 把路径回你，你就知道装成了。装好之后这份
说明书就是它的永久记忆——换会话、聊多久都不会丢。

---

## 0.1.8

### 新增：审核模式——挑完的候选，一屏把关再导出

工作台底部多了「审核候选」：每个位置上「原片这一段 → 替换候选」对照摆开
（真实首帧、时长、镜头替换的变速倍率、★ 预览版标记），**悬停即播、点击
剔除**，一次「确认」后回到工作台接着导出。左侧位置栏显示各处的保留进度。

播放的是本地落好的素材文件，和导出用的是同一份——**看到的就是要交付的**。

Agent 也走同一个能力：跟它说「做完让我审核」，它跑 `ishkafel review` 把
这个页面弹出来；你确认之后告诉它继续，任务里的方案就是审核后的最终结果。
人在审核（或开着工作台）期间 Agent 写不进来，反过来 Agent 操作时人进门
会被提示——谁先进谁处理，可以强制接管。

任务列表右键任务卡也有「审核候选」，随时补审。

### 新增：说明书粘贴即装

说明书开头加了给 Agent 的自举指令：粘过去它的第一个动作就是把自己存成
技能文件（跨会话有效，不再「聊久了就忘」），并回报装到了哪个路径——
你一眼能看出它是真装了还是只读了。

### 修复

- 拖台词语义单元的边界时，被越过的**镜头切点跟着走**，不再被吞掉——
  那些是真实的画面切换，丢了等于让邻居的首镜头横跨好几个切换。转移过去
  的镜头继承原有标签。微调一帧不会再产生碎镜头
- 单元边界的切分锁收紧：两侧单元里任何挑过素材的镜头都拦（原来只查贴边
  那颗，深处的镜头挑过素材时拖边界会让素材静默串位）
- 「改完之后要连带处理吗」在拖拽松手后 0.8 秒就问，不用再等 3 秒

---

## 0.1.7

### 变化

**变速倍率不再设限。** 原来镜头替换的候选必须能在 0.8×~2.0× 内对齐坑位，
超出就拒绝导出。现在拆掉了这道闸：3 秒坑位塞 10 秒素材就按 3.3× 快放导出。

为什么可以拆：预览渲染的就是**真实倍率**的变速切片——你在预览里看到 2.9×
什么样，导出来就是什么样（同一段代码算的同一个倍率），而且镜头替换只变画面、
口播保持原片，声音不会变调。你看过并接受了，软件不该再替你做审美判断。
倍率仍然如实标在时间线与候选卡上。

---

## 0.1.6

### 新增：语义切分与打标可以交给 Agent 自己做

```bash
ishkafel analyze <task> --external=segment,tag
```

analyze 会在该 Agent 做的地方停下来，把**待办**交出去：要什么输入、有哪些
硬约束、做完怎么交回来，一次说清；Agent 做完用 `apply segment` / `apply tags`
回填，软件负责校验和组装。

这不是为了省 API 钱，是为了**让调用它的 Agent 用上自己的判断**——内置的那两步
只看得到文字和时间，Agent 能真的去抽帧看画面再打标签。待办里直接给了源片路径
和每个镜头的抽帧时间点（取中点，两端常踩在转场上）。

**ASR 不可外包**，写了会直接报错。它的时间戳偏 200ms 就毁掉整条链，而且不报错，
一路静默到看成片才发现。能外包的边界是「输出可不可验证」，不是「Agent 会不会」。

回填的东西一律校验：切分必须首尾相接、覆盖全部句子，且不接受调用方改台词文本；
标签必须逐字命中受控词表，不做近似匹配。错误一次全报出来，一批只错一条也整批不落。

`ishkafel todo <task>` 可以把欠着的待办再取一遍——输出丢了不用重跑分析（那会重跑 ASR）。

### 新增：命令行工具随包走，设置里点一下就能用

设置 → 运行环境 → 命令行工具 → 「安装」，弹一次系统授权框，之后在**任意终端**
敲 `ishkafel` 就能用。

以前这东西只能在开发机上自己编，换台电脑就得重来一遍。现在它**随 app 一起走，
Apple Silicon 与 Intel 通用**，使用者不需要知道自己这台是什么芯片，也不用下载
或编译任何东西。

app 换过位置之后命令会指向老路径，设置页会显示「需要重新安装」并说明原因——
不会让人对着一句 `no such file` 发呆。`/usr/local/bin` 里已经有别人的同名命令时
不覆盖，直接说明情况。

### 新增：Agent 说明书，复制全文给任何 Agent

设置 → 运行环境 → Agent 说明书 → 「复制全文」，粘给你的 Agent
（Claude Code、Codex、Cursor、Warp、各家桌面版都行），跟它说
「把这份技能装给你自己」。之后直接说「用 ishkafel 翻新这条片子」就行。
要发给同事就「存成文件」，微信发过去即可。

补的是最后一个缺口：装完 app 和命令行工具，Agent 知道**能调什么**，还不知道
**怎么用才做得出能用的片子**——挑素材要看前后、同一批素材几乎一样要先看图、
方案之间要有可见差异，这些没人告诉它。

为什么是复制全文而不是替你写进技能目录：技能目录每家 Agent 都不一样，
替两家装、别家不管，反而让人以为只支持那两家。说明书本身就是一份 Markdown，
Agent 认字就知道该把它装到自己哪儿。

说明书**编在二进制里**，永远跟这一版的命令对得上。命令行同样能取：
`ishkafel skill` 打印全文。

### 新增：空白任务、导出规格、差异化挑选（GUI 与 CLI 都有）

**不用原片，从素材拼（空白任务）**：新建任务多一条通道，手动加分子、
从标签组词表里选标签，按标签搜素材填进去，拼成新片。原流程只跳过
「有原片」那一步，配乐、矩阵导出全部照旧。CLI 对应 `blank create/add/remove/tags`。

**导出规格对齐剪映**：分辨率 480P~4K、帧率 24~60、码率 推荐/更高/更低/自定义
（推荐档按分辨率×帧率算出具体数字，1080P@30 = 12 Mbps）、编码 H.264/HEVC、
格式 mp4/mov，预计大小实时跟着变。CLI 对应 `export --resolution/--fps/--bitrate/
--codec/--format`。

**挑差异最大的 K 条**：组合太多时可以只导几条彼此差异明显的。距离算在素材
本身的相似度上（同一批拍摄的算「一样」），并尽量让每条素材都露一次面。

**同一条素材不能在一条成片里用两次**：挑的那一刻当场拦（说清已用在哪、
去哪儿解）；Agent 提交方案时同样点名拒绝。

**替换素材也做人声分离**：整体替换的段落铺配乐时用素材的纯人声，
不再和素材自带的背景音叠在一起。预览与导出同一条规则。

### 修复

- `apply` 跑完立刻还锁。原来要等心跳超时 60 秒，这期间人在 app 里打开这个
  任务只能看不能改，还不知道为什么

---

## 0.1.4

### 新增：命令行入口（给 Agent 用）

装了这一版之后多出一个 `ishkafel` 命令，能让 Agent（Claude Code / Codex /
任何能跑 bash 的）独立跑完整条流程：

```bash
ishkafel tag-groups                     # 当前企业有哪些标签组
ishkafel import <视频> --tag-groups 396,365
ishkafel analyze <task>                 # 抽音频 → ASR → 切分 → 打标
ishkafel task <task>                    # 任务全貌
ishkafel candidates <task> --unit 1 --shot 5
ishkafel apply plans <task> --file plans.json
ishkafel export <task> --out ~/Desktop
ishkafel open <task>                    # 把 app 弹出来转人工审核
```

构建：`./scripts/build_cli.sh`，产物在 `build/ishkafel`。
给 Agent 的操作手册在 `docs/AGENT_SKILL.md`——**没有它，CLI 只是一堆能调用
的动作，产不出能用的片子**。

几个刻意的取舍：

- **不做笛卡尔积**。Agent 提交的是完整方案列表，每条都是整体设计过的。
  笛卡尔积隐含「任意搭配都成立」，而挑素材本来就要看前后是否顺畅
- **候选带上下文**：本单元台词、相邻镜头的画面描述与它们已选的素材。只给
  「这个镜头 2.8 秒、标签是厨房清洁」，很容易挑出每一个都合规、连起来很怪的组合
- **导出前如实说代价但不设闸门**——跑不跑是调用方的判断

### 新增：任务锁

Agent 在跑而人打开 GUI，两边都写同一份任务数据会互相覆盖，而且悄无声息。
现在：谁在操作就占着锁，另一边进只读并显示是谁占着；可以强制接管；**锁在
心跳停 60 秒后自动失效**，所以持有者崩了也不会把任务永久封死。

### 修复

- `import` 没指定标签组时明确警告。标签组是打标的受控词表，缺了它后面挑
  替换素材时会没有标签可用——这条链上原本没有任何一步会报错

---

## 0.1.3

### 修复

**登录了却没有企业，界面上只字未提。** 账号登录是通的，但「租户」显示一个
「—」、项目「0 个可选」。这个状态下软件其实什么都干不了：没有企业就没有项目、
没有标签组、检索不到素材。现在：

- 登录完会**确保落到某家企业上**。只有一家的替你选掉不打扰；多家的弹出来让你
  选，且不给跳过；一家都没有会直说要找 miaoa 管理员
- 已经登录但没有企业时，设置页顶上一条橙色警告加一个「选择企业」按钮

上一版把选企业挂在了「登录成功那一刻」，于是**已经登录过的人永远碰不到**——
而那恰恰是会出问题的人所处的状态。

**切换项目只显示了一小部分。** 一个企业下有 69 个项目，界面上只列出 10 个。
两个原因叠在一起：只拉了第一页（每页 20），停用的又在本地被滤掉了。现在翻页
拉全，停用的交给服务端过滤。

**项目多了没法选。** 超过 8 个时出搜索框，可按名称或 ID 搜。本地即时过滤，
同时用 miaoa 的关键词搜索兜底。

### 变化

- 新建任务选标签组时，如果当前企业下**一个标签组都没有**，直说多半是企业选错了
  并指出去哪儿切——原来那句「可以试试标签名」在这种情况下是误导，再怎么换关键词
  都是空的
- 设置页常驻一句提醒：标签组按企业分，选不到标签组时先确认企业

### 内部

- 打包版本号每次自增，`build/dist/` 只保留最新一个包

---

## 0.1.0 及更早（未版本化的包）

> ⚠️ 这一批包**文件名相同、界面看不出区别**，是 8 月 10 日那天陆续打的。
> 版本号自增就是为了终结这件事——今天已经因此误判过一次：以为功能没打进包，
> 其实是对方装的旧包。

### 大的变化

**预览全链路走代理。** 预览用的每一段（原片、候选素材、变速切片）都转成同一个
固定规格（H.264 / 720×1280）。一举解决三件事：

- **Intel Mac 能用了**。原来所有素材要向原片规格看齐，而原片是 HEVC 10bit——
  Intel 的显卡编不了，整条链断掉
- **替换点的顿挫、加速消失了**。段与段规格一致，播放器在接缝处不必重建解码器
- **老机器预览更流畅**。720p 的解码压力比 1080×1920 的 HEVC 10bit 小一个量级

导出**不受影响**，仍然走原始素材（有测试盯死这条边界）。

**在 app 里登录 miaoa。** 设置 → miaoa 账号，手机号收验证码即可，不用再开终端
敲命令。登录状态仍由 miaoa 命令行工具保存，本应用不留任何凭据。

**缺工具就在设置里点一下装。** ffmpeg / miaoa CLI / 人声分离工具，运行环境那一页
直接给「安装」按钮，**走清华镜像**（默认源在国内基本装不上），安装过程逐行显示。

**挑过替换素材的单元/镜头，切分锁死。** 替换方案是按下标记的，切一刀、并一次
下标全变，原本挑给某个镜头的素材会跑到别的镜头上。现在这些段落不能再切分、
合并、改边界，界面上会说明是谁被锁、为什么、怎么解开。

**导出目录可选，并且记得住。** 导出确认页上能看到这个项目之前导过几次、导到哪儿，
每条都能直接打开。

### 修复

- 播到被替换的镜头时播放指针会飞到片尾
- 变速镜头播放时声音像在快进（纠偏 seek 每次都在制造下一次）
- 「正在准备 N 段变速画面」永远转下去
- 整体替换播的一直是没规格化的原始下载
